# purchase-requisition: contract spec

Generated by `scripts/gen-spec.mjs` from the built server. Do not edit by hand: re-run the generator.

| | |
| --- | --- |
| package | `@theluckystrike/mcp-purchase-requisition` |
| version | 0.22.0 |
| bin | `mcp-purchase-requisition` |
| serverInfo.name | `mcp-purchase-requisition` |
| transport | stdio, JSON-RPC 2.0 |
| tools | 16 |
| resources | 1 |
| prompts | 1 |

## What it does

Purchase requisitions you build once and run per order: a template carries its name, description, items with a description, a state (required, optional) and notes; a run snapshots the template's items into its own record with the template's version stamped beside them, moves through a status series from open to approved, rejected or closed, and produces a plain-text document for the approver; the run report totals what was requested, and the export is the record of what was asked for and when. Editing a template afterwards never changes a run already under way.

## Tools (16)

| tool | description |
| --- | --- |
| `license_activate` | Activate a Pro key (MCPL1.xxx.yyy) from checkout: verified offline against a built-in public key, saved to your config file. A wrong, malformed or expired key is refused, unsaved. license_status reads it back. |
| `license_status` | Report this server's licence state: product, tier free or pro, licence id, expiry, the key source and the upgrade URL. No arguments, no network. Explains a free-tier refusal; license_activate installs a key. |
| `purchase-requisition_create` | Create a reusable purchase-requisition and return its CL-NNNN id: a name, a category and an optional description. Add steps with purchase-requisition_item_add. Free tier: 3 of them, runs unlimited. |
| `purchase-requisition_delete` | Delete a purchase-requisition and its steps for good. Runs already started keep their own copy of the steps and stay readable: deleting the purchase-requisition erases nothing anybody signed. |
| `purchase-requisition_item_add` | Add one step to a purchase-requisition and return its I01-style id: text, an optional section heading, and required flag. A required step left unanswered or failed blocks sign-off; an optional one does not. |
| `purchase-requisition_item_remove` | Remove one step from a purchase-requisition by its I01-style id and bump the purchase-requisition version. Runs already under way keep the step they started with, so nothing anybody already ticked is rewritten. |
| `purchase-requisition_list` | Every purchase-requisition with its category, version, step count and how many runs came from it. Filter by category or by a word in the name. Returns at most 500 rows, newest change first. |
| `purchase-requisition_show` | One purchase-requisition with its steps in order, grouped by section, plus how many are required and how many runs have been started from it. Pass as_text for a blank printable copy with a box against each step. |
| `run_check` | Mark one step pass, fail or na, with who did it and when. na means the step did not apply; it counts as answered and never as passed. A step can be answered again while the run is open, and the last answer stands. |
| `run_delete` | Delete a run for good, with every answer on it. A signed-off run is refused: it is the record of what somebody put their name to. Abandon a run you no longer want instead of deleting a signed one. |
| `run_list` | Runs newest first, with their purchaseRequisition, progress and sign-off state. Filter by purchaseRequisition, by status, by reference or to open runs only. Returns at most 500 rows. |
| `run_report` | The run as plain text on every tier: every step with its mark, who answered it, the notes, the counts and a signature block or the recorded signature. Pro also writes it to out_path as a .txt file. |
| `run_show` | The whole run: every step with its answer, who answered it and when, grouped by section, plus the pass, fail and outstanding counts, the failures in full, and whether it can be signed off and why not. |
| `run_sign_off` | Sign off a completed run with a name and a date, which freezes it. Refused while a required step is unanswered or failed, unless force is true, and either way the exceptions stay on the record and print on the report. |
| `run_start` | Start a dated run of a purchase-requisition and return its RUN-YYYY-NNNN id. Steps are COPIED into the run, so editing the purchase-requisition later never changes a run under way. Runs are free, never capped. |
| `run_status` | Move a run back to open so a step can be answered again, or abandon it when the job did not happen. A signed-off run is refused: a signature is the point at which a run stops moving. |

### `license_activate`

Title: Activate license

Activate a Pro key (MCPL1.xxx.yyy) from checkout: verified offline against a built-in public key, saved to your config file. A wrong, malformed or expired key is refused, unsaved. license_status reads it back.

| arg | type | required | description |
| --- | --- | --- | --- |
| `key` | string | yes | License key from the checkout confirmation page |

### `license_status`

Title: License status

Report this server's licence state: product, tier free or pro, licence id, expiry, the key source and the upgrade URL. No arguments, no network. Explains a free-tier refusal; license_activate installs a key.

No arguments.

### `purchase-requisition_create`

Title: Create a purchase-requisition

Create a reusable purchase-requisition and return its CL-NNNN id: a name, a category and an optional description. Add steps with purchase-requisition_item_add. Free tier: 3 of them, runs unlimited.

| arg | type | required | description |
| --- | --- | --- | --- |
| `category` | string | no | A grouping key, lower-cased and hyphenated, e.g. handover or safety. Default general (maxLength 60) |
| `description` | string | no | What this purchase-requisition is for and when to run it, printed at the top of a blank copy (maxLength 2000) |
| `name` | string | yes | What the purchase-requisition is, e.g. Pre-delivery vehicle check or Site handover (maxLength 200) |

### `purchase-requisition_delete`

Title: Delete a purchase-requisition

Delete a purchase-requisition and its steps for good. Runs already started keep their own copy of the steps and stay readable: deleting the purchase-requisition erases nothing anybody signed.

| arg | type | required | description |
| --- | --- | --- | --- |
| `confirm` | true | yes | Must be true. There is no undo and nothing is copied anywhere first |
| `purchaseRequisition` | string | yes | The purchase-requisition id, e.g. CL-0001, or its name when only one carries it (maxLength 200) |

### `purchase-requisition_item_add`

Title: Add a step to a purchase-requisition

Add one step to a purchase-requisition and return its I01-style id: text, an optional section heading, and required flag. A required step left unanswered or failed blocks sign-off; an optional one does not.

| arg | type | required | description |
| --- | --- | --- | --- |
| `note` | string | no | Guidance printed under the step, e.g. the tolerance or the standard it is checked against (maxLength 2000) |
| `position` | integer | no | Insert at this 1-based position instead of at the end. Existing steps keep their ids (min 1, max 500) |
| `purchaseRequisition` | string | yes | The purchase-requisition id, e.g. CL-0001, or its name when only one carries it (maxLength 200) |
| `required` | boolean | no | Whether sign-off is blocked while this step is unanswered or failed. Default true |
| `section` | string | no | A heading to group this step under, e.g. Exterior. Steps keep the order they were added within a section (maxLength 60) |
| `text` | string | yes | The step, as the person doing it will read it, e.g. Tyre pressures checked and recorded (maxLength 2000) |

### `purchase-requisition_item_remove`

Title: Remove a step from a purchase-requisition

Remove one step from a purchase-requisition by its I01-style id and bump the purchase-requisition version. Runs already under way keep the step they started with, so nothing anybody already ticked is rewritten.

| arg | type | required | description |
| --- | --- | --- | --- |
| `item` | string | yes | The step id, e.g. I03, as shown by purchase-requisition_show (maxLength 16) |
| `purchaseRequisition` | string | yes | The purchase-requisition id, e.g. CL-0001, or its name when only one carries it (maxLength 200) |

### `purchase-requisition_list`

Title: List purchase-requisitions

Every purchase-requisition with its category, version, step count and how many runs came from it. Filter by category or by a word in the name. Returns at most 500 rows, newest change first.

| arg | type | required | description |
| --- | --- | --- | --- |
| `category` | string | no | Only purchase-requisitions in this category, matched after the same lower-case hyphenation applied on create (maxLength 60) |
| `contains` | string | no | Only purchase-requisitions whose name contains this text, matched case-insensitively (maxLength 200) |

### `purchase-requisition_show`

Title: Show one purchase-requisition

One purchase-requisition with its steps in order, grouped by section, plus how many are required and how many runs have been started from it. Pass as_text for a blank printable copy with a box against each step.

| arg | type | required | description |
| --- | --- | --- | --- |
| `as_text` | boolean | no | Return a blank printable copy as plain text as well as the structured view. Default false |
| `purchaseRequisition` | string | yes | The purchase-requisition id, e.g. CL-0001, or its name when only one carries it (maxLength 200) |

### `run_check`

Title: Answer a step in a run

Mark one step pass, fail or na, with who did it and when. na means the step did not apply; it counts as answered and never as passed. A step can be answered again while the run is open, and the last answer stands.

| arg | type | required | description |
| --- | --- | --- | --- |
| `at` | string | no | The day it was answered, YYYY-MM-DD. Default the run's own date (maxLength 10) |
| `by` | string | no | Who answered it. Printed against the step on the report (maxLength 200) |
| `item` | string | yes | The step id, e.g. I03, as shown by run_show (maxLength 16) |
| `note` | string | no | What was found. Say why on a fail: the report prints this under the step (maxLength 2000) |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |
| `state` | "pending" \| "pass" \| "fail" \| "na" | yes | pass, fail, na for did not apply, or pending to put the step back to unanswered |

### `run_delete`

Title: Delete a run

Delete a run for good, with every answer on it. A signed-off run is refused: it is the record of what somebody put their name to. Abandon a run you no longer want instead of deleting a signed one.

| arg | type | required | description |
| --- | --- | --- | --- |
| `confirm` | true | yes | Must be true. There is no undo and nothing is copied anywhere first |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |

### `run_list`

Title: List runs

Runs newest first, with their purchaseRequisition, progress and sign-off state. Filter by purchaseRequisition, by status, by reference or to open runs only. Returns at most 500 rows.

| arg | type | required | description |
| --- | --- | --- | --- |
| `open_only` | boolean | no | Only open and complete runs, the ones still editable. Default false |
| `purchaseRequisition` | string | no | Only runs of this purchaseRequisition, by id or name (maxLength 200) |
| `reference` | string | no | Only runs against this job, order or asset id, matched case-insensitively (maxLength 64) |
| `status` | "open" \| "complete" \| "signed_off" \| "abandoned" | no | Only runs in this status |
| `with_failures` | boolean | no | Only runs that have at least one failed step. Default false |

### `run_report`

Title: Produce the run report

The run as plain text on every tier: every step with its mark, who answered it, the notes, the counts and a signature block or the recorded signature. Pro also writes it to out_path as a .txt file.

| arg | type | required | description |
| --- | --- | --- | --- |
| `out_path` | string | no | Where to write the .txt file. Pro only. Omit to get the report back as text, which every tier can do (maxLength 1000) |
| `overwrite` | boolean | no | Replace out_path if a file is already there. Default false, and an existing file is refused with nothing written |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |

### `run_show`

Title: Show one run

The whole run: every step with its answer, who answered it and when, grouped by section, plus the pass, fail and outstanding counts, the failures in full, and whether it can be signed off and why not.

| arg | type | required | description |
| --- | --- | --- | --- |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |

### `run_sign_off`

Title: Sign off a run

Sign off a completed run with a name and a date, which freezes it. Refused while a required step is unanswered or failed, unless force is true, and either way the exceptions stay on the record and print on the report.

| arg | type | required | description |
| --- | --- | --- | --- |
| `by` | string | yes | Who is signing it off, as it should read on the document (maxLength 200) |
| `date` | string | no | The day it was signed, YYYY-MM-DD. Default today (maxLength 10) |
| `force` | boolean | no | Sign off even though required steps are unanswered or failed. Default false; the reasons come back either way |
| `note` | string | no | What the signature covers, or the exception being accepted (maxLength 2000) |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |

### `run_start`

Title: Start a run of a purchase-requisition

Start a dated run of a purchase-requisition and return its RUN-YYYY-NNNN id. Steps are COPIED into the run, so editing the purchase-requisition later never changes a run under way. Runs are free, never capped.

| arg | type | required | description |
| --- | --- | --- | --- |
| `date` | string | no | The day the run happened, YYYY-MM-DD. Default today (maxLength 10) |
| `note` | string | no | (maxLength 2000) |
| `purchaseRequisition` | string | yes | The purchase-requisition id, e.g. CL-0001, or its name when only one carries it (maxLength 200) |
| `reference` | string | no | The job, order or asset id this run belongs to, e.g. WO-2026-0044. Named only; no sibling store is opened (maxLength 64) |
| `title` | string | yes | What this run is against, e.g. Van BX21 KLM, February service (maxLength 200) |

### `run_status`

Title: Reopen or abandon a run

Move a run back to open so a step can be answered again, or abandon it when the job did not happen. A signed-off run is refused: a signature is the point at which a run stops moving.

| arg | type | required | description |
| --- | --- | --- | --- |
| `date` | string | no | The day the step happened, YYYY-MM-DD. Default today (maxLength 10) |
| `note` | string | no | Why. Kept on the run's history and printed nowhere else (maxLength 2000) |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |
| `status` | "open" \| "abandoned" | yes | open puts a complete run back into edit; abandoned closes it without a signature |

## Resources

| uri | name | description |
| --- | --- | --- |
| `purchase-requisition://contract` | contract | Why a run copies its purchaseRequisition, the four item states and what na means, the run status machine, what blocks a sign-off, the free-tier limits and the one directory this server writes. |

## Prompts

| name | description |
| --- | --- |
| `run_the_purchase-requisition` | Pick or build the purchaseRequisition, start a dated run against a job, answer every step with who and when, and sign it off with the exceptions on the record. |

## Invariants

- stdout carries JSON-RPC only. Every diagnostic goes to stderr. A single stray stdout write breaks the client session.
- A tool never throws across the transport. Failures come back as `{ content: [{ type: "text", text: "Error: ..." }], isError: true }`.
- Writes are atomic: the payload goes to `<file>.<pid>.tmp` and is then `rename`d over the target, so a reader never sees a half-written file.
- No partial writes. When a limit or a validation refuses the operation, nothing at all is written; the tool says what was refused and why.
- Money is stored and compared in minor units (integer cents), never as a float. Formatting to a decimal string happens at the edge only.
- Dates are local calendar dates as `YYYY-MM-DD`. No implicit UTC shift is applied to a user-supplied date.
- A store file that fails to parse is quarantined byte-for-byte as `<file>.corrupt-<timestamp>` with a `<file>.corrupt` marker; nothing is overwritten and every later call fails until a human resolves it.
- The load-mutate-save cycle is held under an advisory lock directory (`packages/mcp-license` `withFileLock`), so two processes sharing one data dir cannot lose each other's writes.
- A RUN SNAPSHOTS ITS TEMPLATE, AND THAT IS THE WHOLE SERVER: the items are copied into the run when it starts, text and all, with the template's version recorded beside them. Editing the template afterwards never changes a run already under way, and deleting the template leaves every run readable and complete.
- A RUN'S STATUS MOVES FORWARD ONLY through the named transitions; approving, rejecting or closing one is dated and kept in the run's history, so the record shows who decided what and when, not just the last state.
- ITEM STATES ARE REQUIRED OR OPTIONAL and the run records what was actually asked for against each, so the report can show the gaps without inventing them.
- THE RUN REPORT REFUSES TO INVENT A DECISION: a run still open is reported as open, and the plain-text document says so, so an approver never signs a figure the run has not produced.
- NO SIBLING STORE IS OPENED beyond the profile read of the business name and address printed at the top of a report.
- Hosted at /mcp/purchase-requisition: the mcpb manifest carries remotes.json, and the contract suite asserts the two stay equal by value.

## Free vs Pro

From `servers/purchase-requisition/README.md`.

| | Free | Pro |
| --- | --- | --- |
| Checklists you keep | 3 | unlimited |
| Runs of them | unlimited | unlimited |
| Steps per purchase-requisition | up to 500 | up to 500 |
| `run_show`, `run_list`, the counts | yes | yes |
| The run report as text | yes | yes |
| Writing the report to a file with `out_path` | no | yes |

Enforced limits in the source:

- `FREE_TEMPLATES` = 3 templates on free; runs of a template are unlimited on every tier, and deleting a template frees its slot while every run stays readable.
- `run_report` writing to a .txt file at a path you name is Pro. The refusal is an answer, not a protocol error, and nothing is written.
- `MAX_ITEMS`, `MAX_TEMPLATES` and `MAX_ROWS` = 500 returned by one list answer; template and run names are bounded at 200 characters.

## Storage

Data dir: `${XDG_DATA_HOME:-~/.local/share}/mcp-servers/purchase-requisition/`

| file | holds |
| --- | --- |
| `templates.json` | the requisition templates, each carrying its name, description and items as defined, and nothing derived from them |
| `runs.json` | the runs, each carrying its snapshot of the template's items, its status history and notes as entered, and nothing derived from them |
| `counter.json` | the CL- and RUN- number series |

Primary file (the one a corrupt-store test targets): `runs.json`.

Also written on failure: `<file>.corrupt-<timestamp>` (the quarantined original, byte-for-byte) and `<file>.corrupt` (the marker that blocks every later call). Transient: `<file>.<pid>.tmp` and the `.lock` directory.

Licence key lookup order: `MCP_LICENSE_KEY` env, then `${XDG_CONFIG_HOME:-~/.config}/mcp-servers/license.json`, then free tier.

## Failure modes

Literal error-message heads found in `servers/purchase-requisition/src/*.ts`. Each is returned as `Error: <text>` with `isError: true`, sorted, truncated at the first interpolation.

- `Error: a purchase-requisition named`
- `Error: by is empty. An unsigned signature is not a signature. Nothing was written.`
- `Error: cannot create`
- `Error: cannot read a date:`
- `Error: name is empty. A purchase-requisition nobody can name is a purchase-requisition nobody will find. Nothing was written.`
- `Error: no purchase-requisition matches`
- `Error: no run matches`
- `Error: text is empty. Nothing was written.`
- `Error: the free tier holds`
- `Error: there is no purchase-requisition run yet. Run run_start with a purchase-requisition and a title.`
- `Error: there is no purchase-requisition yet. Run purchase-requisition_create with a name, then purchase-requisition_item_add for each step.`
- `Error: title is empty. A run nobody can point at is a run nobody will find again. Nothing was written.`
