# checklist: contract spec

Generated by `scripts/gen-spec.mjs` from the built server. Do not edit by hand: re-run the generator.

| | |
| --- | --- |
| package | `@theluckystrike/mcp-checklist` |
| version | 0.21.0 |
| bin | `mcp-checklist` |
| serverInfo.name | `mcp-checklist` |
| transport | stdio, JSON-RPC 2.0 |
| tools | 16 |
| resources | 1 |
| prompts | 1 |

## What it does

Checklists you build once and run many times, and the dated record of each run that somebody signs. A checklist is a named list of steps, optionally grouped into sections, each required or optional. A run is one pass of that checklist against a job: every step marked pass, fail or not applicable, with who marked it, on what day, and a note. A run COPIES its checklist's steps when it starts and records the version it copied. Sign-off puts a name and a date on it and freezes it. No count is stored: progress, the failures, and whether a run can be signed off are derived on every call.

## Tools (16)

| tool | description |
| --- | --- |
| `checklist_create` | Create a reusable checklist and return its CL-NNNN id: a name, a category and an optional description. Add the steps with checklist_item_add. Free tier: 3 checklists, and unlimited runs of them. |
| `checklist_delete` | Delete a checklist and its steps for good. Runs already started from it are untouched and stay readable: a run carries its own copy of the steps, so deleting the checklist does not erase what anybody signed. |
| `checklist_item_add` | Add one step to a checklist and return its I01-style id: the text, an optional section heading, and whether it is required. A required step that is unanswered or failed blocks sign-off; an optional one does not. |
| `checklist_item_remove` | Remove one step from a checklist by its I01-style id and bump the checklist version. Runs already under way keep the step they started with, so nothing anybody already ticked is rewritten. |
| `checklist_list` | Every checklist with its category, version, step count and how many runs came from it. Filter by category or by a word in the name. Returns at most 500 rows, newest change first. |
| `checklist_show` | One checklist with its steps in order, grouped by section, plus how many are required and how many runs have been started from it. Pass as_text for a blank printable copy with a box against each step. |
| `license_activate` | Activate a Pro key (MCPL1.xxx.yyy) from checkout: verified offline against a built-in public key, saved to your config file. A wrong, malformed or expired key is refused, unsaved. license_status reads it back. |
| `license_status` | Report this server's licence state: product, tier free or pro, licence id, expiry, the key source and the upgrade URL. No arguments, no network. Explains a free-tier refusal; license_activate installs a key. |
| `run_check` | Mark one step pass, fail or na, with who did it and when. na means the step did not apply; it counts as answered and never as passed. A step can be answered again while the run is open, and the last answer stands. |
| `run_delete` | Delete a run for good, with every answer on it. A signed-off run is refused: it is the record of what somebody put their name to. Abandon a run you no longer want instead of deleting a signed one. |
| `run_list` | Runs newest first, with their checklist, progress and sign-off state. Filter by checklist, by status, by reference or to open runs only. Returns at most 500 rows. |
| `run_report` | The run as plain text on every tier: every step with its mark, who answered it, the notes, the counts and a signature block or the recorded signature. Pro also writes it to out_path as a .txt file. |
| `run_show` | The whole run: every step with its answer, who answered it and when, grouped by section, plus the pass, fail and outstanding counts, the failures in full, and whether it can be signed off and why not. |
| `run_sign_off` | Sign off a completed run with a name and a date, which freezes it. Refused while a required step is unanswered or failed, unless force is true, and either way the exceptions stay on the record and print on the report. |
| `run_start` | Start a dated run of a checklist and return its RUN-YYYY-NNNN id. The steps are COPIED into the run, so editing the checklist afterwards never changes a run already under way. Runs are free and never capped. |
| `run_status` | Move a run back to open so a step can be answered again, or abandon it when the job did not happen. A signed-off run is refused: a signature is the point at which a run stops moving. |

### `checklist_create`

Title: Create a checklist

Create a reusable checklist and return its CL-NNNN id: a name, a category and an optional description. Add the steps with checklist_item_add. Free tier: 3 checklists, and unlimited runs of them.

| arg | type | required | description |
| --- | --- | --- | --- |
| `category` | string | no | A grouping key, lower-cased and hyphenated, e.g. handover or safety. Default general (maxLength 60) |
| `description` | string | no | What this checklist is for and when to run it, printed at the top of a blank copy (maxLength 2000) |
| `name` | string | yes | What the checklist is, e.g. Pre-delivery vehicle check or Site handover (maxLength 200) |

### `checklist_delete`

Title: Delete a checklist

Delete a checklist and its steps for good. Runs already started from it are untouched and stay readable: a run carries its own copy of the steps, so deleting the checklist does not erase what anybody signed.

| arg | type | required | description |
| --- | --- | --- | --- |
| `checklist` | string | yes | The checklist id, e.g. CL-0001, or its name when only one carries it (maxLength 200) |
| `confirm` | true | yes | Must be true. There is no undo and nothing is copied anywhere first |

### `checklist_item_add`

Title: Add a step to a checklist

Add one step to a checklist and return its I01-style id: the text, an optional section heading, and whether it is required. A required step that is unanswered or failed blocks sign-off; an optional one does not.

| arg | type | required | description |
| --- | --- | --- | --- |
| `checklist` | string | yes | The checklist id, e.g. CL-0001, or its name when only one carries it (maxLength 200) |
| `note` | string | no | Guidance printed under the step, e.g. the tolerance or the standard it is checked against (maxLength 2000) |
| `position` | integer | no | Insert at this 1-based position instead of at the end. Existing steps keep their ids (min 1, max 500) |
| `required` | boolean | no | Whether sign-off is blocked while this step is unanswered or failed. Default true |
| `section` | string | no | A heading to group this step under, e.g. Exterior. Steps keep the order they were added within a section (maxLength 60) |
| `text` | string | yes | The step, as the person doing it will read it, e.g. Tyre pressures checked and recorded (maxLength 2000) |

### `checklist_item_remove`

Title: Remove a step from a checklist

Remove one step from a checklist by its I01-style id and bump the checklist version. Runs already under way keep the step they started with, so nothing anybody already ticked is rewritten.

| arg | type | required | description |
| --- | --- | --- | --- |
| `checklist` | string | yes | The checklist id, e.g. CL-0001, or its name when only one carries it (maxLength 200) |
| `item` | string | yes | The step id, e.g. I03, as shown by checklist_show (maxLength 16) |

### `checklist_list`

Title: List checklists

Every checklist with its category, version, step count and how many runs came from it. Filter by category or by a word in the name. Returns at most 500 rows, newest change first.

| arg | type | required | description |
| --- | --- | --- | --- |
| `category` | string | no | Only checklists in this category, matched after the same lower-case hyphenation applied on create (maxLength 60) |
| `contains` | string | no | Only checklists whose name contains this text, matched case-insensitively (maxLength 200) |

### `checklist_show`

Title: Show one checklist

One checklist with its steps in order, grouped by section, plus how many are required and how many runs have been started from it. Pass as_text for a blank printable copy with a box against each step.

| arg | type | required | description |
| --- | --- | --- | --- |
| `as_text` | boolean | no | Return a blank printable copy as plain text as well as the structured view. Default false |
| `checklist` | string | yes | The checklist id, e.g. CL-0001, or its name when only one carries it (maxLength 200) |

### `license_activate`

Title: Activate license

Activate a Pro key (MCPL1.xxx.yyy) from checkout: verified offline against a built-in public key, saved to your config file. A wrong, malformed or expired key is refused, unsaved. license_status reads it back.

| arg | type | required | description |
| --- | --- | --- | --- |
| `key` | string | yes | License key from the checkout confirmation page |

### `license_status`

Title: License status

Report this server's licence state: product, tier free or pro, licence id, expiry, the key source and the upgrade URL. No arguments, no network. Explains a free-tier refusal; license_activate installs a key.

No arguments.

### `run_check`

Title: Answer a step in a run

Mark one step pass, fail or na, with who did it and when. na means the step did not apply; it counts as answered and never as passed. A step can be answered again while the run is open, and the last answer stands.

| arg | type | required | description |
| --- | --- | --- | --- |
| `at` | string | no | The day it was answered, YYYY-MM-DD. Default the run's own date (maxLength 10) |
| `by` | string | no | Who answered it. Printed against the step on the report (maxLength 200) |
| `item` | string | yes | The step id, e.g. I03, as shown by run_show (maxLength 16) |
| `note` | string | no | What was found. Say why on a fail: the report prints this under the step (maxLength 2000) |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |
| `state` | "pending" \| "pass" \| "fail" \| "na" | yes | pass, fail, na for did not apply, or pending to put the step back to unanswered |

### `run_delete`

Title: Delete a run

Delete a run for good, with every answer on it. A signed-off run is refused: it is the record of what somebody put their name to. Abandon a run you no longer want instead of deleting a signed one.

| arg | type | required | description |
| --- | --- | --- | --- |
| `confirm` | true | yes | Must be true. There is no undo and nothing is copied anywhere first |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |

### `run_list`

Title: List runs

Runs newest first, with their checklist, progress and sign-off state. Filter by checklist, by status, by reference or to open runs only. Returns at most 500 rows.

| arg | type | required | description |
| --- | --- | --- | --- |
| `checklist` | string | no | Only runs of this checklist, by id or name (maxLength 200) |
| `open_only` | boolean | no | Only open and complete runs, the ones still editable. Default false |
| `reference` | string | no | Only runs against this job, order or asset id, matched case-insensitively (maxLength 64) |
| `status` | "open" \| "complete" \| "signed_off" \| "abandoned" | no | Only runs in this status |
| `with_failures` | boolean | no | Only runs that have at least one failed step. Default false |

### `run_report`

Title: Produce the run report

The run as plain text on every tier: every step with its mark, who answered it, the notes, the counts and a signature block or the recorded signature. Pro also writes it to out_path as a .txt file.

| arg | type | required | description |
| --- | --- | --- | --- |
| `out_path` | string | no | Where to write the .txt file. Pro only. Omit to get the report back as text, which every tier can do (maxLength 1000) |
| `overwrite` | boolean | no | Replace out_path if a file is already there. Default false, and an existing file is refused with nothing written |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |

### `run_show`

Title: Show one run

The whole run: every step with its answer, who answered it and when, grouped by section, plus the pass, fail and outstanding counts, the failures in full, and whether it can be signed off and why not.

| arg | type | required | description |
| --- | --- | --- | --- |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |

### `run_sign_off`

Title: Sign off a run

Sign off a completed run with a name and a date, which freezes it. Refused while a required step is unanswered or failed, unless force is true, and either way the exceptions stay on the record and print on the report.

| arg | type | required | description |
| --- | --- | --- | --- |
| `by` | string | yes | Who is signing it off, as it should read on the document (maxLength 200) |
| `date` | string | no | The day it was signed, YYYY-MM-DD. Default today (maxLength 10) |
| `force` | boolean | no | Sign off even though required steps are unanswered or failed. Default false; the reasons come back either way |
| `note` | string | no | What the signature covers, or the exception being accepted (maxLength 2000) |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |

### `run_start`

Title: Start a run of a checklist

Start a dated run of a checklist and return its RUN-YYYY-NNNN id. The steps are COPIED into the run, so editing the checklist afterwards never changes a run already under way. Runs are free and never capped.

| arg | type | required | description |
| --- | --- | --- | --- |
| `checklist` | string | yes | The checklist id, e.g. CL-0001, or its name when only one carries it (maxLength 200) |
| `date` | string | no | The day the run happened, YYYY-MM-DD. Default today (maxLength 10) |
| `note` | string | no | (maxLength 2000) |
| `reference` | string | no | The job, order or asset id this run belongs to, e.g. WO-2026-0044. Named only; no sibling store is opened (maxLength 64) |
| `title` | string | yes | What this run is against, e.g. Van BX21 KLM, February service (maxLength 200) |

### `run_status`

Title: Reopen or abandon a run

Move a run back to open so a step can be answered again, or abandon it when the job did not happen. A signed-off run is refused: a signature is the point at which a run stops moving.

| arg | type | required | description |
| --- | --- | --- | --- |
| `date` | string | no | The day the step happened, YYYY-MM-DD. Default today (maxLength 10) |
| `note` | string | no | Why. Kept on the run's history and printed nowhere else (maxLength 2000) |
| `run` | string | yes | The run id, e.g. RUN-2026-0001, or its title when only one carries it (maxLength 200) |
| `status` | "open" \| "abandoned" | yes | open puts a complete run back into edit; abandoned closes it without a signature |

## Resources

| uri | name | description |
| --- | --- | --- |
| `checklist://contract` | contract | Why a run copies its checklist, the four item states and what na means, the run status machine, what blocks a sign-off, the free-tier limits and the one directory this server writes. |

## Prompts

| name | description |
| --- | --- |
| `run_the_checklist` | Pick or build the checklist, start a dated run against a job, answer every step with who and when, and sign it off with the exceptions on the record. |

## Invariants

- stdout carries JSON-RPC only. Every diagnostic goes to stderr. A single stray stdout write breaks the client session.
- A tool never throws across the transport. Failures come back as `{ content: [{ type: "text", text: "Error: ..." }], isError: true }`.
- Writes are atomic: the payload goes to `<file>.<pid>.tmp` and is then `rename`d over the target, so a reader never sees a half-written file.
- No partial writes. When a limit or a validation refuses the operation, nothing at all is written; the tool says what was refused and why.
- Money is stored and compared in minor units (integer cents), never as a float. Formatting to a decimal string happens at the edge only.
- Dates are local calendar dates as `YYYY-MM-DD`. No implicit UTC shift is applied to a user-supplied date.
- A store file that fails to parse is quarantined byte-for-byte as `<file>.corrupt-<timestamp>` with a `<file>.corrupt` marker; nothing is overwritten and every later call fails until a human resolves it.
- The load-mutate-save cycle is held under an advisory lock directory (`packages/mcp-license` `withFileLock`), so two processes sharing one data dir cannot lose each other's writes.
- A RUN SNAPSHOTS ITS CHECKLIST, AND THAT IS THE WHOLE SERVER. The steps are copied into the run when it starts, text and all, with the checklist's version number recorded beside them. Editing the checklist afterwards never changes a run already under way, and deleting the checklist leaves every run readable and complete. Without it, editing a checklist rewrites history: a handover certificate signed for ten checks silently becomes a certificate for eleven, and no field anywhere in the record shows that it happened, so no reader could ever detect it. The contract suite asserts it on the raw `runs.json` bytes and not only through the API.
- NOT APPLICABLE IS NOT A PASS. `na` counts as ANSWERED and never as passed. A step that was looked at and dismissed is a different fact from a step that passed, and merging the two is how a checklist reports full marks for a job where half the steps did not apply. The arithmetic that catches it: `pass + fail + na + pending` equals the item count AND `answered + pending` equals the item count, which only both hold if na sits on the answered side.
- COMPLETE IS A READING, NEVER A CLAIM. A run becomes complete when its last step is answered and goes back to open when a step is set back to pending, and each change is written to the run's history. It is recomputed inside `run_check` rather than set by any tool, so no caller can assert a completeness the steps do not support.
- SETTING A STEP BACK TO PENDING CLEARS WHO ANSWERED IT AND WHEN. A name against an unanswered step is a false record, and it is the kind that survives into a signed document.
- A REQUIRED STEP IS THE ONLY REASON `required` EXISTS. A required step unanswered or failed blocks the signature; an optional one failing does not, though an optional one unanswered does. `force: true` signs anyway and the exceptions stay on the record and print on the report under a heading that names them, so a signature taken over an exception is visible rather than lost.
- SIGN-OFF REFUSES ON THE REASONS, NOT ON THE STATUS MACHINE. A CLOSED run is refused on the machine, because a signature is the point at which a run stops moving. An OPEN one is refused on which steps are outstanding, because `an open run cannot go straight to signed_off` is true and useless. The two can never disagree: the sign-off test is only ready when nothing is pending, and a run with nothing pending is already complete.
- A SIGNED-OFF RUN IS IMMUTABLE ON EVERY EDITING TOOL AND READABLE ON EVERY READING ONE, and it cannot be deleted, because it is the record of what somebody put their name to. An ABANDONED run is closed but is not a signature: it prints the blank signature block and it CAN be deleted.
- NO SIBLING STORE IS OPENED. The job a run is against is a name and nothing more, so a run raised against a work order created on another machine still exists here.
- THERE IS NO MONEY ANYWHERE. A checklist has no amounts: `@theluckystrike/mcp-invoice` is not a dependency, no money function name appears in non-comment source, and no currency symbol appears in src. The contract suite asserts all three.
- THIS SERVER IS STDIO AND .MCPB ONLY, and it ships THREE registry names on one bundle: `checklist`, `snag-list-defect-handover-signoff` and `onboarding-checklist-inspection-runs`. Registry search matches a substring of the full name and never the description, so more names is the only way to be findable on more tokens; the measured landing ranks are 7, 2, 3 and 7 on checklist, snag, handover and onboarding (docs/TOKEN_DEMAND_R1.md). A contract test asserts the tokens are actually present in the names, because a name that lost its token in an edit is a server that silently stops being findable.

## Free vs Pro

From `servers/checklist/README.md`.

| | Free | Pro |
| --- | --- | --- |
| Checklists you keep | 3 | unlimited |
| Runs of them | unlimited | unlimited |
| Steps per checklist | up to 500 | up to 500 |
| `run_show`, `run_list`, the counts | yes | yes |
| The run report as text | yes | yes |
| Writing the report to a file with `out_path` | no | yes |

Enforced limits in the source:

- `FREE_TEMPLATES` = 3 checklists on free. RUNS ARE NEVER CAPPED on any tier, because capping the running of a checklist would cap the only thing a checklist is for. Deleting a checklist frees its slot.
- `run_report` writing a FILE with `out_path` is Pro. The report TEXT comes back on every tier. The refusal is an answer, not a protocol error, and nothing is written to disk before it.
- `MAX_ITEMS` = 500 steps on one checklist; `MAX_TEMPLATES` = 500 checklists; `MAX_SECTION` = 60 characters for a section heading or a category, refused at the schema rather than trimmed.
- `MAX_ROWS` = 500 rows in one `run_list` or `checklist_list` answer. A cut list reports `truncated` with the total and how to narrow it.

## Storage

Data dir: `${XDG_DATA_HOME:-~/.local/share}/mcp-servers/checklist/`

| file | holds |
| --- | --- |
| `templates.json` | the checklists, each carrying its name, category, version and its steps in order, and nothing derived from them |
| `runs.json` | the runs, each carrying its own COPY of the steps it started with, the checklist version it copied, every answer with who and when, and its dated status history |
| `counter.json` | the CL and RUN number series, the RUN series per year of the run date |

Primary file (the one a corrupt-store test targets): `runs.json`.

Also written on failure: `<file>.corrupt-<timestamp>` (the quarantined original, byte-for-byte) and `<file>.corrupt` (the marker that blocks every later call). Transient: `<file>.<pid>.tmp` and the `.lock` directory.

Licence key lookup order: `MCP_LICENSE_KEY` env, then `${XDG_CONFIG_HOME:-~/.config}/mcp-servers/license.json`, then free tier.

## Failure modes

Literal error-message heads found in `servers/checklist/src/*.ts`. Each is returned as `Error: <text>` with `isError: true`, sorted, truncated at the first interpolation.

- `Error: a checklist named`
- `Error: by is empty. An unsigned signature is not a signature. Nothing was written.`
- `Error: cannot create`
- `Error: cannot read a date:`
- `Error: name is empty. A checklist nobody can name is a checklist nobody will find. Nothing was written.`
- `Error: no checklist matches`
- `Error: no run matches`
- `Error: text is empty. Nothing was written.`
- `Error: the free tier holds`
- `Error: there is no checklist run yet. Run run_start with a checklist and a title.`
- `Error: there is no checklist yet. Run checklist_create with a name, then checklist_item_add for each step.`
- `Error: title is empty. A run nobody can point at is a run nobody will find again. Nothing was written.`
