# time-tracker — contract spec

Generated by `scripts/gen-spec.mjs` from the built server. Do not edit by hand: re-run the generator.

| | |
| --- | --- |
| package | `@theluckystrike/mcp-time-tracker` |
| version | 0.4.0 |
| bin | `mcp-time-tracker` |
| serverInfo.name | `time-tracker` |
| transport | stdio, JSON-RPC 2.0 |
| tools | 14 |
| resources | 1 |
| prompts | 1 |

## What it does

A stopwatch and a manual time log per project, with rates, tags, a billable flag, reports and CSV export.

## Tools (14)

| tool | description |
| --- | --- |
| `entry_add` | Log billable (or non-billable) time you already worked on a project - a timesheet entry. Give start plus either end or minutes, and optionally the hourly rate and its currency, e.g. rate '90 euros an hour' -> EUR. |
| `entry_delete` | Delete one time entry by id. |
| `entry_edit` | Change fields of an existing entry. Only the fields you pass are changed. |
| `entry_list` | List logged time entries (timesheet rows) as a compact table, with hours, billable flag and project. Free tier shows the last 7 days. |
| `entry_mark_billed` | Close the loop after an invoice is issued: stamp the tracked hours that went on it with the invoice number, so report and invoice_summary stop offering them and the same hours are never billed twice. Pass the exact ids (the entry_ids invoice_summary returned) or a project plus a from/to range. Already-billed entries are left alone and listed back to you. |
| `export_csv` | Export the timesheet to a CSV file (excel-friendly) you can hand to a bookkeeper: one row per entry with hours, billable, rate, currency and amount. Free tier exports the last 7 days. |
| `invoice_summary` | Turn tracked billable time into invoice line items for one project or client: hours, hourly rate, amount per task and the total, in the currency the work was logged in (EUR 225.00, not $225.00). Free for the last 7 days; Pro invoices any period from the full history. |
| `license_activate` | Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally. |
| `license_status` | Show whether this server runs in free or Pro mode and where to upgrade. |
| `project_set_rate` | Set the hourly rate and currency used to turn tracked hours into money for a project or client. Pass apply_to_existing to re-rate time already logged for that project (add only_missing to touch only entries that carry no rate). |
| `report` | Timesheet report: total tracked hours and billable money for a period, optionally grouped by (group by) project, day, task or tag - hours per project, how much to bill. Omit group_by for the plain total per currency. Money is grouped by currency and never mixes EUR with USD. Free tier covers the last 7 days. |
| `timer_start` | Start a stopwatch for billable work on a project or client (timesheet / hours per project). Only one timer runs at a time: starting a new one stops and logs the previous one. Accepts an hourly rate and a currency, e.g. rate '90 euros'. |
| `timer_status` | Show the running timer, how long it has been running, and today's total hours so far. |
| `timer_stop` | Stop the running timer and log it as a time entry. Returns the duration and the entry id. |

### `entry_add`

Title: Add time entry

Log billable (or non-billable) time you already worked on a project - a timesheet entry. Give start plus either end or minutes, and optionally the hourly rate and its currency, e.g. rate '90 euros an hour' -> EUR.

| arg | type | required | description |
| --- | --- | --- | --- |
| `billable` | boolean | no | Default true; set false for non-billable work |
| `currency` | string | no | Currency of the rate: EUR, USD, GBP, PLN, or words like 'euros'. Defaults to the project currency, else USD. |
| `end` | string | no | ISO 8601 end time (or use minutes) |
| `minutes` | number | no | Duration in minutes (alternative to end) |
| `note` | string | no | Optional note |
| `project` | string | yes | Project or client name. A partial name that matches exactly one existing project is used as that project. (minLength 1) |
| `rate` | number \| string | no | Hourly rate for this entry; a number (90) or the words the user said ('90 euros an hour') |
| `start` | string | yes | ISO 8601 start time, e.g. 2026-09-02T09:00:00 |
| `tags` | string[] | no | Optional tags |
| `task` | string | no | What the work was |

### `entry_delete`

Title: Delete time entry

Delete one time entry by id.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | yes | Entry id from entry_list |

### `entry_edit`

Title: Edit time entry

Change fields of an existing entry. Only the fields you pass are changed.

| arg | type | required | description |
| --- | --- | --- | --- |
| `billable` | boolean | no |  |
| `currency` | string | no | Currency of the rate, e.g. EUR |
| `end` | string | no | ISO 8601 |
| `id` | string | yes | Entry id from entry_list |
| `minutes` | number | no | New duration in minutes, keeps start |
| `note` | string | no |  |
| `project` | string | no |  |
| `rate` | number \| string | no | Hourly rate override for this entry, a number or words like '90 euros' |
| `start` | string | no | ISO 8601 |
| `tags` | string[] | no |  |
| `task` | string | no |  |

### `entry_list`

Title: List time entries

List logged time entries (timesheet rows) as a compact table, with hours, billable flag and project. Free tier shows the last 7 days.

| arg | type | required | description |
| --- | --- | --- | --- |
| `from` | string | no | ISO date/time lower bound |
| `limit` | integer | no | Maximum rows, newest first (default 50) |
| `project` | string | no | Filter by project name |
| `to` | string | no | ISO date/time upper bound |

### `entry_mark_billed`

Title: Mark time entries as billed

Close the loop after an invoice is issued: stamp the tracked hours that went on it with the invoice number, so report and invoice_summary stop offering them and the same hours are never billed twice. Pass the exact ids (the entry_ids invoice_summary returned) or a project plus a from/to range. Already-billed entries are left alone and listed back to you.

| arg | type | required | description |
| --- | --- | --- | --- |
| `billed_at` | string | no | ISO timestamp of the stamp, defaults to now |
| `from` | string | no | ISO date/time start of the billed period, used with project |
| `ids` | string[] | no | Entry ids, e.g. the entry_ids from invoice_summary |
| `invoice_number` | string | yes | The invoice these hours were put on, e.g. INV-2026-0001 (minLength 1) |
| `project` | string | no | Project or client, used with from and to instead of ids |
| `to` | string | no | ISO date/time end of the billed period, used with project |

### `export_csv`

Title: Export entries to CSV

Export the timesheet to a CSV file (excel-friendly) you can hand to a bookkeeper: one row per entry with hours, billable, rate, currency and amount. Free tier exports the last 7 days.

| arg | type | required | description |
| --- | --- | --- | --- |
| `from` | string | no | ISO date/time lower bound |
| `path` | string | no | Target file path; defaults to a file in the local data directory |
| `project` | string | no | Optional project filter |
| `to` | string | no | ISO date/time upper bound |

### `invoice_summary`

Title: Invoice summary

Turn tracked billable time into invoice line items for one project or client: hours, hourly rate, amount per task and the total, in the currency the work was logged in (EUR 225.00, not $225.00). Free for the last 7 days; Pro invoices any period from the full history.

| arg | type | required | description |
| --- | --- | --- | --- |
| `from` | string | yes | ISO date/time start of the billing period |
| `project` | string | yes | Project or client to invoice (minLength 1) |
| `to` | string | yes | ISO date/time end of the billing period |
| `unbilled_only` | boolean | no | Default true: hours already put on an invoice (entry_mark_billed) are left out, so the same hours are never billed twice. Pass false to see the whole period including invoiced work. |

### `license_activate`

Title: Activate license

Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally.

| arg | type | required | description |
| --- | --- | --- | --- |
| `key` | string | yes | License key from the checkout confirmation page |

### `license_status`

Title: License status

Show whether this server runs in free or Pro mode and where to upgrade.

No arguments.

### `project_set_rate`

Title: Set project rate

Set the hourly rate and currency used to turn tracked hours into money for a project or client. Pass apply_to_existing to re-rate time already logged for that project (add only_missing to touch only entries that carry no rate).

| arg | type | required | description |
| --- | --- | --- | --- |
| `apply_to_existing` | boolean | no | Re-rate time already logged for this project: every entry is re-stamped with the new rate, including entries that already carry one. Default false: the new rate applies to future entries only. |
| `currency` | string | no | Currency: a code (EUR, USD, GBP, PLN) or a word ('euros', 'pounds', 'zl'). Default USD. |
| `hourly_rate` | number \| string | yes | Hourly rate: a number (85) or the words the user said ('90 euros an hour'). '1,200 USD' is 1200; '12,50 EUR' is 12.50; anything ambiguous is refused. |
| `only_missing` | boolean | no | Only meaningful with apply_to_existing. True restores the old fill-the-gaps behaviour: only entries that carry no rate of their own are touched. Default false, which re-stamps every entry of the project. |
| `project` | string | yes | Project or client name. A partial name that matches exactly one existing project is used as that project. (minLength 1) |

### `report`

Title: Time report

Timesheet report: total tracked hours and billable money for a period, optionally grouped by (group by) project, day, task or tag - hours per project, how much to bill. Omit group_by for the plain total per currency. Money is grouped by currency and never mixes EUR with USD. Free tier covers the last 7 days.

| arg | type | required | description |
| --- | --- | --- | --- |
| `format` | "table" \| "json" \| "csv" | no | table (default), json or csv |
| `from` | string | yes | ISO date/time start of the period |
| `group_by` | "project" \| "day" \| "task" \| "tag" | no | project \| day \| task \| tag. Optional: omit it for the plain total per currency, with no breakdown. |
| `project` | string | no | Optional project filter |
| `to` | string | yes | ISO date/time end of the period |
| `unbilled_only` | boolean | no | Default true: hours already put on an invoice (entry_mark_billed) are excluded, so the report answers 'what is still to bill'. Pass false for the full timesheet including invoiced work. |

### `timer_start`

Title: Start timer

Start a stopwatch for billable work on a project or client (timesheet / hours per project). Only one timer runs at a time: starting a new one stops and logs the previous one. Accepts an hourly rate and a currency, e.g. rate '90 euros'.

| arg | type | required | description |
| --- | --- | --- | --- |
| `currency` | string | no | Currency of the rate: EUR, USD, GBP, PLN, or words like 'euros'. Defaults to the project currency, else USD. |
| `project` | string | yes | Project or client name, e.g. 'acme-website'. A partial name that matches exactly one existing project is used as that project. (minLength 1) |
| `rate` | number \| string | no | Hourly rate for this timer only; a number (90) or the words the user said ('90 euros an hour'). Defaults to the project rate. |
| `tags` | string[] | no | Free-form tags, e.g. ['dev','meeting'] |
| `task` | string | no | What you are working on right now |

### `timer_status`

Title: Timer status

Show the running timer, how long it has been running, and today's total hours so far.

No arguments.

### `timer_stop`

Title: Stop timer

Stop the running timer and log it as a time entry. Returns the duration and the entry id.

| arg | type | required | description |
| --- | --- | --- | --- |
| `note` | string | no | Optional note stored with the entry |

## Resources

| uri | name | description |
| --- | --- | --- |
| `timetracker://today` | today | Summary of time tracked today, by project, including any running timer. |

## Prompts

| name | description |
| --- | --- |
| `daily_standup` | Write a standup update from the time tracked yesterday and today. |

## Invariants

- stdout carries JSON-RPC only. Every diagnostic goes to stderr. A single stray stdout write breaks the client session.
- A tool never throws across the transport. Failures come back as `{ content: [{ type: "text", text: "Error: ..." }], isError: true }`.
- Writes are atomic: the payload goes to `<file>.<pid>.tmp` and is then `rename`d over the target, so a reader never sees a half-written file.
- No partial writes. When a limit or a validation refuses the operation, nothing at all is written; the tool says what was refused and why.
- Money is stored and compared in minor units (integer cents), never as a float. Formatting to a decimal string happens at the edge only.
- Dates are local calendar dates as `YYYY-MM-DD`. No implicit UTC shift is applied to a user-supplied date.
- A store file that fails to parse is quarantined byte-for-byte as `<file>.corrupt-<timestamp>` with a `<file>.corrupt` marker; nothing is overwritten and every later call fails until a human resolves it.
- The load-mutate-save cycle is held under an advisory lock directory (`packages/mcp-license` `withFileLock`), so two processes sharing one data dir cannot lose each other's writes.
- `timer_stop` with no timer running is a normal answer, not an error.
- An entry needs either an `end` or `minutes`; `end` before `start` is refused.
- `entry_mark_billed` is the seam to invoice: only it flips the billed flag, so hours cannot be invoiced twice.

## Free vs Pro

From `servers/time-tracker/README.md`.

_No "Free vs Pro" section in the README._

Enforced limits in the source:

- `FREE_WINDOW_DAYS` = 7 days of history readable on free.
- `FREE_RATED_PROJECTS` = 2 projects with a rate on free.

## Storage

Data dir: `${XDG_DATA_HOME:-~/.local/share}/mcp-servers/time-tracker/`

| file | holds |
| --- | --- |
| `data.json` | entries, projects and rates, the running timer |

Primary file (the one a corrupt-store test targets): `data.json`.

Also written on failure: `<file>.corrupt-<timestamp>` (the quarantined original, byte-for-byte) and `<file>.corrupt` (the marker that blocks every later call). Transient: `<file>.<pid>.tmp` and the `.lock` directory.

Licence key lookup order: `MCP_LICENSE_KEY` env, then `${XDG_CONFIG_HOME:-~/.config}/mcp-servers/license.json`, then free tier.

## Failure modes

Literal error-message heads found in `servers/time-tracker/src/*.ts`. Each is returned as `Error: <text>` with `isError: true`, sorted, truncated at the first interpolation.

- `Error: cannot read the data file`
- `Error: data file is corrupt; moved to`
- `Error: end must be after start`
- `Error: give either end or minutes`
- `Error: hourly_rate must contain a number`
- `Error: no entry with id`
- `Error: pass either ids: [`
- `Error: rate must be a non-negative number, got`
- `Error: rate must be a non-negative number:`
- `Error: rate must contain a number, got`
