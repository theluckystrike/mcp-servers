# calendar: contract spec

Generated by `scripts/gen-spec.mjs` from the built server. Do not edit by hand: re-run the generator.

| | |
| --- | --- |
| package | `@theluckystrike/mcp-calendar` |
| version | 0.19.0 |
| bin | `mcp-calendar` |
| serverInfo.name | `mcp-calendar` |
| transport | stdio, JSON-RPC 2.0 |
| tools | 12 |
| resources | 1 |
| prompts | 1 |

## What it does

Reads .ics calendar exports into a local store and answers questions about them: what is on, what is free, what conflicts, what is next, and writes a chosen set of events back out as a new .ics file.

## Tools (12)

| tool | description |
| --- | --- |
| `calendars_list` | Every calendar imported into this server: its name, where it came from, how many event definitions it holds and when it was imported. |
| `conflicts` | Every pair of events overlapping in time, with minutes they collide. Across all calendars unless one named, so a clashing work/family event is caught. Whole-day events reported separately. Free: 31 days; Pro: any window. |
| `event_export` | Call this tool to write chosen events to a new .ics file you can send or import elsewhere. Pass either ids (from events_list) or a from/to window. Times are written in UTC so the file lands correctly in any client. |
| `event_to_time_entry` | Take one event and return the exact arguments for the time-tracker's entry_add, so a meeting that already happened becomes billable time without retyping it. Writes nothing: pass the JSON straight to entry_add. |
| `events_list` | Every event between two dates, recurring expanded to occurrences, sorted by start. Times shown in your zone (profile's, else this machine's) unless zone passed. Each id works for event_export or event_to_time_entry. |
| `events_search` | Find events whose title, description, location or attendees contain a phrase. Searches every imported calendar. Without from/to it looks a year back and a year forward on Pro, and the free window either side of today. |
| `free_busy` | Where the time actually went: merged busy blocks from the calendars named, and the gaps in your working hours where nothing is booked. Free/transparent events do not count as busy; whole-day events block the day. |
| `ics_forget` | Remove one imported calendar and the local copy of its .ics file. Nothing else is touched. |
| `ics_import` | Call this tool to read a calendar export and keep it under a name. Give path (.ics file), text (contents), or url (public .ics/webcal feed; Pro). Google, Apple, Outlook exports read. Re-importing a name replaces it. |
| `license_activate` | Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally. |
| `license_status` | Show whether this server runs in free or Pro mode and where to upgrade. |
| `next_event` | The next event that has not started yet, with how long until it begins. Looks ahead up to a year. |

### `calendars_list`

Title: List imported calendars

Every calendar imported into this server: its name, where it came from, how many event definitions it holds and when it was imported.

No arguments.

### `conflicts`

Title: Find double bookings

Every pair of events overlapping in time, with minutes they collide. Across all calendars unless one named, so a clashing work/family event is caught. Whole-day events reported separately. Free: 31 days; Pro: any window.

| arg | type | required | description |
| --- | --- | --- | --- |
| `calendar` | string | no | One calendar name; default every imported calendar (maxLength 80) |
| `from` | string | yes | First day, YYYY-MM-DD (maxLength 20) |
| `to` | string | yes | Last day, YYYY-MM-DD, included (maxLength 20) |

### `event_export`

Title: Export events to a .ics file

Call this tool to write chosen events to a new .ics file you can send or import elsewhere. Pass either ids (from events_list) or a from/to window. Times are written in UTC so the file lands correctly in any client.

| arg | type | required | description |
| --- | --- | --- | --- |
| `calendar` | string | no | With from/to: limit to one calendar (maxLength 80) |
| `from` | string | no | First day, YYYY-MM-DD (alternative to ids) (maxLength 20) |
| `ids` | string[] | no | Event ids from events_list, events_search or next_event |
| `out_path` | string | yes | Where to write the .ics file (maxLength 4096) |
| `to` | string | no | Last day, YYYY-MM-DD, included (maxLength 20) |

### `event_to_time_entry`

Title: Turn a meeting into a time entry

Take one event and return the exact arguments for the time-tracker's entry_add, so a meeting that already happened becomes billable time without retyping it. Writes nothing: pass the JSON straight to entry_add.

| arg | type | required | description |
| --- | --- | --- | --- |
| `currency` | string | no | Currency of the rate: EUR, USD, GBP, PLN, or the word the user said ('euros'). Without it the time-tracker falls back to USD. (maxLength 20) |
| `event_id` | string | yes | Event id from events_list, events_search or next_event (maxLength 200) |
| `project` | string | yes | Project or client the meeting is billed to (maxLength 80) |
| `rate` | number \| string | no | Hourly rate for this entry; a number (120) or the words the user said ('120 euros an hour') |

### `events_list`

Title: List events in a window

Every event between two dates, recurring expanded to occurrences, sorted by start. Times shown in your zone (profile's, else this machine's) unless zone passed. Each id works for event_export or event_to_time_entry.

| arg | type | required | description |
| --- | --- | --- | --- |
| `calendar` | string | no | One calendar name; default every imported calendar (maxLength 80) |
| `from` | string | yes | First day, YYYY-MM-DD (maxLength 20) |
| `to` | string | yes | Last day, YYYY-MM-DD, included (maxLength 20) |
| `zone` | string | no | Show local times in this zone or city instead of your own (maxLength 100) |

### `events_search`

Title: Search events

Find events whose title, description, location or attendees contain a phrase. Searches every imported calendar. Without from/to it looks a year back and a year forward on Pro, and the free window either side of today.

| arg | type | required | description |
| --- | --- | --- | --- |
| `from` | string | no | First day, YYYY-MM-DD (maxLength 20) |
| `query` | string | yes | Words to look for, case-insensitive (maxLength 200) |
| `to` | string | no | Last day, YYYY-MM-DD, included (maxLength 20) |

### `free_busy`

Title: Busy blocks and free windows

Where the time actually went: merged busy blocks from the calendars named, and the gaps in your working hours where nothing is booked. Free/transparent events do not count as busy; whole-day events block the day.

| arg | type | required | description |
| --- | --- | --- | --- |
| `calendars` | string[] | no | Calendar names; default all of them |
| `from` | string | yes | First day, YYYY-MM-DD (maxLength 20) |
| `to` | string | yes | Last day, YYYY-MM-DD, included (maxLength 20) |
| `work_end` | string | no | End of your working day, HH:MM, default 17:00 (maxLength 10) |
| `work_start` | string | no | Start of your working day, HH:MM, default 09:00 (maxLength 10) |
| `zone` | string | no | Zone the working hours and the output are in; default your own (maxLength 100) |

### `ics_forget`

Title: Forget a calendar

Remove one imported calendar and the local copy of its .ics file. Nothing else is touched.

| arg | type | required | description |
| --- | --- | --- | --- |
| `name` | string | yes | The calendar name from calendars_list (maxLength 80) |

### `ics_import`

Title: Import a calendar (.ics)

Call this tool to read a calendar export and keep it under a name. Give path (.ics file), text (contents), or url (public .ics/webcal feed; Pro). Google, Apple, Outlook exports read. Re-importing a name replaces it.

| arg | type | required | description |
| --- | --- | --- | --- |
| `name` | string | yes | What to call this calendar, e.g. "work" or "family" (maxLength 80) |
| `path` | string | no | Path to a .ics file on this machine (maxLength 4096) |
| `text` | string | no | The .ics file contents, pasted (maxLength 5242880) |
| `url` | string | no | Public https:// or webcal:// .ics feed. Fetched once, only because you asked; Pro feature (maxLength 4096) |

### `license_activate`

Title: Activate license

Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally.

| arg | type | required | description |
| --- | --- | --- | --- |
| `key` | string | yes | License key from the checkout confirmation page |

### `license_status`

Title: License status

Show whether this server runs in free or Pro mode and where to upgrade.

No arguments.

### `next_event`

Title: Next event

The next event that has not started yet, with how long until it begins. Looks ahead up to a year.

| arg | type | required | description |
| --- | --- | --- | --- |
| `calendar` | string | no | One calendar name; default every imported calendar (maxLength 80) |

## Resources

| uri | name | description |
| --- | --- | --- |
| `calendar://today` | today | Every event today across all imported calendars, in your own time zone. |

## Prompts

| name | description |
| --- | --- |
| `plan_my_day` | Read today's calendar, find the free stretches, and turn the meetings that already happened into billable time. |

## Invariants

- stdout carries JSON-RPC only. Every diagnostic goes to stderr. A single stray stdout write breaks the client session.
- A tool never throws across the transport. Failures come back as `{ content: [{ type: "text", text: "Error: ..." }], isError: true }`.
- Writes are atomic: the payload goes to `<file>.<pid>.tmp` and is then `rename`d over the target, so a reader never sees a half-written file.
- No partial writes. When a limit or a validation refuses the operation, nothing at all is written; the tool says what was refused and why.
- Dates are local calendar dates as `YYYY-MM-DD`. No implicit UTC shift is applied to a user-supplied date.
- A store file that fails to parse is quarantined byte-for-byte as `<file>.corrupt-<timestamp>` with a `<file>.corrupt` marker; nothing is overwritten and every later call fails until a human resolves it.
- The load-mutate-save cycle is held under an advisory lock directory (`packages/mcp-license` `withFileLock`), so two processes sharing one data dir cannot lose each other's writes.
- There is exactly one network call in the server, and only when the caller passes a `url`: one fetch, 12-second timeout, 5 MB cap, and loopback, private and cloud-metadata addresses refused before and after any redirect.
- Folds are rejoined on the raw bytes before decoding, so a multi-byte character split across an Outlook fold survives (D-24).
- `FREQ=HOURLY` is expanded; `FREQ=SECONDLY` and `FREQ=MINUTELY` are deliberately not, and the note says why. `BYSETPOS`, `BYDAY` ordinals and `RDATE`/`EXDATE` are parsed (D-12, D-14).
- A `DTEND` before `DTSTART` is read as zero length and says so rather than rendering a plausible appointment (D-19).
- A `TZID` that `Intl` does not know keeps the event, read as local time, with the reason named once per file.
- Exported times are written in UTC, and one occurrence of a recurring event is one addressable UID, not twelve VEVENTs sharing one.
- A per-process parse cache is keyed on the stored file's path, size and mtime, so four questions over a 20,000-event calendar do not re-parse it four times.
- ORGANIZER and ATTENDEE (with CN, ROLE, PARTSTAT, RSVP and the mailto value) are carried through export exactly as parsed from the source .ics, never re-derived or invented; an event imported with no attendees exports none (D-R61).

## Free vs Pro

From `servers/calendar/README.md`.

| | Free | Pro ($19 one-time) |
| --- | --- | --- |
| Calendars kept | 2 | unlimited |
| Window per question | up to 31 days | any window |
| `conflicts` over a longer window | answered for the first 31 days, cap named | any window |
| Events per export | 50 | unlimited |
| Import from a URL or webcal feed | -- | yes |
| Everything else | full | full |

Enforced limits in the source:

- `FREE_MAX_CALENDARS` = 2 calendars kept on free.
- `FREE_MAX_WINDOW_DAYS` = 31 days per question on free.
- `FREE_MAX_EXPORT_EVENTS` = 50 events per `event_export` on free. Over the cap nothing is written.
- `conflicts` over a longer window is answered for the first 31 days with the cap named, not refused (docs/GUARDRAILS_RESULT.md).
- Importing from a `url` or webcal feed is Pro. The refusal names the free path that gets the same result: paste the file contents as `text`.
- `MAX_BYTES` = 5 MB per fetched feed, `MAX_TEXT_BYTES` = 5 MB of pasted text, `MAX_WINDOW_DAYS` = 1830, `MAX_IDS` = 500, `MAX_LIST_ROWS` = 500.
- `MAX_CANDIDATES` = 100000 and `MAX_OCCURRENCES` = 20000 bound recurrence expansion, so an endless RRULE cannot run away.

## Storage

Data dir: `${XDG_DATA_HOME:-~/.local/share}/mcp-servers/calendar/`

| file | holds |
| --- | --- |
| `data.json` | imported calendars, their events and the source they came from |

Primary file (the one a corrupt-store test targets): `data.json`.

Also written on failure: `<file>.corrupt-<timestamp>` (the quarantined original, byte-for-byte) and `<file>.corrupt` (the marker that blocks every later call). Transient: `<file>.<pid>.tmp` and the `.lock` directory.

Licence key lookup order: `MCP_LICENSE_KEY` env, then `${XDG_CONFIG_HOME:-~/.config}/mcp-servers/license.json`, then free tier.

## Failure modes

Literal error-message heads found in `servers/calendar/src/*.ts`. Each is returned as `Error: <text>` with `isError: true`, sorted, truncated at the first interpolation.

- `Error: give exactly one of path, text or url, not several.`
- `Error: name cannot be empty.`
- `Error: no calendar called`
- `Error: nothing to export (`
- `Error: only http, https and webcal URLs are supported (got`
- `Error: project cannot be empty.`
- `Error: query cannot be empty.`
- `Error: refusing to fetch`
- `Error: that event is no longer in`
- `Error: that id belongs to a calendar (`
- `Error: that id has an unreadable date.`
- `Error: that window is`
- `Error: the calendar file is empty.`
- `Error: the feed is larger than`
- `Error: the feed redirected to`
- `Error: the feed returned HTTP`
- `Error: the stored copy of`
- `Error: this does not look like a calendar file: no BEGIN:VCALENDAR and no VEVENT was found.`
- `Error: this recurrence rule produces too many dates to expand; narrow the window.`
