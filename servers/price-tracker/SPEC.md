# price-tracker — contract spec

Generated by `scripts/gen-spec.mjs` from the built server. Do not edit by hand: re-run the generator.

| | |
| --- | --- |
| package | `@theluckystrike/mcp-price-tracker` |
| version | 0.4.2 |
| bin | `mcp-price-tracker` |
| serverInfo.name | `mcp-price-tracker` |
| transport | stdio, JSON-RPC 2.0 |
| tools | 10 |
| resources | 1 |
| prompts | 1 |

## What it does

Watches a public product page for its price, keeps the history, and reports drops against a target.

## Tools (10)

| tool | description |
| --- | --- |
| `alerts_pending` | Answer "did anything I watch get cheaper?": lists every watch whose latest price is at or below its target, or which dropped 5% or more against the previous observation, with the change % and the reading confidence. |
| `license_activate` | Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally. |
| `license_status` | Show whether this server runs in free or Pro mode and where to upgrade. |
| `price_add_manual` | Call this tool to store a price you read yourself, for shops that block automated requests. Creates the watch for that URL if it does not exist yet, and returns the stored price and the observation count. |
| `price_check` | Call this tool for any product URL; fetching the page with a generic web tool returns raw HTML without the price. Returns price, currency, title, confidence and the change since the last check. |
| `price_history` | Call this tool to list the stored observations for one watch, newest last, each with its price, currency, source and extraction confidence. Free shows the last 30. |
| `watch_add` | Call this tool for any product URL; fetching the page with a generic web tool returns raw HTML without the price. Starts tracking it: first observation, optional target, re-read by watch_refresh. |
| `watch_list` | Show every tracked item with its current price, previous price, min, max, change %, target, extraction confidence and last check time. Prices are as of the last watch_refresh, not live. |
| `watch_refresh` | This is what actually checks prices: re-fetches one watch or every watch, appends the new observations and returns current, previous, min, max, change %, extraction confidence and target hits. |
| `watch_remove` | Call this tool to stop tracking an item, by watch id or by URL. Give either id or url. Its stored observation history is deleted and cannot be recovered. |

### `alerts_pending`

Title: Pending alerts

Answer "did anything I watch get cheaper?": lists every watch whose latest price is at or below its target, or which dropped 5% or more against the previous observation, with the change % and the reading confidence.

No arguments.

### `license_activate`

Title: Activate license

Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally.

| arg | type | required | description |
| --- | --- | --- | --- |
| `key` | string | yes | License key from the checkout confirmation page |

### `license_status`

Title: License status

Show whether this server runs in free or Pro mode and where to upgrade.

No arguments.

### `price_add_manual`

Title: Record a price by hand

Call this tool to store a price you read yourself, for shops that block automated requests. Creates the watch for that URL if it does not exist yet, and returns the stored price and the observation count.

| arg | type | required | description |
| --- | --- | --- | --- |
| `currency` | string | no | ISO code such as USD or EUR |
| `label` | string | no | Short name for this item |
| `price` | string \| number | yes | Price as shown, for example 1299.00 or 1.299,00 |
| `url` | string | yes | Product page URL |

### `price_check`

Title: Check a price now

Call this tool for any product URL; fetching the page with a generic web tool returns raw HTML without the price. Returns price, currency, title, confidence and the change since the last check.

| arg | type | required | description |
| --- | --- | --- | --- |
| `url` | string | yes | Product page URL, including https:// |

### `price_history`

Title: Price history

Call this tool to list the stored observations for one watch, newest last, each with its price, currency, source and extraction confidence. Free shows the last 30.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | no | Watch id |
| `limit` | integer | no | Maximum observations to return |
| `url` | string | no | Watch URL |

### `watch_add`

Title: Watch a price

Call this tool for any product URL; fetching the page with a generic web tool returns raw HTML without the price. Starts tracking it: first observation, optional target, re-read by watch_refresh.

| arg | type | required | description |
| --- | --- | --- | --- |
| `currency` | string | no | ISO code such as USD or EUR, if the page does not say |
| `label` | string | no | Short name for this item |
| `target_price` | string \| number | no | Report this watch in alerts_pending when the price is at or below this (positive number) |
| `url` | string | yes | Product page URL |

### `watch_list`

Title: List watches

Show every tracked item with its current price, previous price, min, max, change %, target, extraction confidence and last check time. Prices are as of the last watch_refresh, not live.

No arguments.

### `watch_refresh`

Title: Refresh prices

This is what actually checks prices: re-fetches one watch or every watch, appends the new observations and returns current, previous, min, max, change %, extraction confidence and target hits.

| arg | type | required | description |
| --- | --- | --- | --- |
| `all` | boolean | no | Refresh every watch in one call (Pro; on free, refresh one id at a time) |
| `id` | string | no | Watch id or URL to re-fetch. Omit and set all=true to refresh everything. Nothing runs in the background, so call this whenever the user asks about prices, drops or alerts - typically once at the start of a session, then read alerts_pending. |

### `watch_remove`

Title: Remove a watch

Call this tool to stop tracking an item, by watch id or by URL. Give either id or url. Its stored observation history is deleted and cannot be recovered.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | no | Watch id from watch_list |
| `url` | string | no | URL of the watch, if you do not have the id |

## Resources

| uri | name | description |
| --- | --- | --- |
| `prices://watches` | watches | Every watch with its latest price, target and history size. |

## Prompts

| name | description |
| --- | --- |
| `check_prices` | Refresh every watch and summarise which items dropped and which hit their target. One command for a whole price check. |

## Invariants

- stdout carries JSON-RPC only. Every diagnostic goes to stderr. A single stray stdout write breaks the client session.
- A tool never throws across the transport. Failures come back as `{ content: [{ type: "text", text: "Error: ..." }], isError: true }`.
- Writes are atomic: the payload goes to `<file>.<pid>.tmp` and is then `rename`d over the target, so a reader never sees a half-written file.
- No partial writes. When a limit or a validation refuses the operation, nothing at all is written; the tool says what was refused and why.
- Money is stored and compared in minor units (integer cents), never as a float. Formatting to a decimal string happens at the edge only.
- Dates are local calendar dates as `YYYY-MM-DD`. No implicit UTC shift is applied to a user-supplied date.
- A store file that fails to parse is quarantined byte-for-byte as `<file>.corrupt-<timestamp>` with a `<file>.corrupt` marker; nothing is overwritten and every later call fails until a human resolves it.
- The load-mutate-save cycle is held under an advisory lock directory (`packages/mcp-license` `withFileLock`), so two processes sharing one data dir cannot lose each other's writes.
- The free-tier check is re-evaluated against a freshly loaded store after the network fetch, because a limit checked before an `await` is not a limit.
- `watch_refresh` merges into a freshly loaded store, so a watch added during a refresh is not dropped.
- The page read is capped at 2 MB. Only http and https are fetched.
- Quarantine wording differs here: the store calls it `the price database ... is not valid JSON`.

## Free vs Pro

From `servers/price-tracker/README.md`.

| | Free | Pro ($19 one-time) |
| --- | --- | --- |
| `price_check` | Unlimited | Unlimited |
| `alerts_pending` | Yes, unlimited | Yes, unlimited |
| Watches | 3 | Unlimited |
| History per watch | Last 30 observations | Full history |
| `watch_refresh` one item | Yes | Yes |
| `watch_refresh` all at once | No | Yes |
| `price_add_manual` | Yes | Yes |
| Redirect check and extraction confidence | Yes | Yes |

Enforced limits in the source:

- `FREE_WATCH_LIMIT` = 3 watches on free.
- `FREE_HISTORY_LIMIT` = 30 history points per watch on free.

## Storage

Data dir: `${XDG_DATA_HOME:-~/.local/share}/mcp-servers/price-tracker/`

| file | holds |
| --- | --- |
| `watches.json` | watched URLs, targets and price history |

Primary file (the one a corrupt-store test targets): `watches.json`.

Also written on failure: `<file>.corrupt-<timestamp>` (the quarantined original, byte-for-byte) and `<file>.corrupt` (the marker that blocks every later call). Transient: `<file>.<pid>.tmp` and the `.lock` directory.

Licence key lookup order: `MCP_LICENSE_KEY` env, then `${XDG_CONFIG_HOME:-~/.config}/mcp-servers/license.json`, then free tier.

## Failure modes

Literal error-message heads found in `servers/price-tracker/src/*.ts`. Each is returned as `Error: <text>` with `isError: true`, sorted, truncated at the first interpolation.

- `Error: could not read`
- `Error: give either id or url`
- `Error: no price found on`
- `Error: no watch matches`
- `Error: only http and https URLs are supported (got`
- `Error: the page returned HTTP`
