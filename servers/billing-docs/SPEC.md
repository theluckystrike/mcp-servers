# billing-docs — contract spec

Generated by `scripts/gen-spec.mjs` from the built server. Do not edit by hand: re-run the generator.

| | |
| --- | --- |
| package | `@theluckystrike/mcp-billing-docs` |
| version | 0.9.5 |
| bin | `mcp-billing-docs` |
| serverInfo.name | `mcp-billing-docs` |
| transport | stdio, JSON-RPC 2.0 |
| tools | 14 |
| resources | 1 |
| prompts | 1 |

## What it does

Credit notes against the invoices in the invoice server's store and purchase orders to suppliers: negative line totals in minor units, the invoice's own VAT rates reused, an A4 PDF and a pasteable text version.

## Tools (14)

| tool | description |
| --- | --- |
| `billing_docs_report` | What has been credited back per currency, against how many invoices, and what is still on order per currency, with the purchase orders whose delivery date has passed. Pro. |
| `credit_note_create` | Issue a credit note against one invoice: the whole invoice, a gross amount, or named lines with quantities. Line totals are stored negative in minor units and the invoice's VAT rates are reused. |
| `credit_note_get` | The full stored record for one credit note: every negated line, the VAT lines, the totals, the reason and the invoice it was issued against. |
| `credit_note_list` | Every credit note with its invoice, client, reason and negative total. Filter by invoice number, by client or by issue date range. |
| `credit_note_pdf` | Call this tool to write the A4 PDF of one credit note and return the file path. The invoice layout, titled CREDIT NOTE and carrying the invoice number it reverses. Pro. |
| `credit_note_text` | Turn a credit note into a plain-text summary with the negated line table, the VAT lines, the total and the invoice it reverses, ready to paste into an email. Free on every tier. |
| `license_activate` | Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally. |
| `license_status` | Show whether this server runs in free or Pro mode and where to upgrade. |
| `purchase_order_create` | Order from a supplier: line items with quantity and unit price in minor units, VAT, a currency and an expected delivery date. You are the buyer, from the shared business profile. |
| `purchase_order_get` | The full stored record for one purchase order: the buyer and supplier blocks, every line with its unit price and VAT, the totals, the expected delivery date and what has been received. |
| `purchase_order_list` | Every purchase order with its supplier, total, expected delivery date and status (open, partially received or received). Filter by status, by supplier or by order date range. |
| `purchase_order_pdf` | Call this tool to write the A4 PDF of one purchase order and return the file path. The invoice layout, titled PURCHASE ORDER, with the buyer, the supplier and the delivery date. Pro. |
| `purchase_order_receive` | Record that a purchase order arrived, in full or in part. A partial receipt keeps the order open and is kept on the record with its date and note; a full receipt closes it. |
| `purchase_order_text` | Turn a purchase order into a plain-text summary with the line table, the VAT lines, the total and the delivery date, ready to paste into an email to the supplier. Free on every tier. |

### `billing_docs_report`

Title: Credited money and open orders

What has been credited back per currency, against how many invoices, and what is still on order per currency, with the purchase orders whose delivery date has passed. Pro.

| arg | type | required | description |
| --- | --- | --- | --- |
| `from` | string | no | YYYY-MM-DD, earliest document date to count |
| `to` | string | no | YYYY-MM-DD, latest document date to count |

### `credit_note_create`

Title: Credit an invoice

Issue a credit note against one invoice: the whole invoice, a gross amount, or named lines with quantities. Line totals are stored negative in minor units and the invoice's VAT rates are reused.

| arg | type | required | description |
| --- | --- | --- | --- |
| `amount_minor` | integer | no | Credit this GROSS amount, VAT included, in minor units. Split across the invoice's VAT rates in proportion to each rate's share of the total (max 1000000000000) |
| `invoice` | string | yes | The invoice number to credit, e.g. INV-2026-0001 (minLength 1) |
| `issue_date` | string | no | YYYY-MM-DD, defaults to today in your business profile's timezone |
| `lines` | object{line, quantity}[] | no | Credit only these invoice lines. Leave out with amount_minor for a full credit note |
| `notes` | string | no | Free text printed under the totals (maxLength 10000) |
| `reason` | string | yes | Why the money is being given back, e.g. "returned goods" or "billed twice". Printed on the document (minLength 1, maxLength 1000) |

### `credit_note_get`

Title: Show one credit note

The full stored record for one credit note: every negated line, the VAT lines, the totals, the reason and the invoice it was issued against.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | yes | Credit note id such as CN-2026-0001, or an exact client name |

### `credit_note_list`

Title: List credit notes

Every credit note with its invoice, client, reason and negative total. Filter by invoice number, by client or by issue date range.

| arg | type | required | description |
| --- | --- | --- | --- |
| `client` | string | no | Only credit notes for clients whose name contains this text |
| `from` | string | no | YYYY-MM-DD, earliest issue date |
| `invoice` | string | no | Only credit notes issued against this invoice number |
| `to` | string | no | YYYY-MM-DD, latest issue date |

### `credit_note_pdf`

Title: Render the credit note as a PDF

Call this tool to write the A4 PDF of one credit note and return the file path. The invoice layout, titled CREDIT NOTE and carrying the invoice number it reverses. Pro.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | yes | Credit note id such as CN-2026-0001 |
| `out_path` | string | no | Where to write the file. Defaults to the billing-docs data directory under pdf/ |

### `credit_note_text`

Title: Plain-text credit note to paste into email

Turn a credit note into a plain-text summary with the negated line table, the VAT lines, the total and the invoice it reverses, ready to paste into an email. Free on every tier.

| arg | type | required | description |
| --- | --- | --- | --- |
| `greeting` | string | no | Opening line, default "Hello" plus the client name |
| `id` | string | yes | Credit note id such as CN-2026-0001 |
| `sign_off` | string | no | Closing line, default your business name from the shared profile |

### `license_activate`

Title: Activate license

Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally.

| arg | type | required | description |
| --- | --- | --- | --- |
| `key` | string | yes | License key from the checkout confirmation page |

### `license_status`

Title: License status

Show whether this server runs in free or Pro mode and where to upgrade.

No arguments.

### `purchase_order_create`

Title: Raise a purchase order

Order from a supplier: line items with quantity and unit price in minor units, VAT, a currency and an expected delivery date. You are the buyer, from the shared business profile.

| arg | type | required | description |
| --- | --- | --- | --- |
| `currency` | string | no | Defaults to your business default currency (pattern `^[A-Za-z]{3}$`) |
| `discount_percent` | number | no | Discount applied to every line, in percent (min 0, max 100) |
| `expected_delivery_date` | string | no | YYYY-MM-DD, when the goods or work are due |
| `issue_date` | string | no | YYYY-MM-DD, defaults to today in your business profile's timezone |
| `items` | object{currency, description, quantity, tax_rate, unit_price_minor}[] | yes | What is being ordered |
| `notes` | string | no | Free text printed under the totals, e.g. delivery address or terms (maxLength 10000) |
| `supplier` | string | yes | Supplier name or client id. A name the invoice server already knows brings its address, email and VAT id onto the order (minLength 1, maxLength 200) |
| `supplier_address` | string | no | Postal address for the SUPPLIER block, newlines allowed (maxLength 2000) |
| `supplier_email` | string | no | Only if the user gave it; otherwise the stored client's email is used (maxLength 320) |
| `supplier_vat_id` | string | no | Supplier VAT / tax registration id (maxLength 64) |
| `tax_rate` | number | no | VAT percent for lines with no rate of their own. Defaults to the business default (min 0, max 1000) |

### `purchase_order_get`

Title: Show one purchase order

The full stored record for one purchase order: the buyer and supplier blocks, every line with its unit price and VAT, the totals, the expected delivery date and what has been received.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | yes | Purchase order id such as PO-2026-0001, or an exact supplier name |

### `purchase_order_list`

Title: List purchase orders

Every purchase order with its supplier, total, expected delivery date and status (open, partially received or received). Filter by status, by supplier or by order date range.

| arg | type | required | description |
| --- | --- | --- | --- |
| `from` | string | no | YYYY-MM-DD, earliest order date |
| `status` | "open" \| "partially_received" \| "received" \| "all" | no | Default "all" |
| `supplier` | string | no | Only orders to suppliers whose name contains this text |
| `to` | string | no | YYYY-MM-DD, latest order date |

### `purchase_order_pdf`

Title: Render the purchase order as a PDF

Call this tool to write the A4 PDF of one purchase order and return the file path. The invoice layout, titled PURCHASE ORDER, with the buyer, the supplier and the delivery date. Pro.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | yes | Purchase order id such as PO-2026-0001 |
| `out_path` | string | no | Where to write the file. Defaults to the billing-docs data directory under pdf/ |

### `purchase_order_receive`

Title: Mark a purchase order received

Record that a purchase order arrived, in full or in part. A partial receipt keeps the order open and is kept on the record with its date and note; a full receipt closes it.

| arg | type | required | description |
| --- | --- | --- | --- |
| `date` | string | no | YYYY-MM-DD, defaults to today |
| `id` | string | yes | Purchase order id such as PO-2026-0001 |
| `note` | string | no | What arrived, e.g. "8 of 10 units, 2 back-ordered" (maxLength 10000) |
| `partial` | boolean | no | True when only some of the order arrived. The order stays open and can be received again. Default false |

### `purchase_order_text`

Title: Plain-text purchase order to paste into email

Turn a purchase order into a plain-text summary with the line table, the VAT lines, the total and the delivery date, ready to paste into an email to the supplier. Free on every tier.

| arg | type | required | description |
| --- | --- | --- | --- |
| `greeting` | string | no | Opening line, default "Hello" plus the supplier name |
| `id` | string | yes | Purchase order id such as PO-2026-0001 |
| `sign_off` | string | no | Closing line, default your business name from the shared profile |

## Resources

| uri | name | description |
| --- | --- | --- |
| `billing-docs://open-orders` | open-purchase-orders | Every purchase order that has not been received in full, as JSON. |

## Prompts

| name | description |
| --- | --- |
| `chase_deliveries` | Review which purchase orders are late, then draft the chaser to each supplier. |

## Invariants

- stdout carries JSON-RPC only. Every diagnostic goes to stderr. A single stray stdout write breaks the client session.
- A tool never throws across the transport. Failures come back as `{ content: [{ type: "text", text: "Error: ..." }], isError: true }`.
- Writes are atomic: the payload goes to `<file>.<pid>.tmp` and is then `rename`d over the target, so a reader never sees a half-written file.
- No partial writes. When a limit or a validation refuses the operation, nothing at all is written; the tool says what was refused and why.
- Money is stored and compared in minor units (integer cents), never as a float. Formatting to a decimal string happens at the edge only.
- Dates are local calendar dates as `YYYY-MM-DD`. No implicit UTC shift is applied to a user-supplied date.
- A store file that fails to parse is quarantined byte-for-byte as `<file>.corrupt-<timestamp>` with a `<file>.corrupt` marker; nothing is overwritten and every later call fails until a human resolves it.
- The load-mutate-save cycle is held under an advisory lock directory (`packages/mcp-license` `withFileLock`), so two processes sharing one data dir cannot lose each other's writes.
- Money and VAT come from `@theluckystrike/mcp-invoice/lib` (`computeTotals`, `currencyDecimals`, `formatMoney`). This server holds no copy of that arithmetic.
- A credit note never exceeds the invoice's remaining creditable amount: the invoice total less everything already credited against it. The check and the write are one critical section under both locks.
- Crediting a whole invoice, or a whole invoice line, COPIES the stored line and its totals rather than recomputing them. Only a partial quantity is recomputed, on the same unit price and rate the invoice used.
- Every money field on a credit note is stored negative in minor units, including the line unit price, so summing a period's documents gives the net of what was billed.
- The invoice engine's `Invoice` record has no `credited_minor` field, so the link lives on the credit note and `credit_note_list {invoice}` is the query. The field is written back only if a future engine version already carries it.
- Locks are always taken billing-docs first, then invoice, the same order servers/quotes and servers/recurring use, so no two processes in this repo can deadlock.

## Free vs Pro

From `servers/billing-docs/README.md`.

_No README.md._

Enforced limits in the source:

- `FREE_DOCS_PER_MONTH` = 5 documents per calendar month on free, credit notes and purchase orders together, counted by issue date.
- `credit_note_pdf`, `purchase_order_pdf` and `billing_docs_report` are Pro. The refusal is an answer, not a protocol error, and no file is written.
- `MAX_ITEMS` = 200 line items per purchase order.
- `MAX_MINOR` = 1e12 per amount field.

## Storage

Data dir: `${XDG_DATA_HOME:-~/.local/share}/mcp-servers/billing-docs/`

| file | holds |
| --- | --- |
| `credit-notes.json` | credit notes, with the invoice number each was issued against |
| `purchase-orders.json` | purchase orders and their receipts |
| `counter.json` | the CN and PO number series, per prefix and year |

Primary file (the one a corrupt-store test targets): `credit-notes.json`.

Also written on failure: `<file>.corrupt-<timestamp>` (the quarantined original, byte-for-byte) and `<file>.corrupt` (the marker that blocks every later call). Transient: `<file>.<pid>.tmp` and the `.lock` directory.

Licence key lookup order: `MCP_LICENSE_KEY` env, then `${XDG_CONFIG_HOME:-~/.config}/mcp-servers/license.json`, then free tier.

## Failure modes

Literal error-message heads found in `servers/billing-docs/src/*.ts`. Each is returned as `Error: <text>` with `isError: true`, sorted, truncated at the first interpolation.

- `Error: expected_delivery_date`
- `Error: no credit note matches`
- `Error: no invoice numbered`
- `Error: no purchase order matches`
- `Error: pass amount_minor or lines, not both: they are two different ways of saying how much to credit. Nothing was stored.`
- `Error: that credit note would be for nothing. Nothing was stored.`
- `Error: that document totals more than can be represented exactly in minor units. Nothing was stored.`
- `Error: the document currency is`
- `Error: the line items carry more than one currency (`
