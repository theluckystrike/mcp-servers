# kanban — contract spec

Generated by `scripts/gen-spec.mjs` from the built server. Do not edit by hand: re-run the generator.

| | |
| --- | --- |
| package | `@theluckystrike/mcp-kanban` |
| version | 0.9.0 |
| bin | `mcp-kanban` |
| serverInfo.name | `mcp-kanban` |
| transport | stdio, JSON-RPC 2.0 |
| tools | 16 |
| resources | 1 |
| prompts | 1 |

## What it does

A local task board per project: add, move, update and finish tasks across columns, search them, see what is overdue, run a weekly review, and start a timer on the time-tracker server.

## Tools (16)

| tool | description |
| --- | --- |
| `board` | Column-by-column summary of a project board: task counts and estimate totals. |
| `columns_set` | Replace the columns of one board (Pro). Tasks sitting in a removed column move to the first column. |
| `license_activate` | Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally. |
| `license_status` | Show whether this server runs in free or Pro mode and where to upgrade. |
| `overdue` | Every task past its due date, across all boards. |
| `project_list` | All project boards with open task counts, remaining estimate and overdue counts. |
| `task_add` | Add a task to a project board (todo list / kanban card). Optional column, due date, estimate, priority, tags and notes. |
| `task_delete` | Delete a task permanently. |
| `task_done` | Mark a task done: it moves to the done column and is stamped with the time. |
| `task_list` | List tasks as a table, filtered by project, column, tag, due date or overdue. |
| `task_log_time` | Add real minutes worked to a task, so estimate and actual can be compared. |
| `task_move` | Move a task to another column on its board. |
| `task_search` | Find tasks whose title, notes, tags or id match a query. |
| `task_start_timer` | Return the exact arguments to pass to the time-tracker server's timer_start for this task, and record the link on the task. |
| `task_update` | Change any field of a task: title, notes, due date, estimate, priority, tags or project. |
| `weekly_review` | Done versus planned for a week, with estimate against actual minutes per project. |

### `board`

Title: Show board

Column-by-column summary of a project board: task counts and estimate totals.

| arg | type | required | description |
| --- | --- | --- | --- |
| `project` | string | no | Which board; defaults to your busiest one (maxLength 100) |

### `columns_set`

Title: Set board columns

Replace the columns of one board (Pro). Tasks sitting in a removed column move to the first column.

| arg | type | required | description |
| --- | --- | --- | --- |
| `columns` | string[] | yes | The new column names in order, at most 12, e.g. ['inbox','next','doing','done'] |
| `project` | string | yes | Which board (maxLength 100) |

### `license_activate`

Title: Activate license

Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally.

| arg | type | required | description |
| --- | --- | --- | --- |
| `key` | string | yes | License key from the checkout confirmation page |

### `license_status`

Title: License status

Show whether this server runs in free or Pro mode and where to upgrade.

No arguments.

### `overdue`

Title: Overdue tasks

Every task past its due date, across all boards.

| arg | type | required | description |
| --- | --- | --- | --- |
| `as_of` | string | no | Measure against this day instead of today (YYYY-MM-DD, 'next Monday', '+7d') (maxLength 64) |
| `limit` | integer | no | Rows to print; default 200 (min 1, max 2000) |

### `project_list`

Title: List projects

All project boards with open task counts, remaining estimate and overdue counts.

No arguments.

### `task_add`

Title: Add task

Add a task to a project board (todo list / kanban card). Optional column, due date, estimate, priority, tags and notes.

| arg | type | required | description |
| --- | --- | --- | --- |
| `column` | string | no | Column to start in; defaults to the first column (backlog) (maxLength 40) |
| `due` | string | no | Due date: YYYY-MM-DD, 'today', 'tomorrow', 'Friday', 'next Monday' or '+3d' (maxLength 64) |
| `estimate_minutes` | integer | no | How long you think it will take, in minutes (min 0, max 100000) |
| `notes` | string | no | Longer notes for the task (maxLength 5000) |
| `priority` | "low" \| "normal" \| "high" \| "urgent" | no | Priority; defaults to normal |
| `project` | string | no | Project or board name. A partial name matching exactly one existing project is used as that project. Defaults to your only board. (maxLength 100) |
| `tags` | string[] | no | Free-form tags, e.g. ['writing','client'] |
| `title` | string | yes | What the task is, e.g. 'Write the launch email' (maxLength 300) |

### `task_delete`

Title: Delete task

Delete a task permanently.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | yes | Task id, e.g. NOVA-12 (maxLength 64) |

### `task_done`

Title: Complete task

Mark a task done: it moves to the done column and is stamped with the time.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | yes | Task id, e.g. NOVA-12 (maxLength 64) |

### `task_list`

Title: List tasks

List tasks as a table, filtered by project, column, tag, due date or overdue.

| arg | type | required | description |
| --- | --- | --- | --- |
| `column` | string | no | Only this column, e.g. 'doing' (maxLength 40) |
| `due_before` | string | no | Only tasks due on or before this day (YYYY-MM-DD, 'today', 'Friday', '+7d') (maxLength 64) |
| `include_done` | boolean | no | Include finished tasks; default false |
| `limit` | integer | no | Rows to print; default 200 (min 1, max 2000) |
| `overdue` | boolean | no | Only tasks past their due date |
| `project` | string | no | Only this project (maxLength 100) |
| `tag` | string | no | Only tasks carrying this tag (maxLength 60) |

### `task_log_time`

Title: Log time on a task

Add real minutes worked to a task, so estimate and actual can be compared.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | yes | Task id, e.g. NOVA-12 (maxLength 64) |
| `minutes` | integer | yes | Minutes to add; a negative number corrects an over-count (min -100000, max 100000) |

### `task_move`

Title: Move task

Move a task to another column on its board.

| arg | type | required | description |
| --- | --- | --- | --- |
| `column` | string | yes | Target column, e.g. 'doing' (maxLength 40) |
| `id` | string | yes | Task id, e.g. NOVA-12 (maxLength 64) |

### `task_search`

Title: Search tasks

Find tasks whose title, notes, tags or id match a query.

| arg | type | required | description |
| --- | --- | --- | --- |
| `limit` | integer | no | Rows to print; default 200 (min 1, max 2000) |
| `query` | string | yes | Text to look for (maxLength 200) |

### `task_start_timer`

Title: Start a timer for a task

Return the exact arguments to pass to the time-tracker server's timer_start for this task, and record the link on the task.

| arg | type | required | description |
| --- | --- | --- | --- |
| `id` | string | yes | Task id, e.g. NOVA-12 (maxLength 64) |

### `task_update`

Title: Update task

Change any field of a task: title, notes, due date, estimate, priority, tags or project.

| arg | type | required | description |
| --- | --- | --- | --- |
| `column` | string | no | (maxLength 40) |
| `due` | string | no | New due date, or 'none' to clear it (maxLength 64) |
| `estimate_minutes` | integer | no | (min 0, max 100000) |
| `id` | string | yes | Task id, e.g. NOVA-12 (maxLength 64) |
| `notes` | string | no | (maxLength 5000) |
| `priority` | "low" \| "normal" \| "high" \| "urgent" | no |  |
| `project` | string | no | Move the task to another existing project board (maxLength 100) |
| `tags` | string[] | no | Replaces the whole tag list |
| `title` | string | no | (maxLength 300) |

### `weekly_review`

Title: Weekly review

Done versus planned for a week, with estimate against actual minutes per project.

| arg | type | required | description |
| --- | --- | --- | --- |
| `week` | string | no | ISO week, e.g. '2026-W36'. Defaults to this week. Past weeks are a Pro feature. (maxLength 16) |

## Resources

| uri | name | description |
| --- | --- | --- |
| `kanban://today` | today | Tasks due today plus everything already past its due date, across all boards. |

## Prompts

| name | description |
| --- | --- |
| `plan_week` | Turn the open board into a realistic plan for the week ahead. |

## Invariants

- stdout carries JSON-RPC only. Every diagnostic goes to stderr. A single stray stdout write breaks the client session.
- A tool never throws across the transport. Failures come back as `{ content: [{ type: "text", text: "Error: ..." }], isError: true }`.
- Writes are atomic: the payload goes to `<file>.<pid>.tmp` and is then `rename`d over the target, so a reader never sees a half-written file.
- No partial writes. When a limit or a validation refuses the operation, nothing at all is written; the tool says what was refused and why.
- Dates are local calendar dates as `YYYY-MM-DD`. No implicit UTC shift is applied to a user-supplied date.
- A store file that fails to parse is quarantined byte-for-byte as `<file>.corrupt-<timestamp>` with a `<file>.corrupt` marker; nothing is overwritten and every later call fails until a human resolves it.
- The load-mutate-save cycle is held under an advisory lock directory (`packages/mcp-license` `withFileLock`), so two processes sharing one data dir cannot lose each other's writes.
- A blank project name is refused, and `resolveProject` never near-matches an empty stored name, so a board with an empty name cannot swallow every later project (D-K2).
- `columns_set` validates the normalised list, not the raw array, so a board cannot be collapsed to one column, which would make every task on it read as done (D-K4).
- `task_list`, `task_search` and `overdue` cap the printed rows and say what was cut. One 5,000-task listing was 430 KB, roughly 110,000 tokens, which no client can use (D-K9).
- `task_start_timer` reads the sibling `time-tracker/data.json` under the same XDG data root, read-only and best effort, and warns before starting when the two stores would resolve the project name differently (D-K10).
- Accepted due-date forms: `YYYY-MM-DD` (a real calendar day), `today`, `tomorrow`, `yesterday`, `+Nd`, a bare weekday meaning the nearest on or after today, and `next <weekday>` meaning the nearest strictly after. Anything else is refused with the list.

## Free vs Pro

From `servers/kanban/README.md`.

| | Free | Pro |
| --- | --- | --- |
| Project boards | 3 | unlimited |
| Open tasks | 200 | unlimited |
| Columns | the default five (backlog, todo, doing, review, done) | custom columns per board (`columns_set`) |
| Weekly review | the current week | any week, full history |
| Estimate vs actual reports | current week only | every week |
| Listing output | 200 rows per call, `limit` up to 2,000 | same |
| Everything else | included | included |

Enforced limits in the source:

- `FREE_PROJECTS` = 3 project boards on free.
- `FREE_OPEN_TASKS` = 200 open tasks on free.
- `columns_set` (custom columns) is Pro; free boards use the default five: backlog, todo, doing, review, done.
- `weekly_review` is free for the current week; a past week is Pro.
- `MAX_TITLE` = 300, `MAX_NOTES` = 5000, `MAX_PROJECT` = 100, `MAX_ID` = 64, `MAX_COLUMN_NAME` = 40, `MAX_COLUMNS` = 12, `MAX_TAGS` = 30, `MAX_TAG` = 60, `MAX_QUERY` = 200, `MAX_MINUTES` = 100000.
- Listing prints 200 rows by default and `limit` reaches 2000 (`MAX_ROW_LIMIT`). The totals line still counts every row.

## Storage

Data dir: `${XDG_DATA_HOME:-~/.local/share}/mcp-servers/kanban/`

| file | holds |
| --- | --- |
| `data.json` | boards, their columns and every task |

Primary file (the one a corrupt-store test targets): `data.json`.

Also written on failure: `<file>.corrupt-<timestamp>` (the quarantined original, byte-for-byte) and `<file>.corrupt` (the marker that blocks every later call). Transient: `<file>.<pid>.tmp` and the `.lock` directory.

Licence key lookup order: `MCP_LICENSE_KEY` env, then `${XDG_CONFIG_HOME:-~/.config}/mcp-servers/license.json`, then free tier.

## Failure modes

Literal error-message heads found in `servers/kanban/src/*.ts`. Each is returned as `Error: <text>` with `isError: true`, sorted, truncated at the first interpolation.

- `Error: a board needs at least 2 named columns;`
- `Error: cannot read the data file`
- `Error: column names must be unique:`
- `Error: data file is corrupt; moved to`
- `Error: no project board`
- `Error: no task with id`
- `Error: project is blank. Give a board name, or leave project out to use your current board.`
- `Error: that would leave`
- `Error: week must look like 2026-W36, got`
