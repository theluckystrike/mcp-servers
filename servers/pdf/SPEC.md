# pdf: contract spec

Generated by `scripts/gen-spec.mjs` from the built server. Do not edit by hand: re-run the generator.

| | |
| --- | --- |
| package | `@theluckystrike/mcp-pdf` |
| version | 0.19.0 |
| bin | `mcp-pdf` |
| serverInfo.name | `mcp-pdf` |
| transport | stdio, JSON-RPC 2.0 |
| tools | 12 |
| resources | 1 |
| prompts | 1 |

## What it does

Local PDF work with no native dependency: read a document's shape, count and extract pages, merge, split, rotate, reorder, stamp, add a business footer, and read the text back out.

## Tools (12)

| tool | description |
| --- | --- |
| `license_activate` | Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally. |
| `license_status` | Show whether this server runs in free or Pro mode and where to upgrade. |
| `pdf_count` | Page count per file plus the total, for any number of PDFs. Read-only. A file that cannot be read is reported per file; the others still count. Free tier: unlimited. |
| `pdf_info` | Call this tool to inspect a PDF: page count, page sizes in points/mm, paper name, metadata (title, author, producer, dates) and whether it is encrypted. Read-only, never modifies the file. Free tier: unlimited. |
| `pdf_merge` | Call this tool to join several PDFs into one, in the order given. Page sizes are kept as-is, so a merged file may have mixed sizes, and the answer says so. Inputs are never modified. Free tier: up to 5 files per merge. |
| `pdf_pages` | Call this tool to pull selected pages into one new PDF, in the order written: "2,4-6" gives four pages. Asking for a page twice copies it twice. The input is never modified. Free tier: files up to 30 pages. |
| `pdf_reorder` | Call this tool to write a new PDF with pages in the order you give. The order must name every page exactly once, so nothing drops by accident; use pdf_pages for a subset. Pro. |
| `pdf_rotate` | Call this tool to turn pages by a multiple of 90 degrees, clockwise for positive. Rotation is added to whatever the page already had, for a sideways scan. Writes a new file. Free tier: files up to 30 pages. |
| `pdf_split` | Call this tool to write one new PDF per range. Ranges are 1-based and may be open-ended: "1-3,5,7-" gives pages 1-3, page 5, and 7 to the end. The input is never modified. Free tier: files up to 30 pages. |
| `pdf_stamp` | Call this tool to draw a word such as PAID or DRAFT across the pages, in a colour and position you choose. Writes a new file; input untouched. Free tier: the PAID and DRAFT presets in their preset colours. |
| `pdf_text` | Call this tool for best-effort text extraction from standard-font PDFs. Returns nothing for a scan (no OCR), or glyph indices for a custom-encoded font, and says which case applies. Read-only. Free tier: unlimited. |
| `pdf_watermark_business` | Call this tool to put your business name and VAT id in the footer of every page, from the shared profile mcp-invoice and mcp-docx write. Use it before sending a document out. Pro. |

### `license_activate`

Title: Activate license

Activate a Pro license key (format MCPL1.xxx.yyy). Verified offline and saved locally.

| arg | type | required | description |
| --- | --- | --- | --- |
| `key` | string | yes | License key from the checkout confirmation page |

### `license_status`

Title: License status

Show whether this server runs in free or Pro mode and where to upgrade.

No arguments.

### `pdf_count`

Title: Count pages in several PDFs

Page count per file plus the total, for any number of PDFs. Read-only. A file that cannot be read is reported per file; the others still count. Free tier: unlimited.

| arg | type | required | description |
| --- | --- | --- | --- |
| `paths` | string[] | yes | Paths to the PDF files |

### `pdf_info`

Title: Inspect a PDF

Call this tool to inspect a PDF: page count, page sizes in points/mm, paper name, metadata (title, author, producer, dates) and whether it is encrypted. Read-only, never modifies the file. Free tier: unlimited.

| arg | type | required | description |
| --- | --- | --- | --- |
| `path` | string | yes | Path to the PDF file. ~ is expanded; a relative path is resolved against the working directory |

### `pdf_merge`

Title: Merge PDFs into one file

Call this tool to join several PDFs into one, in the order given. Page sizes are kept as-is, so a merged file may have mixed sizes, and the answer says so. Inputs are never modified. Free tier: up to 5 files per merge.

| arg | type | required | description |
| --- | --- | --- | --- |
| `out_path` | string | yes | Where to write the merged PDF |
| `overwrite` | boolean | no | Replace out_path if a file is already there. Default false: an existing file is never overwritten |
| `paths` | string[] | yes | The PDFs to join, in the order they should appear |

### `pdf_pages`

Title: Extract pages into a new PDF

Call this tool to pull selected pages into one new PDF, in the order written: "2,4-6" gives four pages. Asking for a page twice copies it twice. The input is never modified. Free tier: files up to 30 pages.

| arg | type | required | description |
| --- | --- | --- | --- |
| `out_path` | string | yes | Where to write the extracted PDF |
| `overwrite` | boolean | no | Replace out_path if it exists. Default false |
| `pages` | string | yes | 1-based pages and ranges to keep, in output order, e.g. "2,4-6" or "5,1,1" |
| `path` | string | yes | The source PDF |

### `pdf_reorder`

Title: Reorder the pages of a PDF

Call this tool to write a new PDF with pages in the order you give. The order must name every page exactly once, so nothing drops by accident; use pdf_pages for a subset. Pro.

| arg | type | required | description |
| --- | --- | --- | --- |
| `order` | integer[] | yes | The 1-based page numbers in their new order, e.g. [3,1,2] for a three-page file. Every page must appear exactly once |
| `out_path` | string | yes | Where to write the reordered PDF |
| `overwrite` | boolean | no | Replace out_path if it exists. Default false |
| `path` | string | yes | The source PDF |

### `pdf_rotate`

Title: Rotate pages

Call this tool to turn pages by a multiple of 90 degrees, clockwise for positive. Rotation is added to whatever the page already had, for a sideways scan. Writes a new file. Free tier: files up to 30 pages.

| arg | type | required | description |
| --- | --- | --- | --- |
| `degrees` | integer | yes | 90, 180, 270 or -90. Positive turns clockwise. Added to the page's existing rotation |
| `out_path` | string | yes | Where to write the rotated PDF |
| `overwrite` | boolean | no | Replace out_path if it exists. Default false |
| `pages` | string | no | Which pages to turn, e.g. "1" or "2,4-6". Omit for every page |
| `path` | string | yes | The source PDF |

### `pdf_split`

Title: Split a PDF into several files

Call this tool to write one new PDF per range. Ranges are 1-based and may be open-ended: "1-3,5,7-" gives pages 1-3, page 5, and 7 to the end. The input is never modified. Free tier: files up to 30 pages.

| arg | type | required | description |
| --- | --- | --- | --- |
| `out_path_pattern` | string | yes | Output path with a placeholder: {n} is the part number (1, 2, 3...), {range} is the range itself (e.g. 1-3), {name} is the input file name without .pdf. Example: ~/out/{name}-{range}.pdf |
| `overwrite` | boolean | no | Replace existing outputs. Default false: nothing is overwritten and nothing is written at all if any target exists |
| `path` | string | yes | The PDF to split |
| `ranges` | string | yes | Comma-separated 1-based page ranges, e.g. "1-3,5,7-". An open-ended range runs to the last page |

### `pdf_stamp`

Title: Stamp text on a PDF

Call this tool to draw a word such as PAID or DRAFT across the pages, in a colour and position you choose. Writes a new file; input untouched. Free tier: the PAID and DRAFT presets in their preset colours.

| arg | type | required | description |
| --- | --- | --- | --- |
| `color` | string | no | Hex code such as #1b7f3b, or a name: red, green, blue, black, gray, orange, purple. Pro |
| `font_size` | number | no | Point size. By default the stamp is sized to fit the page width |
| `opacity` | number | no | 0 to 1. Default 0.35 in the centre, 0.85 in a corner |
| `out_path` | string | yes | Where to write the stamped PDF |
| `overwrite` | boolean | no | Replace out_path if it exists. Default false |
| `pages` | string | no | Which pages to stamp, e.g. "1" or "2,4-6". Omit for every page |
| `path` | string | yes | The source PDF |
| `position` | "center" \| "top-left" \| "top-center" \| "top-right" \| "middle-left" \| "middle-right" \| "bottom-left" \| "bottom-center" \| "bottom-right" | no | Where on the page. Default center, which is drawn on the 45-degree diagonal like a real stamp |
| `text` | string | yes | What to stamp. PAID and DRAFT are presets with their own colour; any other text is Pro (minLength 1) |

### `pdf_text`

Title: Read the text of a PDF

Call this tool for best-effort text extraction from standard-font PDFs. Returns nothing for a scan (no OCR), or glyph indices for a custom-encoded font, and says which case applies. Read-only. Free tier: unlimited.

| arg | type | required | description |
| --- | --- | --- | --- |
| `pages` | string | no | Which pages, e.g. "1" or "2,4-6". Omit for every page |
| `path` | string | yes | The PDF to read |

### `pdf_watermark_business`

Title: Stamp your business details in the footer

Call this tool to put your business name and VAT id in the footer of every page, from the shared profile mcp-invoice and mcp-docx write. Use it before sending a document out. Pro.

| arg | type | required | description |
| --- | --- | --- | --- |
| `out_path` | string | yes | Where to write the footed PDF |
| `overwrite` | boolean | no | Replace out_path if it exists. Default false |
| `pages` | string | no | Which pages, e.g. "1". Omit for every page |
| `path` | string | yes | The source PDF |

## Resources

| uri | name | description |
| --- | --- | --- |
| `pdf://recent` | recent-operations | What this server has done, newest first: the operation, the input files, the files written and when. |

## Prompts

| name | description |
| --- | --- |
| `mark_invoice_paid` | Find an invoice, stamp PAID across it and save the stamped copy beside the original. |

## Invariants

- stdout carries JSON-RPC only. Every diagnostic goes to stderr. A single stray stdout write breaks the client session.
- A tool never throws across the transport. Failures come back as `{ content: [{ type: "text", text: "Error: ..." }], isError: true }`.
- Writes are atomic: the payload goes to `<file>.<pid>.tmp` and is then `rename`d over the target, so a reader never sees a half-written file.
- No partial writes. When a limit or a validation refuses the operation, nothing at all is written; the tool says what was refused and why.
- Dates are local calendar dates as `YYYY-MM-DD`. No implicit UTC shift is applied to a user-supplied date.
- The load-mutate-save cycle is held under an advisory lock directory (`packages/mcp-license` `withFileLock`), so two processes sharing one data dir cannot lose each other's writes.
- `reserveOutput(out, overwrite, inputs)` refuses an `out_path` that resolves to, or shares an inode with, any input of the same call, before any directory is made and before any work. `overwrite: true` does not override it: it is consent to replace some other file, never to consume an input (D-P1).
- A tier limit is an answer, not a protocol error: it comes back without `isError`, and nothing is written when one refuses a call.
- The operation register is best effort. The file is already on disk when the register is touched, so a corrupt register is quarantined and reported as a note on a successful answer, never as "nothing was written".
- Stamp text is sanitised, not deleted: a whitespace control is a word separator (D-P2), and a character outside WinAnsi is transliterated (`OPLACONE` for `OPŁACONE`) with the count of replacements and the text actually drawn reported (D-P3).
- A stamp that does not fit is refused with the measurement, not drawn off the page: the width is compared against the room available and the overflow is named in points (D-P4).
- `pdf_text` is best effort and says so: it reads `Tj`, `TJ`, `'` and `"` out of the FlateDecode content streams with `node:zlib`. A scan carries no text operators, and the answer names the exact argument that continues a truncated read (D-P6).

## Free vs Pro

From `servers/pdf/README.md`.

| | Free | Pro |
| --- | --- | --- |
| `pdf_info`, `pdf_count`, `pdf_text` | Unlimited | Unlimited |
| `pdf_merge` | Up to 5 files per call | Any number of files |
| `pdf_split`, `pdf_pages`, `pdf_rotate` | Files up to 30 pages | Any length |
| `pdf_stamp` | The `PAID` and `DRAFT` presets in their preset colours | Any text, any colour, any size |
| `pdf_watermark_business` | - | Your business name and VAT id in the footer |
| `pdf_reorder` | - | Yes |

Enforced limits in the source:

- `FREE_MAX_MERGE_FILES` = 5 files per `pdf_merge` on free.
- `FREE_MAX_PAGES` = 30 pages per file for `pdf_split`, `pdf_pages` and `pdf_rotate` on free.
- `pdf_stamp` is free for the `PAID` and `DRAFT` presets in their preset colours; custom text, colour and size are Pro.
- `pdf_watermark_business` and `pdf_reorder` are Pro.
- `MAX_BYTES` = 100 MB per input, `MAX_FONT_SIZE` = 1600 points, `MAX_TEXT_CHARS` = 200000 per `pdf_text` answer.

## Storage

Data dir: `${XDG_DATA_HOME:-~/.local/share}/mcp-servers/pdf/`

| file | holds |
| --- | --- |
| `operations.json` | the last 500 operations this server performed, for the pdf://recent resource |

Primary file (the one a corrupt-store test targets): `operations.json`.

Also written on failure: `<file>.corrupt-<timestamp>` (the quarantined original, byte-for-byte) and `<file>.corrupt` (the marker that blocks every later call). Transient: `<file>.<pid>.tmp` and the `.lock` directory.

Licence key lookup order: `MCP_LICENSE_KEY` env, then `${XDG_CONFIG_HOME:-~/.config}/mcp-servers/license.json`, then free tier.

## Failure modes

Literal error-message heads found in `servers/pdf/src/*.ts`. Each is returned as `Error: <text>` with `isError: true`, sorted, truncated at the first interpolation.

- `Error: a PDF can only record rotation in multiples of 90 degrees;`
- `Error: cannot read the data file`
- `Error: data file is corrupt; moved to`
- `Error: empty page range. Use something like`
- `Error: font_size must be greater than 0 and at most`
- `Error: no business name is stored, so there is nothing to put in the footer and nothing was written.`
- `Error: opacity must be greater than 0 and at most 1; got`
- `Error: out_path_pattern`
- `Error: page numbers start at 1;`
- `Error: the order must name each of the`
- `Error: the pattern produces the path`
- `Error: the stamp text is empty after removing characters a built-in PDF font cannot carry. Nothing was written.`
