# R9 SEO Copy — query-matched titles/H1s on 8 pages + /suites/freelancer hub

STATUS: in progress

## Architecture findings (source of truth)
- The /s/<id> page title, meta description, and H1 do NOT live literally in billing/src/index.js.
- `billing/src/index.js` renders /s/ pages from `PAGES[id]` (imported from `billing/src/pages.js`):
  - `<title>` = `pg.title + " for Claude, Cursor and any MCP client"` (index.js L1466)
  - meta description = `pg.description` (index.js L1425, `pg.description`)
  - H1 = first `<h1>` inside `pg.html` (rendered README)
- `billing/src/pages.js` is GENERATED by `scripts/build-pages.mjs` (do-not-edit header).
  - build-pages.mjs L257: `out[id] = { title: f.title, tagline: f.tagline, description: f.does, html: body }`
  - `f = data/facts.json servers[id]`; `body` = rendered `servers/<id>/README.md` with bundle CTA.
- => SOURCE OF TRUTH: title → `data/facts.json servers.<id>.title`; meta → `servers.<id>.does`;
  H1 → first heading of `servers/<id>/README.md`. Regenerate pages.js after edits.

## Query-matched titles (from positions_sweep.md)
| id | CURRENT title | NEW title (facts.json) |
|----|--------------|------------------------|
| invoice | MCP Invoice | MCP Server for Invoices — Generate Invoices from Claude |
| expense-tracker | MCP Expense Tracker | MCP Expense Tracker — Log Expenses from Claude |
| pdf | MCP PDF Tools | MCP PDF Server — Merge, Split, Fill Forms |
| time-tracker | MCP Time Tracker | MCP Time Tracking Server — Free, No Install |
| kanban | MCP Kanban | MCP Kanban Server — Boards from Claude |
| docx | MCP Docx | MCP Word Document Server — Create .docx from Claude |
| dunning-letters | MCP Dunning Letters | MCP Dunning Letters Server — Automated Payment Reminders |
| petty-cash | MCP Petty Cash | MCP Petty Cash Server — Track a Petty Cash Float from Claude |

### Title suffix
- index.js L1466 appends " for Claude, Cursor and any MCP client". To render the EXACT positions_sweep
  phrase ending in "| zovo.one", change suffix to " | zovo.one".
- No test asserts the old suffix; bundle.test/home-title tests are unaffected.

## H1s (README first heading)
| id | CURRENT H1 | NEW H1 |
|----|-----------|--------|
| invoice | mcp-invoice | Generate & send invoices from Claude with a free MCP server |
| expense-tracker | mcp-expense-tracker | Log expenses from Claude with a free MCP server |
| pdf | mcp-pdf | Merge, split and fill PDF forms from Claude |
| time-tracker | mcp-time-tracker | Track time from Claude with a free, no-install server |
| kanban | mcp-kanban | Run task boards from Claude with a free MCP kanban server |
| docx | mcp-docx | Create .docx Word documents from Claude |
| dunning-letters | mcp-dunning-letters | Automated payment reminders for overdue invoices |
| petty-cash | mcp-petty-cash | Track a petty cash float from Claude |

## New hub page
- /suites/freelancer — "Free MCP Servers for Freelancers — Invoices, Time Tracking, Expenses"
- Links /s/invoice, /s/expense-tracker, /s/pdf, /s/time-tracker, /s/kanban, /s/docx,
  /s/dunning-letters, /s/petty-cash. Add route handler + sitemap entry in billing/src/index.js.

## Evidence log
- Baseline `npm test` in billing/: 164 pass / 0 fail. BILLING_TEST_COUNT stays 155 (no tests added).
- git: HEAD da96e3df "billing: test count 154->155". Untracked: data/r9_*.md.
- positions_sweep.md read. facts.json + READMEs + build-pages.mjs read. Confirmed source-of-truth chain.