{
  "loop": 33,
  "date": "2026-09-10",
  "headline": "Two findings, either of which would have justified the loop. A blind assistant asked the buyer own question named 47 MCP servers and none were ours, and the sources it cited put GitHub first and the official registry at zero. And the zero-install URL this estate advertised everywhere lists its tools and then refuses every call.",
  "primary_kpi": {
    "name": "Named by a blind assistant",
    "value": "0 of 18 buyer questions",
    "how": "A subagent with no knowledge of this estate researched 18 frozen buyer-intent questions with web search and named the server it would recommend. Procedure in docs/BLIND_RECOMMENDATION_PROCEDURE.md, questions frozen in data/blind_questions.json.",
    "why": "The buyer does not browse a registry. They ask an assistant and install what it names. Every other discovery metric this project tracked measures whether a machine can find the catalogue, not whether a person gets told about it."
  },
  "citation_sources": {
    "table": "github.com 21, glama.ai 7, mcp.zovo.one 4, apify.com 4, mcpservers.org 3, 17 other hosts at 1 to 2 each, registry.modelcontextprotocol.io 0",
    "github_detail": "20 of the 21 GitHub citations are individual one-server repositories and exactly one is an awesome-list. Ten map one for one onto servers we already ship.",
    "glama_detail": "6 of the 7 Glama citations are /mcp/servers/<owner>/<repo> and none are /mcp/connectors/. We hold 25 connector listings and 1 of 33 server listings, so the surface we invested in is not the surface that is read.",
    "consequence": "The official registry delivers about 10 human referrals per 14 days and no assistant recommendations. Its real job is as an upstream that api.mcp.github.com and Glama consume, not as a destination anyone reads. Those were being treated as one job."
  },
  "retrieval_vs_citation": {
    "finding": "mcp.zovo.one was retrieved for 2 of 18 questions, at rank 8 and rank 10, both times the homepage, and cited in zero answers.",
    "mechanism": "Reading the homepage as a machine reader explains it. Asked how to generate an invoice, an assistant needs a named server, an exact URL and enough certainty to commit. The homepage gives a category description and a pointer to see the full table elsewhere. A page that defers cannot be cited, because the assistant will not follow a link to finish an answer it can get whole somewhere else.",
    "defect_found": "The live homepage stated three different counts of its own catalogue at once: Thirty-one three times, 32 servers, and 31 servers. ls servers/ returns 32."
  },
  "repo_front_door": {
    "clones_vs_views": "3,410 clones from 502 unique clients against 54 views from 31 humans in 14 days, a ratio of 63 to 1",
    "referrers": "registry.modelcontextprotocol.io 21 views from 10 uniques is the only human referrer; github.com 1 and mcp.zovo.one 1",
    "read": "The repo front door serves machines by about 63 to 1, and machines are what produce recommendations, so this is not a problem to fix but an audience to write for."
  },
  "estate": {
    "live_validation_baseline": "951/951 before any change this loop",
    "servers": 32,
    "hosted": 30
  },
  "agents": [],
  "shipped": [
    "The advertised zero-install URL is corrected everywhere it is printed. The product-page hero and every llms.txt line now lead with /mcp/connect and print the /mcp/<server>/t/<token> shape, and the 401 body now leads with where to GET a token rather than only where to put one.",
    "/mcp/connect minted a token on EVERY request including HEAD, at 1 KV read and 2 writes each with a 30-day tenant, against a cap of 10 mints per IP per hour. Two consecutive Googlebot-agent GETs returned two different tokens. Pointing 30 product pages and every llms.txt line at it would have returned 429 to the eleventh visitor behind any NAT, VPN exit or office proxy, taking the free tier entry point off the air for everyone behind that address. Minting is now gated on a real browser navigation or an explicit action; everyone else gets the same page with placeholders and a button.",
    "The hosted endpoints answered HTTP 406 to Accept: */*, to a missing Accept header and to an empty one, which is what curl sends by default and what naive health probers send. Root cause is the SDK testing the header with a literal substring match rather than content negotiation. Fixed by wrapping the SDK, not forking it, per RFC 9110. Measured across seven Accept variants on three servers: three cases go 406 to 200 and the three that genuinely exclude both media types still 406.",
    "remote/build-vendor.mjs was exiting 1 at HEAD, so the hosted worker could not be built or deployed at all, and nothing said so because neither npm test nor release-check ever ran it. Fixed, and a test now asserts it exits 0.",
    "The fleet-wide minimum tool description moved from quote_create at 2.7 to a predicted clause_update at 3.5. 26 descriptions rewritten, no tool, parameter or schema field renamed, all within the 220-character contract, verified with a positive control that a 231-character injection fails the gate.",
    "A machine-readable payment descriptor now rides on the 429 body, with a pricing resource and an upgrade prompt on all 30 hosted endpoints, declaring honestly that the price cannot be settled by an agent.",
    "Two factual defects in shipped descriptions corrected: trip_export claimed one payload per currency when it emits subsistence and optional lodging in one currency, and delivery-schedule claimed a free tier of 5 open schedules when the code enforces 3.",
    "The upgrade-click counter was rebuilt. v1 counted crawlers and our own probes; a Googlebot user agent on a dead product id was recorded as a click, and the counter moved 1,685 to 1,690 in 16 unattended minutes. v2 counts opt-in on a browser navigation signal and starts at zero.",
    "A new distribution surface with no submission step at all: the Gemini CLI extension gallery, which discovers a repository by a topic plus a manifest at its root and crawls daily. 30 of 32 mirrors now carry both, generated per server from its own manifest. Verified from the live repositories: 30 valid, 2 correctly absent, 0 wrong. delivery-schedule and office-suite get no manifest because their endpoints 404, and a manifest that breaks on first use is worse than none.",
    "All 32 mirror repositories rewritten through one generator, so a sync reproduces the live bytes exactly, proved by a dry run producing a byte-identical README and a second run reporting zero changes. GitHub search coverage against the 18 frozen buyer questions moved 0.608 to 0.862, topics 8-9 to 15-16, and the README now leads with capability. The npx install block it used to lead with was dead: npm returns 404 for those packages against a passing control, and no dist is committed.",
    "Two new servers released end to end and on the channel that produces recommendations: bundles built and attached to v0.21.0 and publicly downloadable at the exact byte size, both booting standalone from the extracted archive, product pages live, listed in llms.txt, sold in the catalogue, added to the office-suite bundle, and two new one-server GitHub mirrors created with 15 and 16 topics, a homepage and an accurate description. checklist has its registry row, verified end to end: the row points at a product page that resolves, which points at a bundle that downloads, with a checksum that does not drift."
  ],
  "human_gated": [],
  "open": [
    "Registry entries at latest version reads 72 of 91, against 85 of 85 before. The target rose because new manifests exist, and the shortfall is names that have never been published, including the two new servers. Spot-checked live: existing entries are present at 0.21.0. Verify the count returns to full after the publish rather than assuming it.",
    "Whether the Gemini CLI gallery indexes the 30 mirrors. Its crawler runs daily, so check on or after 2026-09-11.",
    "Whether the GitHub vocabulary pass moves ranks once the index settles. Re-run scripts/gh-search-rank.mjs on 2026-09-17 and 2026-10-10; today measurement was 20 minutes after the change and its small moves are index noise.",
    "Whether IndexNow moves anything. 154 URLs accepted by Bing, Yandex, Seznam and Naver, and accepted is not indexed. Re-measure 2026-09-24. Brave is not an IndexNow participant, so this cannot move the one index we are proven to be in.",
    "The blind recommendation KPI is 0 of 18 and must be re-run by a FRESH agent after the GitHub and content changes have been crawled, per docs/BLIND_RECOMMENDATION_PROCEDURE.md.",
    "The research file the operator pointed at, ~/Desktop/platform-analysis-2026/, could not be read. All 20 files are iCloud-evicted: ls -lO reports compressed,dataless and cat returns an empty body with exit 0, which is the signature of an evicted file rather than an empty one. Nothing in this loop is based on it.",
    "The two new servers ship without a First five minutes section on their product pages: scripts/build-pages.mjs warns that no user-value round has ever covered them. Every other server has one, and it is the section that turns a page into something a reader can act on. Cover them in the next user-value round."
  ],
  "dead_advertised_path": {
    "what": "https://mcp.zovo.one/mcp/<server>, the headline connect-by-URL path, needs a token. It answers initialize and tools/list with 200 and the full tool list, by design so directory health probes do not mark it down, and refuses every tools/call without one. A token works either as an Authorization Bearer header or in the /mcp/<server>/t/<token> path form, and a token is minted at /mcp/connect.",
    "scope": "It was advertised as the zero-install path on 30 of 30 product pages and all 31 llms.txt lines WITHOUT saying a token was needed or where to get one, and 81 of 85 active registry rows land there. The whole point of a paste-a-URL path is a client that cannot set headers, and that is exactly the client that cannot use the bare form at all.",
    "why_it_hid": "It looks alive from every direction an automated check looks. A previous loop deliberately opened initialize and tools/list to unauthenticated callers so directory health probes would stop publishing the servers as down. Every validation and every prober passed. Only a tools/call fails, and nothing was calling one against the tokenless URL. In a client the user sees the tools appear and then a tool error.",
    "confirmed_by": "Two independent instruments: the funnel audit, and an orchestrator probe that ran the same tools/call against the tokenless URL (401) and against a freshly minted token URL (200, business profile saved).",
    "lesson": "For a hosted endpoint the shipping test is a real tools/call through the exact URL the marketing prints, carrying only what the page tells a reader to have. Discovery succeeding is not the product working.",
    "precision": "An earlier wording called the endpoint dead. That overstates it and was corrected after a second agent challenged it and was right: the bare URL returns 200 with a Bearer header, verified live. The defect is that it was advertised without its prerequisite, not that it is broken."
  },
  "click_instrument": {
    "was_lying": "Proved unconfounded: a Googlebot user agent with Accept text/html, no probe header, on a dead product id was recorded as a click. The counter also moved 1,685 to 1,690 in 16 unattended minutes.",
    "in_product": "In-product cap clicks stand at 3 of 1,685, which is 0.18 percent, and have not moved since 2026-09-07.",
    "now": "A v2 counter starts at zero, counts opt-in on a browser navigation signal, buckets untagged hits out of the totals, and keeps v1 under a legacy key. Never quote v1."
  },
  "corrections_to_prior_beliefs": [
    "Loop 32 concluded Glama connectors beat the Glama servers directory because connectors are free, need no account and re-score daily. Both halves were true and the conclusion was still wrong: being scored is not being read. 6 of 7 Glama citations are servers-directory pages and none are connector pages. Worse, the connectors index holds 19,647 entries and its sort options are Featured, Search Relevance, GitHub Stars, Name and Date Updated. Tool quality score is not among them, so the score does not rank us anywhere.",
    "The servers directory was recorded as an unenterable queue. It is not: we are on it with 2 of 34 mirrors and the discriminator appears to be head-of-queue residency in GitHub recently-pushed list. office-suite is last in the sync script and is the one that got in, which also explains lags previously recorded as 33 minutes for one repo and about 120 days for another.",
    "A watcher reported 6 of 34 mirrors indexed on Glama. That was an artifact: its HEAD probe flaps, and a nonexistent repo returned 404, 200, 404, 404, 404 on repeat. The sitemap gives the true figure of 2.",
    "Stars were assumed to be why our repos do not rank in GitHub search. Measured, they are not. Across 87 competitor repos captured in top-five results the median is 1 star and 35 have zero, and the top result for three separate buyer queries had zero, zero and three stars with no topics at all."
  ],
  "answered_no": [
    "An agent-native payment rail is not reachable without a human step. The MCP specification has no payment primitive; the proposal for one closed unmerged. x402 is live and a no-account facilitator exists, but it needs a wallet address, which is a custody decision only the operator can take, and decisively no mainstream MCP client settles a 402, so every one of them would need a separately funded bridge. Median x402 price is a cent per call against a nineteen dollar one-time product.",
    "The tokenless endpoint should NOT self-provision a tenant. The transport issues no session id, so a session-scoped tenant would silently lose stored documents, bypass the mint rate limit, and break the binding that lets a buyer keep their data, meaning someone who paid would land back on the free tier with nothing."
  ],
  "github_channel": {
    "baseline": "22 of 34 buyer-intent GitHub queries surfaced one of our repos, with rank 1 on 10 of them. The split was total: 11 of 17 uncontested queries in the top 3, and 0 of 7 contested ones.",
    "mechanism": "Repository search reads name, description and topics and nothing else, proved with a control pair where a word present only in topics matches and a word present only in package.json does not. A hyphenated topic does not match its component words. Query-term coverage is the gate at 1.00 mean where we surfaced against 0.90 where we did not, and every sub-1.00 query returned nothing of ours.",
    "stars_refuted": "Stars are not the ranking factor. Zero-star, zero-topic repos held rank 1 on fields of 14 to 91 results; across 87 competitors captured in top-fives the median is 1 star and 35 have zero.",
    "acted": "All 32 mirrors rewritten through a single generator so a sync reproduces the live bytes exactly. Coverage against the 18 frozen buyer questions moved 0.608 to 0.862, topics 8-9 to 15-16, and the README now leads with capability and only install paths that work.",
    "result": "22 of 34 to 32 of 34 surfaced, rank 1 from 10 to 15. Contested queries stayed 0 of 7 in the top three across all three runs, so coverage fixed appearing and not winning. The second pass ran about 20 minutes after the change, so its small moves are index noise and the honest re-measure dates are recorded as 2026-09-17 and 2026-10-10.",
    "refused": "Four keyword additions were refused as untrue, notably putting pdf on bank-statement, which is CSV only.",
    "gemini": "30 of 32 mirrors carry a Gemini CLI extension manifest and topic, verified live. No submission, no account, no queue.",
    "independent_spot_check": "Verified by the orchestrator against the live GitHub search API, separately from the agent that made the change. mcp invoice generator: rank 6 of 11, having been absent because the word generator was missing. mcp server petty cash: rank 1 of 1. mcp zip unzip archives: rank 1 of 3. mcp quote estimate: rank 1 of 3. mcp currency converter: absent on a field of 30, which is a contested query and matches the agent stated caveat that coverage fixed appearing and not winning."
  },
  "product_gap": {
    "finding": "Blind question 4, whether an assistant can read a bank statement PDF and categorise the transactions, has NO honest answer in this estate. The bank-statement server takes CSV and text exports, not PDFs.",
    "why_it_matters": "It is the one question of the 18 where our own catalogue implies an answer we cannot give, and the market named four competitors for it. A PDF-to-transactions path is the clearest demand-evidenced product gap the loop found.",
    "status": "Recorded, not acted on. Adding it late in this loop would have been scope creep."
  },
  "google": {
    "cause": "The inbound link graph into mcp.zovo.one resolves to ONE url. 129 of the 429 indexed zovo.one pages carry a followed sitewide anchor into the host and every one of them points at the bare root. That root is the only URL Google has ever crawled, the only one indexed, and the only one with an impression, at position 7.0 on 2026-09-08.",
    "census": "A full 154-URL inspection census returns 1 indexed, 1 ever crawled, 115 Discovered-currently-not-indexed and 38 unknown, every one with lastCrawlTime null.",
    "killed": "Eight competing explanations were tested and killed: robots.txt served byte-identical to the source with no Cloudflare injection, off-host canonicals, noindex, hreflang, Googlebot-versus-browser cloaking, JS-dependent rendering, sitemap validity, and loop 29 thin-content hypothesis. That last one is killed by arithmetic: Google has fetched 1 of 154 pages, so it cannot have declined 153 on content, and the earlier 312-to-154 sitemap trim could not have moved this.",
    "host_level": "Yes, but not specific to us. 8 of 8 zovo.one URLs at comparable depth are indexed against 1 of 11 on mcp, and the sibling subdomain tg.zovo.one shows the identical shape. This is ordinary new-host crawl rationing spent on whatever is linked, and the linked set was one URL wide.",
    "acted": "Five deep contextual links added on pages verified as indexed first, and pushed. A footer repair went with them: the repo had lost a live anchor, so deploying as it stood would have deleted a link across the estate.",
    "counter_evidence": "Recorded against our own lever: three deep links placed the same way a day earlier have live targets that are still uncrawled. This may not work.",
    "stale_number_corrected": "The 0 Google impressions figure this loop opened with is now stale. It is 1."
  },
  "sitemap_lastmod_decision": "The served sitemap carries zero lastmod across all 154 URLs. Deliberately not added. The only date available at build time is the deploy date, so every page would claim it changed on every deploy, and a lastmod that is not trustworthy is discounted by crawlers rather than ignored. A real per-page modification date would have to come from content history, which is worth doing properly or not at all.",
  "checked_not_a_defect": [
    "The two different IndexNow keys in data/ were flagged as a latent defect. They are not. billing/src/index.js serves BOTH from a SITE_KEY_FILES set with a comment explaining that the submitted key string and its keyLocation must match exactly or every URL is rejected, so keeping an old key served is deliberate. data/indexnow.key has no reader; the submitting script reads data/indexnow.json, whose key and keyLocation agree with each other. Recorded so it is not re-raised."
  ],
  "new_servers": {
    "method": "70 candidates probed with full cursor pagination of the registry to compute where each would actually land, rather than guessing.",
    "top5": "checklist rank 7 (score .977), itinerary rank 1 (.926), glossary rank 1 (.837), supplier rank 6 (.677), readability rank 1 (.644).",
    "built": "checklist, the top candidate, whose three registry names put one codebase on page one for checklist at 7, snag at 2, handover at 3 and onboarding at 7. And packing-list at rank 3, built because it is the strongest candidate inside the order-to-invoice chain the estate already covers.",
    "honesty": "Bare-token GitHub counts turned out to be homonym-contaminated by up to 767x, so training ranked first on the raw count and 25th once qualified. Every candidate was re-queried with a qualified phrase and both counts published with the ratio. The MCP-spec axis was measured with a working positive control, returned zero real hits for all 70, and is recorded as empty rather than scored. 12 candidates are marked demand-unsourced and excluded rather than given a number.",
    "not_sold_yet": "Deliberately not added to the billing catalogue. A product page that takes money for a server whose bundle is not downloadable and whose registry row does not exist is worse than no product page. Release, then publish, then sell."
  },
  "post_deploy_verified": [
    "Accept negotiation live: a POST with no Accept header and one with */* both went 406 to 200, while application/json alone and text/html still 406 because they genuinely exclude one of the required media types. The auth and transport boundaries did not move.",
    "The connect page live: a crawler gets the page with zero tokens minted and a button, and an explicit mint still works.",
    "The 401 message live: it now names /mcp/connect, where a token comes from, rather than only where to put one.",
    "Hosted zip_extract live: publishes one download link per entry again.",
    "Checkout live: a browser navigation gets a real Stripe session, and a Googlebot-shaped request is sent to the product page instead of having a session created for it.",
    "Content live: sitemap 154 to 164 including the connect page and both wedge pages, llms.txt no longer points at a URL that 404s, product pages say 31-server bundle instead of nineteen, and the home page opens with the count, the exact token command and a table of every server with the URL to paste.",
    "Gemini CLI manifests live on 30 of 32 mirrors with the topic set, and correctly absent on the two whose endpoints 404.",
    "Live validation 951 of 951 after both deploys."
  ],
  "registry_npm_validation": "The registry now validates that a declared npm package exists and refuses the manifest when it does not, with a 400 naming the 404 on npmjs.com. Four of about ninety manifests declare npm and the rest declare an mcpb bundle and publish fine, so the catalogue is not stuck, but those four rows cannot exist until the packages do. This is the likely cause of the registry KPI moving from 85 of 85 to 72 of 91, and it changes the npm sign-in from the most valuable human step to a blocking one for that slice. Rule worth keeping: never ship a server with an npm manifest alone."
}