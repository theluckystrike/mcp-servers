// generated by scripts/build-pages.mjs, do not edit
export const PAGES = {"time-tracker":{"title":"MCP Time Tracker","tagline":"Track billable time without leaving the chat.","description":"Starts and stops timers, records manual entries, sets hourly rates per project, and turns the log into reports grouped by project, day, task or tag, CSV exports and invoice-ready line items. Money is computed from seconds and integer cents in one rounding step so totals always add up.","html":"<h1>mcp-time-tracker</h1>\n<p>Track billable time without leaving your AI chat. Say &quot;start a timer on the acme redesign&quot;, keep working, then\nask for &quot;my hours this week by project&quot; or &quot;invoice lines for acme in August&quot;. It keeps a running timer, lets you\nlog time you forgot to track, applies your hourly rate per project, and turns the result into a report, a CSV file\nor a set of invoice line items. Everything is stored as plain JSON on your own machine.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n<p><img src=\"../../assets/demo-time-tracker.gif\" alt=\"time-tracker demo\"></p>\n<p><strong>Track billable time from chat and turn it straight into a report or invoice line items -- zero setup, all local.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-time-tracker</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>time-tracker.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;time-tracker&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-time-tracker&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add time-tracker -- npx -y @theluckystrike/mcp-time-tracker\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;time-tracker&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-time-tracker&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/time-tracker\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/time-tracker/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>timer_start</code></td>\n<td>Start a timer on a project (optional task, tags, rate, currency). Starting a new one stops and logs the old one. A partial project name that matches exactly one existing project is used as that project.</td>\n</tr>\n<tr>\n<td><code>timer_stop</code></td>\n<td>Stop the running timer, write the entry, return the duration.</td>\n</tr>\n<tr>\n<td><code>timer_status</code></td>\n<td>What is running, for how long, and today&#39;s total.</td>\n</tr>\n<tr>\n<td><code>entry_add</code></td>\n<td>Log time you already worked (start plus end or minutes), with an optional rate and currency: rate &quot;90 euros an hour&quot; bills as EUR 225.00 for 2.5 h. Partial project names resolve like <code>timer_start</code>.</td>\n</tr>\n<tr>\n<td><code>entry_list</code></td>\n<td>Compact table of entries, filtered by date range and project.</td>\n</tr>\n<tr>\n<td><code>entry_edit</code></td>\n<td>Change any field of an entry.</td>\n</tr>\n<tr>\n<td><code>entry_delete</code></td>\n<td>Delete an entry by id.</td>\n</tr>\n<tr>\n<td><code>project_set_rate</code></td>\n<td>Set the hourly rate and currency used for money totals (currency accepts codes or words: EUR, euros, pounds, zl). <code>apply_to_existing: true</code> re-rates time already logged for that project; add <code>only_missing: true</code> to touch only entries that carry no rate.</td>\n</tr>\n<tr>\n<td><code>report</code></td>\n<td>Hours and money for a period, optionally grouped by project, day, task or tag; omit <code>group_by</code> for the plain total per currency. Table, JSON or CSV. Hours already invoiced are left out; pass <code>unbilled_only: false</code> for the full timesheet.</td>\n</tr>\n<tr>\n<td><code>export_csv</code></td>\n<td>Write entries to a CSV file and return the path.</td>\n</tr>\n<tr>\n<td><code>invoice_summary</code></td>\n<td>Invoice-ready line items for one project: hours, rate, amount, total, in the currency the time was logged in. One line per task and rate, so no line ever shows a blended rate nobody agreed. Returns the <code>entry_ids</code> behind the lines, and skips hours already invoiced (<code>unbilled_only: false</code> includes them). Free for the last 7 days, Pro for any period from full history.</td>\n</tr>\n<tr>\n<td><code>entry_mark_billed</code></td>\n<td>Stamp the hours that went on an invoice with its number (<code>ids</code> from <code>invoice_summary</code>, or <code>project</code> + <code>from</code> + <code>to</code>), so <code>report</code> and <code>invoice_summary</code> stop offering them and the same hours are never billed twice.</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Free or Pro, and where to upgrade.</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline).</td>\n</tr>\n</tbody></table>\n<p>Also exposed: the resource <code>timetracker://today</code> (today&#39;s summary) and the prompt <code>daily_standup</code>\n(writes a standup update from yesterday&#39;s and today&#39;s tracked time).</p>\n<h2>What you can say</h2>\n<p>No tool names required. These are the sentences that were actually tested against the server; the tool\ncolumn is what answered them.</p>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;Start a timer for the Acme website project.&quot;</td>\n<td><code>timer_start</code></td>\n</tr>\n<tr>\n<td>&quot;Stop the timer and tell me how long I worked.&quot;</td>\n<td><code>timer_stop</code></td>\n</tr>\n<tr>\n<td>&quot;What&#39;s running right now, and for how long?&quot;</td>\n<td><code>timer_status</code></td>\n</tr>\n<tr>\n<td>&quot;Log 2.5 hours yesterday for Acme, design review, at 90 euros an hour.&quot;</td>\n<td><code>entry_add</code></td>\n</tr>\n<tr>\n<td>&quot;Show me my Acme entries for this week.&quot;</td>\n<td><code>entry_list</code></td>\n</tr>\n<tr>\n<td>&quot;That last entry should say API refactor, not design review.&quot;</td>\n<td><code>entry_edit</code></td>\n</tr>\n<tr>\n<td>&quot;Delete the entry I just logged twice.&quot;</td>\n<td><code>entry_delete</code></td>\n</tr>\n<tr>\n<td>&quot;Set my rate for Acme to 90 EUR an hour.&quot;</td>\n<td><code>project_set_rate</code></td>\n</tr>\n<tr>\n<td>&quot;How many hours did I bill this month, grouped by project?&quot;</td>\n<td><code>report</code></td>\n</tr>\n<tr>\n<td>&quot;Give me invoice lines for Acme in August.&quot;</td>\n<td><code>invoice_summary</code></td>\n</tr>\n<tr>\n<td>&quot;I invoiced those hours as INV-2026-0001.&quot;</td>\n<td><code>entry_mark_billed</code></td>\n</tr>\n</tbody></table>\n<p>Two more worth knowing: &quot;export my time to a CSV for my bookkeeper&quot; (<code>export_csv</code>) and &quot;write my standup\nupdate from yesterday and today&quot; (the <code>daily_standup</code> prompt).</p>\n<h2>Worked example</h2>\n<p>This is a real transcript from the audit in <code>docs/USER_VALUE_R2.md</code>, numbers unchanged.</p>\n<pre><code>You: Log 2.5 hours yesterday for Acme, design review, at 90 euros an hour.\n\n  entry_add { project: &quot;Acme&quot;, ... }\n  -&gt; &quot;Acme&quot; resolved to the existing project &quot;Acme website&quot;\n  -&gt; rate &quot;90 euros an hour&quot; parsed as EUR 90.00/h\n\nAssistant: Logged 2.50 h for Acme website - design review at EUR 90.00/h = EUR 225.00.\n\nYou: How much do I bill Acme this week? Give me invoice lines.\n\n  invoice_summary { project: &quot;Acme website&quot;, from: &quot;2026-08-31&quot;, to: &quot;2026-09-02&quot; }\n\nAssistant: Invoice summary - Acme website\n  design review    2.50 h   EUR 90.00/h   EUR 225.00\n  TOTAL            2.50 h                 EUR 225.00\n</code></pre>\n<p>One call each. The rate carries its currency all the way through: the report never prints a bare &quot;225&quot;,\nand it never turns into &quot;$225&quot; by accident.</p>\n<p>A second worked example, the weekly report and the <code>daily_standup</code> prompt:</p>\n<pre><code>You: How many hours did I put into Acme this week, grouped by task?\n\n  report { project: &quot;Acme website&quot;, from: &quot;2026-08-27&quot;, to: &quot;2026-09-02&quot;, group_by: &quot;task&quot; }\n\nAssistant: Acme website, 2026-08-27 to 2026-09-02\n  design review   2.50 h   EUR 225.00\n  TOTAL           2.50 h   EUR 225.00\n\nYou: Write my standup update.\n\n  (prompt) daily_standup\n\nAssistant: Yesterday: 2.50 h on Acme website (design review).\nToday: nothing logged yet.\n</code></pre>\n<h3>Billed hours close</h3>\n<p>An hour that has been invoiced is finished. <code>entry_mark_billed {ids, invoice_number}</code> writes\n<code>billed_at</code> and <code>billed_invoice</code> onto those entries; from then on <code>report</code> and <code>invoice_summary</code>\nskip them by default, so next month&#39;s &quot;invoice Acme&quot; cannot re-bill work already paid for. The\nwhole timesheet is still there: pass <code>unbilled_only: false</code> to any of them. <code>invoice_summary</code>\nreturns the <code>entry_ids</code> it used precisely so they can be handed straight to <code>entry_mark_billed</code>\nonce the invoice exists.</p>\n<p><code>report</code> and <code>invoice_summary</code> answer overlapping questions on purpose: <code>report</code> is for &quot;how much time\nand money,&quot; grouped any way you like; <code>invoice_summary</code> is for &quot;give me the lines I can put on an\ninvoice,&quot; which is a narrower, invoice-shaped view of the same entries for one project.</p>\n<h2>How it stores data</h2>\n<p>Entries, projects and rates live in one JSON file:\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/time-tracker/data.json</code>.</p>\n<p>Every write (starting or stopping a timer, adding, editing or deleting an entry, setting a rate) happens\nunder an advisory lock file at <code>.../time-tracker/.lock</code>, held across the whole load-mutate-save cycle, so\ntwo overlapping calls cannot interleave and corrupt the file. The save itself writes to a temporary file\nand renames it into place, so a crash or a killed process mid-write leaves either the old file or the new\none, never a half-written one. Reads (<code>entry_list</code>, <code>report</code>, <code>timer_status</code>, <code>export_csv</code>) do not take\nthe lock.</p>\n<p>To back up your data, copy the single <code>data.json</code> file (and <code>.lock</code> if present, though it holds no data).\nThere is no database and no hidden second file.</p>\n<p>If <code>data.json</code> is ever unreadable or not valid JSON, the server does <strong>not</strong> treat that as &quot;no data yet&quot;.\nIt moves the file aside byte-for-byte as <code>data.json.corrupt-&lt;timestamp&gt;</code>, writes a <code>data.json.corrupt</code>\nmarker and makes every tool -- reads included -- return <code>data file is corrupt; moved to ...; nothing was written</code>. Restore a good <code>data.json</code> (the quarantined copy is right there) and delete the marker file to\ncarry on. Nothing is overwritten in the meantime.</p>\n<h2>Dates, times and rates</h2>\n<ul>\n<li><strong>Timestamps with no offset are your local time.</strong> <code>2026-09-02T09:00:00</code> means 09:00 where you are, not\nUTC. Pass an explicit offset (<code>2026-09-02T09:00:00+02:00</code>) or a trailing <code>Z</code> and it is honoured exactly.</li>\n<li><strong>Date-only bounds cover whole local days.</strong> <code>from: &quot;2026-09-01&quot;</code> is 00:00:00 local on the 1st and\n<code>to: &quot;2026-09-30&quot;</code> is 23:59:59.999 local on the 30th, so a month reported by dates includes its last\nday. Timestamps with a time are used as given.</li>\n<li><strong>Entries are clipped to the window.</strong> An entry that starts before <code>from</code> or ends after <code>to</code> counts for\nthe part inside the period, not all of it and not none of it.</li>\n<li><strong>Entries are split at local midnight for day grouping.</strong> Work from 23:30 to 01:30 is 0.5 h on the first\nday and 1.5 h on the next, including across a month boundary. <code>timer_status</code> counts only the part of an\nentry -- or of the running timer -- that falls after midnight today.</li>\n<li><strong>Rate strings are parsed, never guessed.</strong> <code>&quot;1,200 USD&quot;</code> is 1200 (a comma followed by exactly three\ndigits is thousands grouping), <code>&quot;12,50 EUR&quot;</code> is 12.50 (the unambiguous European decimal shape), and\n<code>&quot;1.200,50&quot;</code> is 1200.50. Anything that could mean either thing, such as <code>&quot;1,2345&quot;</code>, is refused with a\nworked example instead of being read as the wrong number.</li>\n<li><strong>Rates are captured when the time is logged.</strong> <code>entry_add</code> and <code>timer_stop</code> store the effective hourly\nrate and currency on the entry, and reports and invoices use that stored rate. <code>project_set_rate</code>\ntherefore applies to future entries only; pass <code>apply_to_existing: true</code> to re-rate the time already\nlogged for that project. That re-stamps EVERY entry of the project, including entries that already\ncarry a rate, and the response says how many changed and the project&#39;s new total. Add\n<code>only_missing: true</code> to touch only entries that captured no rate of their own.</li>\n<li><strong>Tag rows overlap.</strong> In <code>group_by: &quot;tag&quot;</code> an entry tagged <code>dev</code> and <code>meeting</code> appears in both rows; the\ntotal is computed from the entries once, so it is never the sum of the rows.</li>\n</ul>\n<h2>Limits and honest caveats</h2>\n<ul>\n<li>Free <code>entry_list</code>, <code>report</code>, <code>export_csv</code> and <code>invoice_summary</code> only see the last 7 days. Timers and\nentries themselves are unlimited and nothing is ever deleted -- the window just narrows what a free\ncall can read back.</li>\n<li>Free tier supports hourly rates on 2 projects; a third rated project needs Pro.</li>\n<li>Every <code>report</code> grouping is free, tag included: the tag total is a correctness fix, not a premium\nfeature. <code>group_by</code> itself is optional -- omit it for the plain total per currency.</li>\n<li>Only one timer can run at a time. Starting a second one stops and logs the first -- there is no\nconcurrent-timer mode.</li>\n<li>There is no reminder or idle-detection: if you forget to stop a timer, it keeps running until you stop\nit or start another.</li>\n</ul>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong><code>npx</code> hangs or fails to find the package</strong>: npm publish for this package is pending. Use the <code>.mcpb</code>\nbundle or the clone-and-build path above until it lands.</li>\n<li><strong>Using the <code>.mcpb</code> bundle</strong>: it installs into Claude Desktop directly; there is no separate path to\nconfigure.</li>\n<li><strong>Using the clone path</strong>: the server binary is <code>servers/time-tracker/dist/index.js</code> after\n<code>npm run build</code>. Point your client&#39;s <code>command</code> at <code>node</code> with that absolute path as the only argument.</li>\n<li><strong>Node version</strong>: requires Node &gt;= 18. Check with <code>node -v</code>.</li>\n<li><strong>Nothing shows up / silent failures</strong>: this server writes logs to stderr only, never stdout (stdout is\nreserved for the MCP protocol). In Claude Desktop, check Settings -&gt; Developer -&gt; the server&#39;s log\nfile; in Claude Code, run with <code>--mcp-debug</code> or check the terminal you launched it from.</li>\n<li><strong>A Pro key isn&#39;t recognized</strong>: run <code>license_status</code> to see what the server thinks your tier is, and\nconfirm <code>MCP_LICENSE_KEY</code> is set in the same process the client launches (not just your shell).</li>\n</ul>\n<h2>Privacy</h2>\n<p>All data stays local: entries live in <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/time-tracker/data.json</code>.\nThe server makes no network requests, has no telemetry, and needs no account. License keys are Ed25519\nsignatures verified offline against a public key compiled into the package -- activation works with no\ninternet connection.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../invoice/README.md\">mcp-invoice</a> -- turn <code>invoice_summary</code> output straight into a numbered PDF invoice.</li>\n<li><a href=\"../spreadsheet/README.md\">mcp-spreadsheet</a> -- export a CSV with <code>export_csv</code> and query or reshape it.</li>\n<li><a href=\"../price-tracker/README.md\">mcp-price-tracker</a> -- if you also buy things for the client, watch those prices.</li>\n<li><a href=\"../office-suite/README.md\">office-suite</a> -- all four servers behind one install, one config entry.</li>\n<li>Guide: <a href=\"https://mcp.zovo.one/guides/track-time-in-claude-code\">Track billable hours in Claude Code and Cursor</a></li>\n</ul>\n<h2>FAQ</h2>\n<p><strong>Does this work in Cursor as well as Claude Code and Claude Desktop?</strong>\nYes. All three speak MCP over stdio with the same config shape; the tools and the data file are identical\nregardless of client.</p>\n<p><strong>What happens when the free 7-day window runs out on an old entry?</strong>\nNothing is deleted. The entry stays in <code>data.json</code> forever; it just will not appear in <code>entry_list</code>,\n<code>report</code>, <code>export_csv</code> or <code>invoice_summary</code> results until you activate Pro, which opens full history.</p>\n<p><strong>Can I bill different clients in different currencies?</strong>\nYes. Currency is set per project (or per entry, overriding the project default) and every total is grouped\nby currency -- a report never adds EUR and USD together.</p>\n<p><strong>What happens if two entries have overlapping times?</strong>\nThe server does not block overlaps; it logs what you tell it. <code>entry_edit</code> lets you fix a mistake after\nthe fact.</p>\n<p><strong>Does it need an internet connection?</strong>\nNo. There are no network calls anywhere in this server, including for license activation, which is\nverified with a local public key.</p>\n<h2>License</h2>\n<p>MIT</p>\n<h2>One business profile for the whole suite</h2>\n<p>Your identity is stored once, at <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/profile/business.json</code>,\nand every server in the suite reads it: the invoice issuer, the docx letterhead, the recurring\nissuer, expense-tracker&#39;s default VAT rate, time-tracker&#39;s and timezone&#39;s home zone, and the\nresume and contract letterheads. Set it once with <code>business_set</code> (invoice or docx) - you never\nrepeat it anywhere else. An email address is only ever taken from that profile or from an explicit\nargument; when none is stored, documents show <code>[add: email]</code> and the tool says so rather than\nletting anyone improvise an address.</p>\n"},"price-tracker":{"title":"MCP Price Tracker","tagline":"Check and watch product prices on ordinary shop pages.","description":"Fetches a product page and extracts the price from JSON-LD, microdata, Open Graph tags, common price markup or a currency-aware text fallback, normalising 1.299,00 EUR and 1,299.00 USD alike. Stores watches with a price history, refreshes them, computes min, max and change, and flags target hits and drops of 5% or more. Manual entry covers pages that block bots.","html":"<h1>mcp-price-tracker</h1>\n<p>Ask your assistant what something costs right now. Point it at any product page and it reads the price off the page, remembers it, and tells you next time whether the price moved, how far, and whether it reached the number you were waiting for. It works on ordinary shop pages by reading the structured data most stores already publish (JSON-LD, Open Graph, microdata) and falling back to the visible price when they do not. When a large retailer blocks automated requests it says so plainly and lets you record the price yourself so the history stays intact. Everything is stored in a JSON file on your own machine.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n<p><img src=\"../../assets/demo-price-tracker.gif\" alt=\"price-tracker demo\"></p>\n<p><strong>Watch any product page for price drops from chat -- no scraping service, no account, all local.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-price-tracker</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>price-tracker.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;price-tracker&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-price-tracker&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add price-tracker -- npx -y @theluckystrike/mcp-price-tracker\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;price-tracker&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-price-tracker&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/price-tracker\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/price-tracker/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>price_check</code></td>\n<td>Fetch a page now; returns price, currency, title, extraction confidence and the change against the last stored observation for that URL. No watch needed.</td>\n</tr>\n<tr>\n<td><code>watch_add</code></td>\n<td>Start tracking a URL. Stores the first observation. Optional <code>label</code>, <code>target_price</code>, <code>currency</code>.</td>\n</tr>\n<tr>\n<td><code>watch_list</code></td>\n<td>Every watch with current price, min, max, change, target, confidence and source.</td>\n</tr>\n<tr>\n<td><code>watch_remove</code></td>\n<td>Stop tracking, by <code>id</code> or <code>url</code>.</td>\n</tr>\n<tr>\n<td><code>watch_refresh</code></td>\n<td>Re-fetch one watch (<code>id</code>) or all of them (<code>all: true</code>); appends observations and returns current, previous, min, max, change % and target-hit flags. This is the only thing that checks prices.</td>\n</tr>\n<tr>\n<td><code>price_history</code></td>\n<td>Stored observations for one watch, oldest first, optional <code>limit</code>.</td>\n</tr>\n<tr>\n<td><code>price_add_manual</code></td>\n<td>Record a price you read yourself, for shops that block bots. Creates the watch if needed.</td>\n</tr>\n<tr>\n<td><code>alerts_pending</code></td>\n<td>Watches whose latest price is at or below target, or which dropped 5% or more since the previous observation. Free.</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Free or Pro, and where to upgrade.</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key. Verified offline.</td>\n</tr>\n</tbody></table>\n<p>Resource: <code>prices://watches</code> - JSON of every watch with its latest price.</p>\n<p>Prompt: <code>check_prices</code> - refreshes every watch and summarises the drops and target hits in one command.</p>\n<h2>What you can say</h2>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;What does this cost right now: &lt;url&gt;?&quot;</td>\n<td><code>price_check</code></td>\n</tr>\n<tr>\n<td>&quot;Watch that page and alert me if it goes under 40.&quot;</td>\n<td><code>watch_add</code></td>\n</tr>\n<tr>\n<td>&quot;Show me everything I&#39;m watching and whether anything dropped.&quot;</td>\n<td><code>watch_refresh</code> (all) then <code>watch_list</code></td>\n</tr>\n<tr>\n<td>&quot;Stop tracking that laptop, I bought it elsewhere.&quot;</td>\n<td><code>watch_remove</code></td>\n</tr>\n<tr>\n<td>&quot;Refresh my watches.&quot;</td>\n<td><code>watch_refresh</code></td>\n</tr>\n<tr>\n<td>&quot;What&#39;s the price history on the bookshelf I&#39;m watching?&quot;</td>\n<td><code>price_history</code></td>\n</tr>\n<tr>\n<td>&quot;I checked myself, it&#39;s 39.99 GBP in-store, log that.&quot;</td>\n<td><code>price_add_manual</code></td>\n</tr>\n<tr>\n<td>&quot;Has anything I&#39;m watching hit its target or dropped a lot?&quot;</td>\n<td><code>alerts_pending</code></td>\n</tr>\n<tr>\n<td>&quot;Am I on the free plan or Pro?&quot;</td>\n<td><code>license_status</code></td>\n</tr>\n<tr>\n<td>&quot;Here&#39;s my license key, activate it.&quot;</td>\n<td><code>license_activate</code></td>\n</tr>\n</tbody></table>\n<h2>Worked example</h2>\n<p>A real run against <code>books.toscrape.com</code>, from <code>docs/USER_VALUE_R2.md</code>, with <code>WebFetch</code>/<code>WebSearch</code>\ndisallowed so the price-tracker&#39;s own tools had to answer:</p>\n<pre><code>You: What does this cost right now: https://books.toscrape.com/catalogue/...?\n\n  price_check { url: &quot;https://books.toscrape.com/catalogue/...&quot; }\n  -&gt; price 51.77, currency GBP, source regex-fallback, confidence low\n\nAssistant: GBP 51.77. Confidence: low (regex-fallback) -- this page has no\nstructured price data, so I read it from the visible text.\n</code></pre>\n<p>The confidence label is not decoration: <code>books.toscrape.com</code> carries no JSON-LD or microdata, so this is\nthe one case in testing where the regex fallback was the only path to a number, and the model relayed the\n&quot;low&quot; label to the user unprompted rather than stating 51.77 as a fact.</p>\n<h2>Alerts: nothing runs in the background</h2>\n<p>There is no scheduler and no daemon. A watch is a stored history, not a subscription: prices are re-read only when <code>watch_refresh</code> runs, and <code>alerts_pending</code> reports on what is already stored. The working pattern is to say <strong>&quot;refresh my watches&quot;</strong> at the start of a session (or run the <code>check_prices</code> prompt), then ask what dropped.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro ($19 one-time)</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>price_check</code></td>\n<td>Unlimited</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>alerts_pending</code></td>\n<td>Yes, unlimited</td>\n<td>Yes, unlimited</td>\n</tr>\n<tr>\n<td>Watches</td>\n<td>3</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td>History per watch</td>\n<td>Last 30 observations</td>\n<td>Full history</td>\n</tr>\n<tr>\n<td><code>watch_refresh</code> one item</td>\n<td>Yes</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td><code>watch_refresh</code> all at once</td>\n<td>No</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td><code>price_add_manual</code></td>\n<td>Yes</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td>Redirect check and extraction confidence</td>\n<td>Yes</td>\n<td>Yes</td>\n</tr>\n</tbody></table>\n<p><a href=\"https://mcp.zovo.one/buy/price-tracker\">Get Pro</a> - $19 for this server, $39 for <a href=\"https://mcp.zovo.one/buy/bundle\">every server, lifetime</a>.</p>\n<h2>How prices are read</h2>\n<p>In order: JSON-LD <code>Product</code>/<code>Offer</code> (<code>price</code>, <code>priceCurrency</code>, <code>lowPrice</code>), microdata <code>itemprop=&quot;price&quot;</code>, Open Graph <code>og:price:amount</code> / <code>product:price:amount</code>, <code>meta itemprop</code>, <code>data-price</code> attributes, common price class and id hints (including Amazon&#39;s <code>a-offscreen</code>), and finally a regex over the visible text of the first 200 KB. European and US separators are both understood, so <code>1.299,00 EUR</code> and <code>1,299.00 USD</code> both become <code>1299.00</code>.</p>\n<p>Prices are stored as decimal strings in the major unit with a <code>.</code> decimal separator and no grouping, for example <code>&quot;1299.00&quot;</code>. Timestamps are ISO 8601 UTC.</p>\n<p>Every reading carries a <strong>confidence</strong>: <code>high</code> for JSON-LD, microdata, <code>meta itemprop</code> and Open Graph; <code>medium</code> for <code>data-price</code> attributes and price class or id hints; <code>low</code> for the regex fallback. A <code>low</code> reading on a page with no product title is reported but never stored.</p>\n<p>Where a page describes several products in JSON-LD (recommendations, &quot;customers also bought&quot;), the offers of one product are never pooled with another&#39;s: the product whose name matches the page title wins, otherwise the first product carrying offers. Crossed-out and previous prices (<code>&lt;s&gt;</code>, <code>&lt;del&gt;</code>, <code>&lt;strike&gt;</code>, and classes such as <code>old-price</code>, <code>was</code>, <code>compare-at-price</code>, <code>list-price</code>, <code>rrp</code>) are skipped, so a struck 199 next to a live 99 reports 99. An ISO code written next to the number (&quot;$10 USD&quot;) wins over the currency guessed from the domain, and <code>twitter:data1</code> - free text such as &quot;Free shipping over $50&quot; - is not read as a price.</p>\n<p>Pages are fetched with a desktop browser User-Agent, a 12 second timeout, redirects followed, and a 2 MB body cap. After the fetch the final URL is compared with the one you asked for: if the shop redirected off the product path - a different path depth, a category or home page, or a generic title such as &quot;Products&quot;, &quot;Home&quot;, the shop name alone, or &quot;not found&quot; - you get <code>the shop redirected to &lt;finalUrl&gt;, which is not a product page</code> instead of the cheapest item on the listing it landed on. A redirect that only inserts a canonical slug in front of the same product id or path (Newegg, Amazon <code>/dp/</code>, Zalando-style URLs) is accepted, not refused.</p>\n<h2>How it stores data</h2>\n<p>Watches and their price history live in one JSON file:\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/price-tracker/watches.json</code>. Every mutation (<code>watch_add</code>,\n<code>watch_remove</code>, <code>watch_refresh</code>, <code>price_add_manual</code>) happens under an advisory lock file,\n<code>.../price-tracker/.lock</code>, held across the load-mutate-save cycle so two refreshes running at once cannot\ninterleave. <code>price_check</code> and read-only tools do not need the lock. Saves write to a temporary file and\nrename it into place, so an interrupted write cannot leave a half-written file. To back up your watch\nhistory, copy <code>watches.json</code>.</p>\n<p>Only a missing file counts as &quot;no watches yet&quot;. If <code>watches.json</code> exists but cannot be read or parsed, the\nserver never treats it as empty: the unreadable bytes are moved to <code>watches.json.corrupt-&lt;timestamp&gt;</code>, the\nfault is written to stderr, and every tool answers with an error naming that file until the server is\nrestarted. A damaged database can therefore never be silently overwritten by the next price you record.</p>\n<h2>Limits and honest caveats</h2>\n<ul>\n<li><strong>Real-world extraction success is roughly 5 of 12</strong> on a mixed sample of major retailers: five sites\nreturned an outright bot-wall 403 (H&amp;M, Allegro, MediaMarkt, Home Depot, Etsy), one timed out (Best\nBuy), and the rest split between correct prices and one intentionally refused reading (a redirect off\nthe product page). This is a property of the open web, not a bug the server can fix -- there is no\nheadless browser or CAPTCHA solver here, by design (no native deps, no paid API). <code>price_add_manual</code>\nis the working answer for a blocked shop.</li>\n<li><strong>There is no background job.</strong> Nothing checks prices unless you (or your client, via a scheduled\nscript you run yourself) call <code>watch_refresh</code>. This is not a missing feature; a local stdio server has\nno business running a daemon on your machine.</li>\n<li>Free tier caps watches at 3 and history at the last 30 observations per watch; <code>price_check</code> and\n<code>alerts_pending</code> are unlimited on free.</li>\n<li>A <code>low</code>-confidence regex reading is still reported, but never silently treated as certain -- pass it on\nto the user as an estimate, not a fact.</li>\n</ul>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong><code>npx</code> hangs or fails to find the package</strong>: npm publish for this package is pending. Use the <code>.mcpb</code>\nbundle or the clone-and-build path above until it lands.</li>\n<li><strong>Using the <code>.mcpb</code> bundle</strong>: it installs into Claude Desktop directly; there is no separate config\nstep.</li>\n<li><strong>Using the clone path</strong>: the server binary is <code>servers/price-tracker/dist/index.js</code> after\n<code>npm run build</code>. Point your client&#39;s <code>command</code> at <code>node</code> with that absolute path as the only argument.</li>\n<li><strong>Node version</strong>: requires Node &gt;= 18. Check with <code>node -v</code>.</li>\n<li><strong>A watch always errors &quot;not a product page&quot;</strong>: the shop redirected you off the URL you gave it (a\ncategory page, home page, or a generic title). Fetch the exact product URL again from the shop and\nre-add the watch, or use <code>price_add_manual</code> if the shop keeps redirecting.</li>\n<li><strong>A shop returns 403 every time</strong>: it is blocking automated requests. This is expected for a subset of\nlarge retailers (see Limits above); use <code>price_add_manual</code>.</li>\n<li><strong>Nothing shows up / silent failures</strong>: logs go to stderr only, never stdout. In Claude Desktop check\nSettings -&gt; Developer -&gt; the server&#39;s log file; in Claude Code check the terminal or <code>--mcp-debug</code>.</li>\n</ul>\n<h2>Privacy</h2>\n<p>All data stays local, in <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/price-tracker/watches.json</code>. The only network requests are to the product pages you name. License keys verify offline; nothing is sent anywhere.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../invoice/README.md\">mcp-invoice</a> -- bill a client for something you bought after tracking its price.</li>\n<li><a href=\"../spreadsheet/README.md\">mcp-spreadsheet</a> -- export <code>price_history</code> and analyze it as a sheet.</li>\n<li><a href=\"../time-tracker/README.md\">mcp-time-tracker</a> -- track the hours you spend shopping around, if that is somehow billable.</li>\n<li><a href=\"../office-suite/README.md\">office-suite</a> -- all four servers behind one install, one config entry.</li>\n<li>Guide: <a href=\"https://mcp.zovo.one/guides/price-drop-alerts-with-claude\">Watch a product price with Claude and get told when it drops</a></li>\n</ul>\n<h2>FAQ</h2>\n<p><strong>Why didn&#39;t it check the price automatically overnight?</strong>\nThere is no scheduler in this server. Prices are only re-read when <code>watch_refresh</code> runs, which happens\nwhen you ask for it (or when a cron/launchd job you set up yourself calls it).</p>\n<p><strong>Why does it refuse to give me a price on some pages?</strong>\nEither the shop returned a bot-wall response (403) or the request redirected off the product page onto a\nlisting, category or home page. Both are reported honestly instead of returning a wrong number; use\n<code>price_add_manual</code> to keep the history going by hand.</p>\n<p><strong>What does &quot;confidence: low&quot; mean?</strong>\nThe price came from a text-pattern fallback rather than the page&#39;s own structured data (JSON-LD,\nmicrodata, Open Graph). It is usually right but has not been verified against a machine-readable field.</p>\n<p><strong>Can I track a price in a currency different from what the page shows?</strong>\n<code>watch_add</code> and <code>price_add_manual</code> accept an explicit <code>currency</code>, but the number you store should match\nwhat you actually read -- the server does not convert currencies.</p>\n<p><strong>Does it ever send my watch list anywhere?</strong>\nNo. The only outbound requests are to the product URLs you add, to fetch their pages. There is no\ntelemetry and no account.</p>\n<p>MIT licensed.</p>\n"},"spreadsheet":{"title":"MCP Spreadsheet","tagline":"Read, query, edit and convert xlsx and csv files safely.","description":"Opens xlsx, xlsm, xlsb, ods, csv and tsv files, guesses the header row on messy exports, sniffs delimiters, and answers questions with a safe expression language (no eval): filters, sorts, per-column stats, find, computed columns such as [Qty] * [Unit Price], and conversion between formats. It never overwrites the source unless told to.","html":"<h1>mcp-spreadsheet</h1>\n<p>Hand your AI assistant a spreadsheet and talk to it. Point it at any <code>.xlsx</code>, <code>.xlsm</code>, <code>.xlsb</code>, <code>.xls</code>, <code>.ods</code>, <code>.csv</code> or <code>.tsv</code> file on your machine and ask what is in it, filter it, compute a new column, or save it in another format. It handles the messy parts of real files for you: it guesses which row holds the headers, sniffs whether a CSV is separated by commas, semicolons or tabs, keeps quoted commas and newlines intact, reads numbers out of <code>$1,250.00</code> style text, and reports per-column types and empty counts. It never edits your original file: every write goes to a new path unless you explicitly choose <code>overwrite</code>. Nothing leaves the machine, and there is no API key to get.</p>\n<p><img src=\"../../assets/demo-spreadsheet.gif\" alt=\"spreadsheet demo\"></p>\n<p><strong>Read, query and extend real spreadsheets from chat without ever touching the original file.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-spreadsheet</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>spreadsheet.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;spreadsheet&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-spreadsheet&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add spreadsheet -- npx -y @theluckystrike/mcp-spreadsheet\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;spreadsheet&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-spreadsheet&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/spreadsheet\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/spreadsheet/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>sheet_info</code></td>\n<td>Sheet names, size, guessed header row, per-column type, sample values, empty counts</td>\n</tr>\n<tr>\n<td><code>sheet_read</code></td>\n<td>Read rows as a text table, JSON records or CSV; <code>limit</code>/<code>offset</code> paging or an A1 <code>range</code></td>\n</tr>\n<tr>\n<td><code>sheet_query</code></td>\n<td>Filter with <code>where</code>, <code>group_by</code> + <code>aggregate</code> (sum, count, avg, min, max), pick columns with <code>select</code>, <code>sort</code> (aggregate aliases too), <code>limit</code></td>\n</tr>\n<tr>\n<td><code>sheet_stats</code></td>\n<td>count, empty, distinct, min, max, sum, mean, median per column (top values for text columns)</td>\n</tr>\n<tr>\n<td><code>sheet_find</code></td>\n<td>Find text anywhere in the workbook; returns cell addresses and a row preview</td>\n</tr>\n<tr>\n<td><code>sheet_write</code></td>\n<td>Write rows (objects or arrays) as a new file, an append, or an explicit overwrite</td>\n</tr>\n<tr>\n<td><code>sheet_add_column</code></td>\n<td>Add a computed column from a formula, saved to a new file. Numeric results are rounded like their inputs (2 decimals in, 2 decimals out); <code>decimals</code> overrides</td>\n</tr>\n<tr>\n<td><code>sheet_convert</code></td>\n<td>Convert a sheet to csv, xlsx or json</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Free or Pro, and where to upgrade</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline)</td>\n</tr>\n</tbody></table>\n<p>Resource template: <code>sheet://&lt;path&gt;</code> returns the <code>sheet_info</code> summary for that file.\nResource: <code>sheet://recent</code> lists the files this server has opened since it started, most recent first (in memory only -- nothing is written to disk, so the list is empty again after a restart).</p>\n<p>Prompt: <code>explore_sheet</code> walks an unfamiliar file -- <code>sheet_info</code> first, then concrete <code>sheet_query</code> calls built from the columns it actually found.</p>\n<h2>What you can say</h2>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;Open sales.xlsx and tell me what&#39;s in it.&quot;</td>\n<td><code>sheet_info</code></td>\n</tr>\n<tr>\n<td>&quot;Show me the first 50 rows.&quot;</td>\n<td><code>sheet_read</code></td>\n</tr>\n<tr>\n<td>&quot;Which rep sold the most units in the North region? Top 5 with totals.&quot;</td>\n<td><code>sheet_query</code> (<code>group_by</code> + <code>aggregate</code>)</td>\n</tr>\n<tr>\n<td>&quot;How many empty cells are in the Email column, and what&#39;s the median order size?&quot;</td>\n<td><code>sheet_stats</code></td>\n</tr>\n<tr>\n<td>&quot;Find every row that mentions &#39;refund&#39;.&quot;</td>\n<td><code>sheet_find</code></td>\n</tr>\n<tr>\n<td>&quot;Write this as a new sheet called Q3 Results.&quot;</td>\n<td><code>sheet_write</code></td>\n</tr>\n<tr>\n<td>&quot;Add a Revenue column that&#39;s Units times Unit Price, save it as a CSV next to the original.&quot;</td>\n<td><code>sheet_add_column</code></td>\n</tr>\n<tr>\n<td>&quot;Convert this xlsx to JSON.&quot;</td>\n<td><code>sheet_convert</code></td>\n</tr>\n<tr>\n<td>&quot;Am I on the free plan or Pro?&quot;</td>\n<td><code>license_status</code></td>\n</tr>\n<tr>\n<td>&quot;Here&#39;s my license key, activate it.&quot;</td>\n<td><code>license_activate</code></td>\n</tr>\n</tbody></table>\n<h2>Worked example</h2>\n<p>From <code>docs/USER_VALUE_R2.md</code>, a 400-row fixture with a title row and a blank row above the real header\n(row 3), sheets <code>Sales</code> / <code>Reps</code> / <code>Notes</code>, prices stored as strings like <code>&quot;1,516.16&quot;</code>. One call, ground\ntruth confirmed against the generator: Turing 650, Hopper 567, Linus T 551, Lovelace 486, Liskov 290.</p>\n<pre><code>You: Which rep sold the most units in the North region? Top 5 with totals.\n\n  sheet_query {\n    path: &quot;/Users/you/sales.xlsx&quot;, sheet: &quot;Sales&quot;,\n    where: &quot;[Region] = \\&quot;North\\&quot;&quot;,\n    group_by: [&quot;Rep&quot;],\n    aggregate: [{ col: &quot;Units&quot;, fn: &quot;sum&quot;, as: &quot;total_units&quot; }],\n    sort: { col: &quot;total_units&quot;, dir: &quot;desc&quot; },\n    limit: 5\n  }\n\nAssistant: Query: where [Region] = &quot;North&quot;; group by Rep; sum Units as total_units;\nsort total_units desc; limit 5\n5 groups from 102 of 400 rows, showing 5\n\nAlan Turing     650\nGrace Hopper    567\nLinus T         551\nAda Lovelace    486\nBarbara Liskov  290\n</code></pre>\n<p>The response now echoes the exact query it ran (the <code>Query:</code> line) before the numbers, so a filter you\ndid not ask for -- an accidental <code>AND [Status] = &quot;Closed&quot;</code>, say -- is visible instead of silently\nnarrowing the answer.</p>\n<p>A second example, adding a column and saving without touching the source:</p>\n<pre><code>You: Add a Revenue column and save it as a CSV next to the original.\n\n  sheet_add_column {\n    path: &quot;/Users/you/sales.xlsx&quot;, sheet: &quot;Sales&quot;,\n    name: &quot;Revenue&quot;, formula: &quot;[Units] * [Unit Price]&quot;,\n    out_path: &quot;/Users/you/sales.csv&quot;\n  }\n\nAssistant: Wrote 400 rows (401 lines with header) to sales.csv.\nRevenue = Units * Unit Price on every row, total 10,142,542.04.\nSource file untouched.\n</code></pre>\n<h3>The <code>where</code> and <code>formula</code> language</h3>\n<p>A small expression language, parsed and evaluated directly. There is no <code>eval</code> and no code execution: a bare word is always a column name, never a JavaScript value.</p>\n<ul>\n<li>Columns: <code>[Unit Price]</code> for names with spaces, <code>Qty</code> otherwise. Lookup is case insensitive.</li>\n<li>Comparisons: <code>=</code> <code>!=</code> <code>&gt;</code> <code>&gt;=</code> <code>&lt;</code> <code>&lt;=</code> <code>contains</code> <code>startswith</code> <code>endswith</code></li>\n<li>Logic: <code>AND</code> <code>OR</code> <code>NOT</code> and parentheses. <code>AND</code> binds tighter than <code>OR</code>.</li>\n<li>Arithmetic in formulas: <code>+</code> <code>-</code> <code>*</code> <code>%</code> <code>/</code> with the usual precedence.</li>\n<li>Strings: <code>&#39;single&#39;</code> or <code>&quot;double&quot;</code> quotes; double a quote to escape it.</li>\n</ul>\n<pre><code>[Qty] &gt;= 5 AND ([Status] = &quot;open&quot; OR [Region] contains &quot;north&quot;)\n[Amount] &gt; 1000 AND NOT [Customer] startswith &#39;Test&#39;\n</code></pre>\n<p>Formula example for <code>sheet_add_column</code>: <code>[Qty] * [Unit Price]</code>. When every column the formula reads holds at most 2 decimals, the result is rounded to 2 decimals, so <code>[Amount] * 1.23</code> on money gives <code>40.79</code> rather than <code>40.7868</code>. Pass <code>decimals</code> (0-10) to choose the precision yourself.</p>\n<p>Numbers written as text in a CSV are converted by pattern, not by length: <code>1250.00</code>, <code>12.00</code> and <code>1,250.00</code> all become numbers in the xlsx output, so Excel&#39;s own <code>SUM</code> counts them. Identifier-shaped and ambiguous values stay text: <code>007</code> keeps its leading zeros and <code>1.250,00</code> is left alone rather than guessed at.</p>\n<p>Text comparisons ignore case and surrounding whitespace. Values like <code>$1,250.00</code>, <code>1 250</code>, and <code>12%</code> compare as numbers, so <code>[Amount] &gt; 1000</code> works on a column your spreadsheet stored as text.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Every tool that reads a file (<code>sheet_info</code>, <code>sheet_read</code>, <code>sheet_query</code>, <code>sheet_stats</code>, <code>sheet_find</code>, <code>sheet_add_column</code>, <code>sheet_convert</code>)</td>\n<td>Files up to 5 MB and 5,000 rows</td>\n<td>No limit (up to the 50 MB file ceiling)</td>\n</tr>\n<tr>\n<td><code>sheet_write</code>, <code>sheet_add_column</code>, <code>sheet_convert</code></td>\n<td>Up to 500 rows written per file; over that nothing is written and the tool says so</td>\n<td>No limit</td>\n</tr>\n<tr>\n<td>Sheets, formats, expression language</td>\n<td>All</td>\n<td>All</td>\n</tr>\n</tbody></table>\n<p>Over a free read limit the tool still does the work and returns the part it is allowed to return (the first 5,000 rows), with a note saying what was left out. Over the free write limit nothing at all is written: a partial file that looks complete is worse than no file, so the tool refuses, tells you the row count and the cap, and suggests a free workaround (filter the rows down first, or write in 500-row batches). Nothing fails silently.</p>\n<h2>Get Pro</h2>\n<p>$19 one-time for this server, $39 for every server, lifetime: <a href=\"https://mcp.zovo.one/buy/spreadsheet\">https://mcp.zovo.one/buy/spreadsheet</a></p>\n<h2>How it stores data</h2>\n<p>This server keeps no database of its own -- it reads and writes the spreadsheet files you point it at,\ndirectly on your disk, and nothing else. Every write (<code>sheet_write</code>, <code>sheet_add_column</code>, <code>sheet_convert</code>\nin <code>overwrite</code> mode) goes to a temporary file in the same directory first, then is renamed into place, so\nan interrupted write leaves either the untouched original or the complete new file, never a truncated\none. Because there is no shared state file, there is no advisory lock to take: two calls writing to two\ndifferent output paths cannot collide, and a call to <code>overwrite</code> the same file twice in a row is simply\ntwo writes in sequence. To back up your data, back up the spreadsheet files themselves -- there is\nnothing else to copy.</p>\n<h2>Limits and honest caveats</h2>\n<ul>\n<li>Free reads cap at 5,000 rows and 5 MB; free writes cap at 500 rows per file and <strong>refuse rather than\ntruncate</strong> -- you get an error naming the row count and the cap, never a shorter file that looks\ncomplete.</li>\n<li>The hard ceiling is 50 MB regardless of tier; a file over that is refused outright with a clear message\nrather than risking memory exhaustion.</li>\n<li>The <code>where</code>/<code>formula</code> language is intentionally small: no regular expressions, no custom functions,\nno cross-sheet references in a single formula. It covers comparisons, boolean logic and arithmetic,\nnothing more.</li>\n<li><strong>Writing an xlsx replaces one sheet, not the workbook.</strong> <code>sheet_write</code> with <code>append</code> or <code>overwrite</code>\nreads the whole workbook, swaps the sheet you named and writes every other sheet back, so\n<code>Sheet2</code> and its data survive an append to <code>Sheet1</code>. What is <em>not</em> preserved is the sheet being\nwritten: it is rebuilt from values, so formulas, cell formatting, conditional formatting, charts,\ndata validation and merged cells on <strong>that one sheet</strong> become plain values. Other sheets keep their\ncells as read. Take a copy first if the target sheet carries formatting you cannot recreate.</li>\n<li><strong>Numbers written as text are read with locale-aware rules.</strong> <code>1,250.00</code>, <code>1 250.00</code>, <code>$1,250.00</code>,\n<code>12,99</code>, <code>1.234,56</code> and <code>EUR 1 250,00</code> all read as numbers; a decimal comma is only accepted in the\nunambiguous shape (a comma with exactly two digits at the end, dots or spaces grouping). Anything\nthat mixes separators another way (<code>1,2500.00</code>) stays text rather than being guessed at. Values\nwith leading zeros (<code>007</code>) and integers too large for exact arithmetic\n(over 9,007,199,254,740,991) stay text so they are never silently altered.</li>\n<li><strong>Dates keep their cell type.</strong> A date cell read from an xlsx stays a date through queries and\nthrough a conversion back to xlsx. In text, CSV and JSON output it is rendered as ISO:\n<code>2026-09-04</code> for a date, <code>2026-09-03T15:30:00</code> when the cell carries a time.</li>\n<li><code>sheet_info</code>&#39;s header-row guess is a heuristic (looks for the first row with lower emptiness and higher\ntext density than the rows above it). It handles a title row and a blank row above the header; it is\nnot proof against every layout, and you can always confirm what it picked before querying.</li>\n</ul>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong><code>npx</code> hangs or fails to find the package</strong>: npm publish for this package is pending. Use the <code>.mcpb</code>\nbundle or the clone-and-build path above until it lands.</li>\n<li><strong>Using the <code>.mcpb</code> bundle</strong>: it installs into Claude Desktop directly; there is no separate config\nstep.</li>\n<li><strong>Using the clone path</strong>: the server binary is <code>servers/spreadsheet/dist/index.js</code> after\n<code>npm run build</code>. Point your client&#39;s <code>command</code> at <code>node</code> with that absolute path as the only argument.</li>\n<li><strong>Node version</strong>: requires Node &gt;= 18. Check with <code>node -v</code>.</li>\n<li><strong>&quot;Path does not exist&quot;</strong>: the message includes the resolved absolute path (with <code>~</code> expanded) -- check\nit against where the file actually lives, especially inside a sandboxed or containerized client.</li>\n<li><strong>A write is refused with a row-count message</strong>: you hit the free 500-row write cap. Filter the data\ndown with <code>sheet_query</code> first, write in batches, or activate Pro.</li>\n<li><strong>Nothing shows up / silent failures</strong>: logs go to stderr only, never stdout. In Claude Desktop check\nSettings -&gt; Developer -&gt; the server&#39;s log file; in Claude Code check the terminal or <code>--mcp-debug</code>.</li>\n</ul>\n<h2>Safety</h2>\n<ul>\n<li>Paths that do not exist are refused with the resolved path in the message; <code>~</code> is expanded.</li>\n<li>Files over 50 MB are refused with a clear message rather than exhausting memory.</li>\n<li><code>sheet_add_column</code> and <code>sheet_convert</code> write to a new file and refuse to clobber an existing one unless you pass <code>out_path</code> yourself.</li>\n<li><code>sheet_write</code> with <code>mode: &quot;new_file&quot;</code> refuses to write over an existing file. Only <code>mode: &quot;overwrite&quot;</code> replaces file contents.</li>\n<li>Output files are written to a temporary name and renamed into place, so an interrupted write cannot truncate a file.</li>\n</ul>\n<h2>Privacy</h2>\n<p>All data stays local. Files are read from and written to your own disk, license keys are verified offline with an embedded public key, and the server makes no network requests at all.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../time-tracker/README.md\">mcp-time-tracker</a> -- export a CSV with <code>export_csv</code>, then query and reshape it here.</li>\n<li><a href=\"../invoice/README.md\">mcp-invoice</a> -- pull line items out of a spreadsheet before turning them into an invoice.</li>\n<li><a href=\"../price-tracker/README.md\">mcp-price-tracker</a> -- analyze exported price history as a sheet.</li>\n<li><a href=\"../office-suite/README.md\">office-suite</a> -- all four servers behind one install, one config entry.</li>\n<li>Guide: <a href=\"https://mcp.zovo.one/guides/read-excel-in-cursor\">Ask questions about an Excel or CSV file from Cursor or Claude</a></li>\n</ul>\n<h2>FAQ</h2>\n<p><strong>Does it handle a spreadsheet with a title row above the headers?</strong>\nYes. <code>sheet_info</code> guesses the header row and reports which row it picked, so an export with a title line\nand a blank line above the real headers opens correctly without you specifying anything.</p>\n<p><strong>Can it group and sum, or does it only filter?</strong>\nIt groups. <code>sheet_query</code> takes <code>group_by</code> plus <code>aggregate</code> with sum, count, avg, min or max, and can sort\nby an aggregate alias, so top-N-by-category questions are a single call.</p>\n<p><strong>Will it overwrite my original file?</strong>\nNo, not unless you explicitly pass an output path that points at the source. <code>sheet_add_column</code> and\n<code>sheet_convert</code> write a new file next to the original by default.</p>\n<p><strong>What happens on the free tier with a file bigger than the limit?</strong>\nReads return the first 5,000 rows with a note naming what was omitted. Writes over 500 rows are refused\noutright rather than producing a truncated file, and the message tells you the row count, the cap and a\nfree way round.</p>\n<p><strong>Is my data sent anywhere?</strong>\nNo. The server runs locally on your machine and reads your files directly. It makes no network requests,\nand it stores nothing of its own beyond the files you ask it to write.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>. Support: <a href=\"mailto:support@zovo.one\">support@zovo.one</a></p>\n"},"invoice":{"title":"MCP Invoice","tagline":"Numbered invoices with tax lines, rendered to a professional PDF.","description":"Stores the issuer profile and clients, allocates invoice numbers that never repeat (INV-YYYY-NNNN), computes per-line rounding, multiple VAT rates and discounts in integer minor units, renders an A4 PDF with pdfkit (multi-page tables, totals, payment details), tracks paid status and reports overdue invoices. Pairs with the time tracker: hours become an invoice in the same chat.","html":"<h1>mcp-invoice</h1>\n<p>Say &quot;make an invoice for Acme, 12 hours at 90 EUR, due in 14 days&quot; and get a real PDF you can send. This MCP server stores your business profile and your clients, allocates a sequential invoice number that is never reused, computes the subtotal, any discount, one tax line per VAT rate and the total in integer minor units, and renders an A4 PDF with your issuer and payment details, a wrapping item table and a proper totals block. It also tracks payments and, on Pro, reports what is overdue and by how many days. Everything is stored in plain JSON files on your own machine; nothing is uploaded anywhere.</p>\n<p><img src=\"../../assets/demo-invoice.gif\" alt=\"invoice demo\"></p>\n<p><strong>Create numbered invoices with tax lines and a real PDF from chat -- no invoicing SaaS required.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-invoice</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>invoice.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;invoice&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-invoice&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add invoice -- npx -y @theluckystrike/mcp-invoice\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;invoice&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-invoice&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/invoice\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/invoice/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>business_set</code></td>\n<td>Store the issuer profile: name, address, email, VAT id, IBAN, bank, logo, default currency, default tax rate, payment terms, invoice prefix. <code>tax_rate</code>, <code>vat_rate</code> and <code>vat</code> are accepted as aliases for <code>default_tax_rate</code>, and any unrecognised field is reported back rather than dropped</td>\n</tr>\n<tr>\n<td><code>client_add</code></td>\n<td>Add or update a client (name, address, email, VAT id)</td>\n</tr>\n<tr>\n<td><code>client_list</code></td>\n<td>List stored clients with their ids</td>\n</tr>\n<tr>\n<td><code>invoice_create</code></td>\n<td>Create an invoice from line items; allocates the next number, computes discount, tax per rate and total. Items may carry a per-line <code>currency</code>, and a mix is refused rather than billed as one currency. If the client is created from a bare name the response says the BILL TO block has no address and how to add one</td>\n</tr>\n<tr>\n<td><code>invoice_from_hours</code></td>\n<td>Shortcut: bill one client for N hours at an hourly rate. <code>target_currency</code> + <code>fx_rates</code> issue the invoice in another currency (you supply the rate); <code>entry_ids</code> come back with the new invoice number so the tracked hours can be closed with the time tracker&#39;s <code>entry_mark_billed</code></td>\n</tr>\n<tr>\n<td><code>invoice_list</code></td>\n<td>List invoices, filtered by status, client and issue-date range</td>\n</tr>\n<tr>\n<td><code>invoice_get</code></td>\n<td>Full stored record for one invoice number</td>\n</tr>\n<tr>\n<td><code>invoice_mark_paid</code></td>\n<td>Record a payment in full or in part; reports the balance due</td>\n</tr>\n<tr>\n<td><code>invoice_pdf</code></td>\n<td>Render the A4 PDF and return the file path. The response calls the file a PDF only when it is one</td>\n</tr>\n<tr>\n<td><code>overdue_report</code></td>\n<td>Unpaid invoices past due, days overdue, outstanding totals per currency. Free</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Show free or Pro mode</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline)</td>\n</tr>\n</tbody></table>\n<p>Resource: <code>invoices://open</code> returns every unpaid or partly paid invoice as JSON.</p>\n<p>Prompt: <code>monthly_invoicing</code> reviews what is unpaid and what is overdue for a month, then drafts the next invoice.</p>\n<h2>What you can say</h2>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;Set up my business: Acme Consulting, EUR, 23% VAT, 14 day terms.&quot;</td>\n<td><code>business_set</code></td>\n</tr>\n<tr>\n<td>&quot;Add a client: Beta Corp, their VAT id is..., email <a href=\"mailto:billing@beta.example\">billing@beta.example</a>.&quot;</td>\n<td><code>client_add</code></td>\n</tr>\n<tr>\n<td>&quot;List my clients.&quot;</td>\n<td><code>client_list</code></td>\n</tr>\n<tr>\n<td>&quot;Invoice Acme for 12 hours at 90 EUR plus 300 EUR setup, 23% VAT, PDF.&quot;</td>\n<td><code>invoice_create</code> + <code>invoice_pdf</code></td>\n</tr>\n<tr>\n<td>&quot;Bill Beta Corp for 5 hours at 120 EUR.&quot;</td>\n<td><code>invoice_from_hours</code></td>\n</tr>\n<tr>\n<td>&quot;Show me every unpaid invoice from August.&quot;</td>\n<td><code>invoice_list</code></td>\n</tr>\n<tr>\n<td>&quot;Pull up invoice INV-2026-0007.&quot;</td>\n<td><code>invoice_get</code></td>\n</tr>\n<tr>\n<td>&quot;Mark INV-2026-0007 as paid in full.&quot;</td>\n<td><code>invoice_mark_paid</code></td>\n</tr>\n<tr>\n<td>&quot;Which invoices are unpaid and overdue?&quot;</td>\n<td><code>overdue_report</code></td>\n</tr>\n<tr>\n<td>&quot;Am I on the free plan or Pro?&quot;</td>\n<td><code>license_status</code></td>\n</tr>\n</tbody></table>\n<h2>Worked example</h2>\n<p>From <code>docs/USER_VALUE_R2.md</code>, the exact request tested end to end and independently checked against the\narithmetic.</p>\n<pre><code>You: Invoice Acme for 12 hours of API work at 90 EUR plus a 300 EUR setup fee,\n23% VAT, due in 14 days, and give me the PDF.\n\n  invoice_create {\n    client: &quot;Acme&quot;, currency: &quot;EUR&quot;, due_days: 14,\n    items: [\n      { description: &quot;API work&quot;,  quantity: 12, unit_price: 90,  tax_rate: 23 },\n      { description: &quot;Setup fee&quot;, quantity: 1,  unit_price: 300, tax_rate: 23 }\n    ]\n  }\n  -&gt; INV-2026-0001, client &quot;Acme&quot; created with no address on file\n\n  invoice_pdf { number: &quot;INV-2026-0001&quot; }\n  -&gt; /Users/you/.../invoice/pdf/INV-2026-0001.pdf\n\nAssistant: Created INV-2026-0001 for Acme.\n  API work    12 h  x  EUR 90.00   =  EUR 1080.00\n  Setup fee    1    x  EUR 300.00  =  EUR 300.00\n  Subtotal                            EUR 1380.00\n  Tax 23% on EUR 1380.00               EUR 317.40\n  Total                                EUR 1697.40\nNote: Acme was created from a bare name with no address. Add one with\nclient_add before you send this. PDF: INV-2026-0001.pdf\n</code></pre>\n<p>The arithmetic: 12 x 90 = 1080.00, plus 300.00 = 1380.00 subtotal; 23% of 1380.00 = 317.40; total\n1697.40 EUR. Every amount on the PDF and in the text response carries its currency code -- nothing is\never shown as a bare number.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Invoices</td>\n<td>3 per calendar month</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>overdue_report</code></td>\n<td>Yes, unlimited</td>\n<td>Yes, unlimited</td>\n</tr>\n<tr>\n<td>PDF</td>\n<td>Carries the line &quot;Generated with mcp-invoice by theluckystrike&quot;</td>\n<td>No branding</td>\n</tr>\n<tr>\n<td>Logo on the PDF</td>\n<td>No</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td>Custom invoice prefix</td>\n<td>No, fixed <code>INV</code></td>\n<td>Yes</td>\n</tr>\n<tr>\n<td>Clients, tax lines, discounts, payments, multi-currency</td>\n<td>Yes</td>\n<td>Yes</td>\n</tr>\n</tbody></table>\n<p>Pro is a one-time $19, or $39 for every server in the collection, lifetime.</p>\n<p><strong>Get Pro: <a href=\"https://mcp.zovo.one/buy/invoice\">https://mcp.zovo.one/buy/invoice</a></strong></p>\n<h2>Numbers and money</h2>\n<p>Invoice numbers are <code>PREFIX-YYYY-NNNN</code>. The counter is persisted per prefix and year and is written before the invoice is stored, so a crash burns a number rather than reusing one; existing numbers are also scanned so a restored data file can never hand back a number that is already on a sent document.</p>\n<p>Every money value printed anywhere - line unit prices, line amounts, subtotal, discount, each tax line, the total and the balance due, in the text response and on the PDF - carries its currency code, for example <code>EUR 1080.00</code>. No amount is ever shown as a bare number.</p>\n<p>All amounts are held as integer minor units. How many make one unit comes from an ISO 4217 table, not a guess: 2 for most currencies, 0 for JPY, KRW, VND, CLP, ISK and the rest of the zero-decimal list, 3 for BHD, IQD, JOD, KWD, LYD, OMR and TND, 4 for CLF and UYW. <code>KWD 1.234</code> is 1234 minor units, not 123. The table is identical to the one in <a href=\"../expense-tracker\">mcp-expense-tracker</a>, because the two servers exchange amounts and a currency that is 3-decimal in one and 2-decimal in the other would rescale money by ten. HUF is 2 decimals: ISO 4217 gives it two minor digits even though it is usually quoted without them. Rounding is per line, then summed: each line&#39;s gross is rounded first, an invoice-level <code>discount_percent</code> is applied and rounded per line, tax is computed and rounded per line and then grouped into one line per rate, and the totals are plain integer sums of those already-rounded values. A printed total can therefore never disagree with the printed lines. Dates are ISO <code>YYYY-MM-DD</code>.</p>\n<h2>How it stores data</h2>\n<p>Business profile, clients, invoices and the number counter live under\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/invoice/</code> as separate JSON files, plus a <code>pdf/</code> subfolder\nholding the rendered PDFs. Every mutating call (<code>business_set</code>, <code>client_add</code>, <code>invoice_create</code>,\n<code>invoice_from_hours</code>, <code>invoice_mark_paid</code>) runs inside <code>locked()</code>, which takes an advisory lock file at\n<code>.../invoice/.lock</code> for the duration of the call -- this is what makes number allocation safe when two\ninvoices are created in the same second, since the counter read, increment and invoice write all happen\nunder one lock. Saves go to a temporary file and are renamed into place. To back up your invoicing data,\ncopy the whole <code>invoice/</code> data directory, including <code>pdf/</code> if you want the rendered files too -- they can\nalways be regenerated from the stored records with <code>invoice_pdf</code>.</p>\n<p>If one of those JSON files is unreadable or not valid JSON, it is never treated as &quot;empty&quot;. The file is\nmoved aside byte-for-byte as <code>&lt;name&gt;.json.corrupt-&lt;timestamp&gt;</code>, a <code>&lt;name&gt;.json.corrupt</code> marker is written,\nand every tool returns <code>data file is corrupt; moved to ...; nothing was written</code> until you restore a good\ncopy and delete the marker. A truncated <code>clients.json</code> can no longer be silently replaced by an empty\nclient list.</p>\n<h2>Limits and honest caveats</h2>\n<ul>\n<li>Free tier allows 3 invoices per calendar month; the counter resets on the 1st. <code>overdue_report</code> and\neverything else (clients, tax lines, discounts, payments, multi-currency) is unrestricted on free.</li>\n<li>Free PDFs carry a small &quot;Generated with mcp-invoice&quot; footer line; Pro removes it and adds a logo.</li>\n<li>Creating an invoice for a client name the server has never seen creates that client with no address --\nthe response says so and names <code>client_add</code> as the fix, but nothing blocks you from sending a PDF with\na bare-name BILL TO block if you ignore the note.</li>\n<li>There is no email-sending, payment-link or accounting-software sync in this server: it produces the PDF\nand the record; getting it to the client is up to you.</li>\n<li>Currency conversion is not performed anywhere -- an invoice&#39;s currency is fixed at creation and every\nline must use amounts already in that currency.</li>\n</ul>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong><code>npx</code> hangs or fails to find the package</strong>: npm publish for this package is pending. Use the <code>.mcpb</code>\nbundle or the clone-and-build path above until it lands.</li>\n<li><strong>Using the <code>.mcpb</code> bundle</strong>: it installs into Claude Desktop directly; there is no separate config\nstep.</li>\n<li><strong>Using the clone path</strong>: the server binary is <code>servers/invoice/dist/index.js</code> after <code>npm run build</code>.\nPoint your client&#39;s <code>command</code> at <code>node</code> with that absolute path as the only argument.</li>\n<li><strong>Node version</strong>: requires Node &gt;= 18. Check with <code>node -v</code>.</li>\n<li><strong>Mixed currencies</strong>: an item may carry its own <code>currency</code>. Every line on one invoice must agree with the invoice currency; a mix is refused and the message names the conversion call to make (<code>expense_to_invoice</code> with <code>target_currency</code> and <code>fx_rates</code>) rather than silently billing a EUR line under a USD heading. With no <code>currency</code> on the invoice, a single agreed item currency becomes the invoice&#39;s.</li>\n<li><strong>No business profile yet</strong>: invoicing is never blocked by it. <code>invoice_create</code> and <code>invoice_from_hours</code>\nissue the document with the placeholder issuer &quot;Your business&quot; and say so in one line; run\n<code>business_set {name, address, vat_id, iban}</code> and render the PDF again to replace it.</li>\n<li><strong>PDF render fails or looks wrong</strong>: <code>invoice_pdf</code> uses <code>pdfkit</code>, a pure-JS renderer with no native\ndependency, so failures are almost always a missing or malformed <code>business_set</code> field (check\n<code>invoice_get</code> first) rather than an environment issue.</li>\n<li><strong>&quot;3 invoices this month&quot; hit unexpectedly</strong>: the free cap is per calendar month across all clients,\nnot per client. <code>invoice_list</code> shows what already counted against it.</li>\n<li><strong>Nothing shows up / silent failures</strong>: logs go to stderr only, never stdout. In Claude Desktop check\nSettings -&gt; Developer -&gt; the server&#39;s log file; in Claude Code check the terminal or <code>--mcp-debug</code>.</li>\n</ul>\n<h2>Privacy</h2>\n<p>All data stays local, in <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/invoice/</code>. There are no network calls: license keys are verified offline with a public key compiled into the package, and PDFs are rendered on your machine.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../time-tracker/README.md\">mcp-time-tracker</a> -- <code>invoice_summary</code> output there maps directly onto <code>invoice_create</code> line items here.</li>\n<li><a href=\"../spreadsheet/README.md\">mcp-spreadsheet</a> -- pull line items or client lists out of a sheet before invoicing.</li>\n<li><a href=\"../price-tracker/README.md\">mcp-price-tracker</a> -- invoice a client for something you tracked the price of.</li>\n<li><a href=\"../office-suite/README.md\">office-suite</a> -- all four servers behind one install, one config entry.</li>\n<li>Guide: <a href=\"https://mcp.zovo.one/guides/invoice-pdf-from-chat\">Create an invoice PDF from a chat message with an MCP server</a></li>\n</ul>\n<h2>FAQ</h2>\n<p><strong>Can I put several VAT rates on one invoice?</strong>\nYes. Tax rate is per line item. The totals block prints one tax line per distinct rate, so a 23% line\nand a 0% reverse-charge line appear separately and the total adds up.</p>\n<p><strong>Is the PDF good enough to send to a client&#39;s accounts department?</strong>\nIt is a single page A4 with issuer and client blocks, dates, a line table with per-line tax, subtotal,\ntax lines, total, and payment details with IBAN and reference. Add the client&#39;s address with <code>client_add</code>\nfirst, otherwise BILL TO shows only the name.</p>\n<p><strong>What is the invoice number format and can I change it?</strong>\n<code>INV-YYYY-NNNN</code>, allocated in sequence and never reused. The prefix is configurable with <code>business_set</code>;\na prefix other than <code>INV</code> is a Pro feature.</p>\n<p><strong>Does anything get uploaded when the PDF is rendered?</strong>\nNo. Rendering is local with <code>pdfkit</code>, and the invoice records live in\n<code>~/.local/share/mcp-servers/invoice/</code>. The server makes no network calls at all.</p>\n<p><strong>How exact is the money arithmetic?</strong>\nAmounts are integer minor units. Each line is rounded once, then lines are summed, so 12 h at 90 EUR\nplus 300 EUR with 23% VAT gives 1380.00 plus 317.40 = 1697.40 with no floating point residue.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n<h2>One business profile for the whole suite</h2>\n<p>Your identity is stored once, at <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/profile/business.json</code>,\nand every server in the suite reads it: the invoice issuer, the docx letterhead, the recurring\nissuer, expense-tracker&#39;s default VAT rate, time-tracker&#39;s and timezone&#39;s home zone, and the\nresume and contract letterheads. Set it once with <code>business_set</code> (invoice or docx) - you never\nrepeat it anywhere else. An email address is only ever taken from that profile or from an explicit\nargument; when none is stored, documents show <code>[add: email]</code> and the tool says so rather than\nletting anyone improvise an address.</p>\n"},"expense-tracker":{"title":"MCP Expense Tracker","tagline":"Receipts, mileage and expenses that turn into invoice lines.","description":"Logs expenses with per-expense currency, VAT split and receipt attachment (path + sha256), auto-categorises merchants by rules, records mileage with country rate tables, summarises by category, project, month or merchant per currency, exports CSV or xlsx, and emits invoice-ready line items (net + tax rate, so nothing is taxed twice) for rebilling a project with optional markup.","html":"<h1>mcp-expense-tracker</h1>\n<p><img src=\"../../assets/demo-expense-tracker.gif\" alt=\"expense-tracker demo\"></p>\n<p>Say &quot;12.30 euros at Adobe, software, billable to Acme&quot; and it is logged, categorised, VAT-split and ready to rebill. This MCP server keeps a local ledger of your business expenses: every amount is held in integer minor units in its own currency, <code>vat_rate</code> splits the gross on the receipt into net and VAT (set it once with <code>expense_settings</code> and every later expense is split without repeating it), merchant rules categorise new expenses on their own, receipts are attached by path and sha256 so an audit can prove the file has not changed, and business trips are priced from a built-in mileage table. Summaries group by category, project, month or merchant, always per currency and never mixed. It exports to CSV, xlsx or JSON, and <code>expense_to_invoice</code> hands the billable expenses of a project to <code>mcp-invoice</code> in exactly the line-item shape <code>invoice_create</code> expects. Everything is stored in a plain JSON file on your own machine; nothing is uploaded anywhere.</p>\n<p><strong>Log receipts and mileage in chat, split the VAT, and rebill them onto an invoice -- no expense SaaS required.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-expense-tracker</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a\nclone+build is the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>expense-tracker.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;expense-tracker&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-expense-tracker&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add expense-tracker -- npx -y @theluckystrike/mcp-expense-tracker\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;expense-tracker&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-expense-tracker&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/expense-tracker\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/expense-tracker/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>expense_add</code></td>\n<td>Log one expense: amount, currency, category, merchant, date, project, note, receipt path, billable flag, VAT rate. An empty category is filled in from the merchant rules. <code>billable</code> defaults to <strong>true when a <code>project</code> is given</strong> and false otherwise, and the response always states which value was used</td>\n</tr>\n<tr>\n<td><code>expense_settings</code></td>\n<td>Read or set the defaults <code>expense_add</code> uses when a call names none: <code>default_vat_rate</code>, <code>default_currency</code></td>\n</tr>\n<tr>\n<td><code>expense_list</code></td>\n<td>List expenses in a date range, filtered by project, category or billable, with totals per currency</td>\n</tr>\n<tr>\n<td><code>expense_update</code></td>\n<td>Change any field of a stored expense by id. <code>rebilled: false</code> clears both the rebilled marker and the invoice number. Amount, currency and <code>vat_rate</code> are refused on an expense already rebilled unless you pass <code>unlink_rebill: true</code>, which clears the rebill link, because the invoice charged something else</td>\n</tr>\n<tr>\n<td><code>expense_delete</code></td>\n<td>Delete one expense by id. The receipt file itself is left alone</td>\n</tr>\n<tr>\n<td><code>receipt_attach</code></td>\n<td>Attach a receipt file to an expense. The file must exist; its path and sha256 are stored</td>\n</tr>\n<tr>\n<td><code>category_rules</code></td>\n<td>Set or list the merchant-to-category rules. A match with no regex metacharacters is a plain substring; one with them is compiled only if it cannot backtrack exponentially, and a pattern like <code>(a+)+</code> is refused</td>\n</tr>\n<tr>\n<td><code>expense_summary</code></td>\n<td>Totals for a range grouped by category, project, month or merchant, with the gross, net and VAT per currency</td>\n</tr>\n<tr>\n<td><code>mileage_add</code></td>\n<td>Log a trip in km or miles and price it from the rate table (or your own rate)</td>\n</tr>\n<tr>\n<td><code>expense_export</code></td>\n<td>Write the range to csv, xlsx or json and return the path. Never writes a partial file</td>\n</tr>\n<tr>\n<td><code>expense_to_invoice</code></td>\n<td>Preview a project&#39;s unbilled billable expenses as <code>invoice_create</code> line items, per currency, with an optional markup and an optional <code>assume_vat_rate</code>. Pass <code>target_currency</code> + <code>fx_rates</code> to fold every currency into one group. When nothing matches it returns <code>count: 0</code>, no <code>fx_note</code>, and the plain reason. Marks nothing as rebilled, ever</td>\n</tr>\n<tr>\n<td><code>expense_mark_rebilled</code></td>\n<td>Mark expenses rebilled once the invoice exists, by ids or by project, date range and <code>currency</code>. <code>invoice_number</code> is required</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Show free or Pro mode</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline)</td>\n</tr>\n</tbody></table>\n<p>Resource: <code>expenses://month</code> returns the current calendar month&#39;s totals by category, per currency.</p>\n<p>Prompt: <code>monthly_close</code> walks the month&#39;s totals, the billable expenses not yet invoiced, and the expenses with no receipt attached.</p>\n<h2>Mileage rates</h2>\n<p>The table holds one flat rate per region. Each row is an approximation with a stated assumption, not a tax calculation:</p>\n<table>\n<thead>\n<tr>\n<th>Region</th>\n<th>Rate</th>\n<th>Unit</th>\n<th>Currency</th>\n<th>What it assumes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>PL</td>\n<td>1.15</td>\n<td>km</td>\n<td>PLN</td>\n<td>A car over 900 cm3. The Polish limit is PLN 0.89/km up to 900 cm3, and there are separate motorcycle and moped rates</td>\n</tr>\n<tr>\n<td>UK</td>\n<td>0.45</td>\n<td>mile</td>\n<td>GBP</td>\n<td>A car, within the first 10,000 business miles of the tax year. HMRC pays a lower rate above that threshold, and different rates for motorcycles and bicycles</td>\n</tr>\n<tr>\n<td>US</td>\n<td>0.70</td>\n<td>mile</td>\n<td>USD</td>\n<td>The IRS business standard rate for one calendar year. The IRS re-issues it every year, and has changed it mid-year</td>\n</tr>\n<tr>\n<td>EU</td>\n<td>0.30</td>\n<td>km</td>\n<td>EUR</td>\n<td>A generic per-kilometre allowance. There is no single EU rate; each member state sets its own</td>\n</tr>\n</tbody></table>\n<p>There are deliberately no year, vehicle or threshold tables behind these numbers: a table that looks authoritative but is a year out of date is worse than one that says what it is. Every <code>mileage_add</code> reply names the rate it used and repeats that caveat, for example <code>(table rate PL 1.15 PLN/km, an approximation; pass rate_per_km for your exact scheme)</code>.</p>\n<p>With no <code>region</code>, miles use the US rate and kilometres the EU rate. <code>rate_per_km</code> overrides the table with your own rate for the unit you passed, which is the supported way to claim an exact scheme (an engine class, a mid-year rate, the band above 10,000 miles). <code>currency</code> is only accepted <strong>together with</strong> <code>rate_per_km</code>: a table rate is quoted in its own currency, and relabelling PLN 1.15/km as EUR 1.15/km would convert nothing and book roughly four times the real cost, so that call is refused.</p>\n<h2>Pairs with the rest of the collection</h2>\n<p><code>expense_to_invoice</code> returns <code>{description, quantity, unit_price, tax_rate}</code> objects, which is exactly the <code>items</code> array <a href=\"../invoice\">mcp-invoice</a> takes. <code>unit_price</code> is the net amount, <code>tax_rate</code> is the VAT rate recorded on the expense, so the invoice recomputes the same tax rather than double-charging it, and the line total comes back to the gross on the receipt. Where rounding the tax a second time cannot reproduce that gross (EUR 0.03 at 23% splits into 0.02 + 0.01, but 0.02 taxed at 23% rounds to 0.00), the <code>unit_price</code> is nudged by the one cent if that lands the invoice exactly, and otherwise the group carries a visible <code>[rounding adjustment ...]</code> line at <code>tax_rate: 0</code>; <code>rounding_adjustment_lines</code> counts them.</p>\n<h3>Mixed currencies</h3>\n<p>One invoice carries one currency, so a week of USD hours, a EUR receipt and a GBP mileage line comes back as three groups. To get a single invoice, supply the target and your own rates:</p>\n<pre><code>expense_to_invoice {project: &quot;Nova&quot;, from: &quot;2026-09-01&quot;, to: &quot;2026-09-07&quot;,\n                    target_currency: &quot;USD&quot;, fx_rates: {&quot;EUR&quot;: 1.08, &quot;GBP&quot;: 1.27}}\n</code></pre>\n<p><code>fx_rates</code> reads as &quot;1 unit of that currency = X units of <code>target_currency</code>&quot;. Every line is converted and one group is returned, and each converted line says so on its own face: <code>... [converted from EUR 12.40 at 1.08]</code>. Nothing here fetches or invents a rate; a currency with no rate is refused by name. Without <code>fx_rates</code>, a mixed range returns the exact call to make instead of leaving you to work it out.</p>\n<p>A stored <code>vat_rate</code> of <code>0</code> is a rate, not a gap: an exempt receipt stays exempt. An expense recorded with <strong>no</strong> rate holds a gross amount, and it is rebilled as-is with <code>tax_rate: 0</code> and <code>tax_rate: 0 (VAT unknown, gross rebilled as-is; pass assume_vat_rate to split)</code> in the description, so a default rate on the invoice cannot tax the receipt twice. The <code>expense_settings</code> default is applied when the expense is <strong>inserted</strong> and never retroactively at rebill time: changing that default later must not rewrite the tax meaning of receipts entered before it existed. To split those older lines anyway, pass <code>assume_vat_rate</code> explicitly on the <code>expense_to_invoice</code> call and the lines are flagged <code>[vat assumed 23%]</code>.</p>\n<p>Nothing is marked rebilled by that call, and there is no option to make it: create the invoice first, then call <code>expense_mark_rebilled</code> with a required <code>invoice_number</code>. Because one invoice carries one currency, the result is grouped per currency, each group carries its own <code>expense_ids</code>, and you pass one group. Marking by ids is the precise route; marking by project and date range additionally <strong>requires</strong> <code>currency</code>, and touches only billable, not-yet-rebilled expenses in it, so invoicing the EUR group cannot mark the PLN one. <a href=\"../time-tracker\">mcp-time-tracker</a> bills the hours on the same project; this server bills what the project cost you.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Logging expenses, mileage and receipts</td>\n<td>Unlimited</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>expense_list</code> and <code>expense_summary</code></td>\n<td>Last 30 days</td>\n<td>Full history</td>\n</tr>\n<tr>\n<td>Projects</td>\n<td>3</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td>Category rules</td>\n<td>5</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td>CSV and JSON export</td>\n<td>Up to 200 rows</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td>xlsx export</td>\n<td>No</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td><code>expense_to_invoice</code></td>\n<td>20 items at a time, <code>markup_percent</code> included</td>\n<td>Unlimited items</td>\n</tr>\n<tr>\n<td>Multi-currency, VAT split, receipt hashing</td>\n<td>Yes</td>\n<td>Yes</td>\n</tr>\n</tbody></table>\n<p>A limit never writes a partial file and never silently truncates: the export is refused with nothing on disk, and the tool says what to narrow.</p>\n<p>Pro is a one-time $19, or $39 for every server in the collection, lifetime.</p>\n<p><strong>Get Pro: <a href=\"https://mcp.zovo.one/buy/expense-tracker\">https://mcp.zovo.one/buy/expense-tracker</a></strong></p>\n<h2>Numbers and money</h2>\n<p>Every amount is an integer number of minor units in the expense&#39;s own currency, and every printed amount carries its currency code, for example <code>EUR 61.50</code>. How many minor units make one unit comes from an ISO 4217 table, not a guess: 2 for most currencies, 0 for JPY, KRW, VND, CLP, ISK and the rest of the zero-decimal list, 3 for BHD, IQD, JOD, KWD, LYD, OMR and TND, 4 for CLF and UYW. So <code>KWD 1.234</code> is 1234 minor units, not 123. The same table is used by <a href=\"../invoice\">mcp-invoice</a>, because the two servers exchange amounts. HUF is 2 decimals here: ISO 4217 gives it two minor digits even though it is usually quoted without them.</p>\n<p>The amount you record is the gross on the receipt. <code>vat_rate</code> splits it by rounding the VAT, not the net: <code>vat = round(gross * rate / (100 + rate))</code>, <code>net = gross - vat</code>, so net plus VAT is always exactly the gross and a half-cent of VAT rounds up instead of disappearing (EUR 0.03 at 23% is net 0.02 plus VAT 0.01). Summaries sum already-rounded per-expense values inside one currency; currencies are never added together and never converted. Mileage money is <code>round(distance * rate)</code> in the rate&#39;s currency. Dates are ISO <code>YYYY-MM-DD</code>.</p>\n<h2>Privacy</h2>\n<p>If <code>data.json</code> is ever unreadable or not valid JSON, it is not treated as &quot;no expenses yet&quot;. The file is moved aside byte-for-byte as <code>data.json.corrupt-&lt;timestamp&gt;</code>, a <code>data.json.corrupt</code> marker is written, and every tool returns <code>data file is corrupt; moved to ...; nothing was written</code> until you restore a good copy and delete the marker -- so a truncated file can never be overwritten by an empty database.</p>\n<p>All data stays local, in <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/expense-tracker/</code>. There are no network calls: receipts are hashed on your machine, exports are written on your machine, and license keys are verified offline with a public key compiled into the package.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n<h2>One business profile for the whole suite</h2>\n<p>Your identity is stored once, at <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/profile/business.json</code>,\nand every server in the suite reads it: the invoice issuer, the docx letterhead, the recurring\nissuer, expense-tracker&#39;s default VAT rate, time-tracker&#39;s and timezone&#39;s home zone, and the\nresume and contract letterheads. Set it once with <code>business_set</code> (invoice or docx) - you never\nrepeat it anywhere else. An email address is only ever taken from that profile or from an explicit\nargument; when none is stored, documents show <code>[add: email]</code> and the tool says so rather than\nletting anyone improvise an address.</p>\n"},"currency":{"title":"MCP Currency Converter","tagline":"ECB reference rates: convert, history, and fx_rates for rebilling.","description":"Fetches and caches the European Central Bank daily and historical reference rates (keyless, free), converts any amount between currencies via EUR cross rates with the rate date stated, returns history tables with min, max and average, and emits the exact fx_rates object the expense tracker needs to rebill a project in one currency, so a model can chain the two without the user supplying rates.","html":"<h1>mcp-currency</h1>\n<p>Ask your assistant what something is worth in another currency and get a real answer with a date on it. It reads the European Central Bank&#39;s daily euro foreign exchange reference rates -- the same published series banks, tax authorities and accountants use -- converts amounts between any of the currencies the ECB quotes, and shows how a pair has moved over time. There is no API key, no account and no rate limit, because the ECB publishes the file openly. Both files are cached on your own machine, so after the first download every answer is instant and the server keeps working on a plane. Every answer states which rate date it used, because ECB rates are published once a day and a Sunday carries Friday&#39;s rate.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n<p><img src=\"../../assets/demo-currency.gif\" alt=\"currency demo\"></p>\n<p><strong>Real ECB exchange rates in your chat -- no API key, cached locally, works offline.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-currency</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>currency.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;currency&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-currency&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add currency -- npx -y @theluckystrike/mcp-currency\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;currency&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-currency&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/currency\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/currency/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>rates_latest</code></td>\n<td>The latest ECB reference rates, re-expressed against any base. Optional <code>quotes</code> to narrow the list.</td>\n</tr>\n<tr>\n<td><code>convert</code></td>\n<td>Convert an amount between two currencies, today or on a past date. States the rate, the rate date and the rounding.</td>\n</tr>\n<tr>\n<td><code>convert_many</code></td>\n<td>One amount into several currencies at once, all off the same rate date.</td>\n</tr>\n<tr>\n<td><code>fx_rates_for</code></td>\n<td>Returns the <code>fx_rates</code> object the expense tracker&#39;s <code>expense_to_invoice</code> takes, plus the rate date.</td>\n</tr>\n<tr>\n<td><code>rate_history</code></td>\n<td>A pair over a window, with min, max, average and the change across it.</td>\n</tr>\n<tr>\n<td><code>rate_on</code></td>\n<td>The rate on one date, falling back to the last published rate on or before it and saying so. Both directions are returned (<code>rate</code> and <code>inverse_rate</code>) and <code>published_direction</code> names which way round the ECB itself quotes the pair, so the reciprocal is never relayed as the published figure.</td>\n</tr>\n<tr>\n<td><code>currencies_list</code></td>\n<td>Every currency the ECB quotes, with its euro rate and its number of decimal places.</td>\n</tr>\n<tr>\n<td><code>cache_status</code></td>\n<td>What is cached, how old it is, when it refreshes next, and where it lives.</td>\n</tr>\n<tr>\n<td><code>license_status</code> / <code>license_activate</code></td>\n<td>Check or activate a Pro key. Verified offline.</td>\n</tr>\n</tbody></table>\n<p>Resource <code>fx://latest</code> exposes the cached rates as JSON. Prompt <code>convert_invoice_lines</code> walks the whole\nexpense-tracker -&gt; currency -&gt; invoice chain in one step.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro ($19 one-time)</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Latest rates, <code>convert</code>, <code>convert_many</code>, <code>fx_rates_for</code></td>\n<td>Yes, unlimited</td>\n<td>Yes, unlimited</td>\n</tr>\n<tr>\n<td>Currencies</td>\n<td>All 30+ the ECB quotes</td>\n<td>All 30+ the ECB quotes</td>\n</tr>\n<tr>\n<td>Rate history</td>\n<td>Up to 90 days per call, back 90 days</td>\n<td>Unlimited windows, back to 1999-01-04</td>\n</tr>\n<tr>\n<td><code>rate_on</code></td>\n<td>Last 90 days</td>\n<td>Any date since 1999-01-04</td>\n</tr>\n<tr>\n<td>Offline cache</td>\n<td>Yes</td>\n<td>Yes</td>\n</tr>\n</tbody></table>\n<p>A refused history window returns the reason and the exact narrower call to make; it never silently truncates\na table and it never returns a transport error.</p>\n<p><strong>Get Pro:</strong> <a href=\"https://mcp.zovo.one/buy/currency\">https://mcp.zovo.one/buy/currency</a> (or $39 for the whole bundle: <a href=\"https://mcp.zovo.one/buy/bundle\">https://mcp.zovo.one/buy/bundle</a>)</p>\n<h2>Pairs with</h2>\n<ul>\n<li><strong><a href=\"../expense-tracker\">expense-tracker</a></strong> -- <code>expense_to_invoice</code> takes <code>target_currency</code> plus an <code>fx_rates</code>\nobject and folds a multi-currency expense set into one invoice currency, but it will not fetch or invent a rate.\n<code>fx_rates_for {target, currencies}</code> here returns exactly that object. &quot;Rebill Nova in USD&quot; then works end to end\nwithout the user typing a single exchange rate.</li>\n<li><strong><a href=\"../invoice\">invoice</a></strong> -- one invoice carries one currency. Convert first, issue second, and put the returned\n<code>invoice_note</code> (&quot;Converted at ECB reference rates of 2026-09-02&quot;) on the document so the client can check it.</li>\n</ul>\n<pre><code>expense_to_invoice {project, from, to}          -&gt; which currencies are actually present\nfx_rates_for {target: &quot;USD&quot;, currencies: [...]} -&gt; {&quot;EUR&quot;: 1.0812, &quot;GBP&quot;: 1.2717}, rate date\nexpense_to_invoice {..., target_currency: &quot;USD&quot;, fx_rates: {...}} -&gt; one currency, every line annotated\ninvoice_create {currency: &quot;USD&quot;, items: [...]}  -&gt; the document\n</code></pre>\n<h2>How the rates work</h2>\n<ul>\n<li>Source: the ECB euro foreign exchange reference rates, <code>eurofxref-daily.xml</code> and <code>eurofxref-hist.xml</code>.</li>\n<li>Published around 16:00 CET on TARGET business days. There is no rate for a weekend, 1 January, Good Friday,\nEaster Monday, 1 May, or 25 and 26 December.</li>\n<li>Asking for a date with no rate returns the last rate published on or before it -- the convention every bank\nuses -- and the answer says which date it landed on and why. A date inside the cached range with no rate is\na weekend or a TARGET holiday and is named as one; a date after the newest day in the cache is reported as\nnot published yet, with the cache&#39;s latest date and whether this call went to the ECB to look for it.</li>\n<li>The ECB quotes everything against the euro, so a USD/PLN rate is a cross rate: the ratio is formed and\nmultiplied at full precision, and the only rounding in the path is the last one, to the target currency&#39;s\nminor units. The answer carries both numbers: <code>rate_exact</code> is the multiplier, <code>rate</code> is the same rate rounded\nto 6 decimals for reading. Recomputing by hand from the 6-decimal <code>rate</code> can differ by a minor unit or two;\nit is far out for pairs whose rate is nowhere near 1 (1,000,000 VND is KWD 11.667, not KWD 12.000).</li>\n<li>Results are rounded to the target currency&#39;s own ISO 4217 minor units: JPY comes back whole, BHD to three places.</li>\n<li>These are reference rates for accounting and reporting. They are not dealing rates; your bank&#39;s rate will differ.</li>\n</ul>\n<h2>Privacy</h2>\n<p>All data stays local. The only network request this server makes is to <code>www.ecb.europa.eu</code> for the two public rate\nfiles, and only when the local copy is older than 6 hours (daily) or 24 hours (history). Nothing about your amounts,\nyour currencies or your machine is sent anywhere. The cache lives in\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/currency/</code>; delete that directory to reset the server. Set\n<code>ECB_BASE_URL</code> to point at your own mirror if outbound access is restricted.</p>\n<p>MIT licensed. Support: <a href=\"mailto:support@zovo.one\">support@zovo.one</a>. Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n"},"timezone":{"title":"MCP Timezone Planner","tagline":"Find meeting slots inside everyone's working hours.","description":"Converts times between IANA zones with correct DST via Intl, resolves cities and countries to zones, finds ranked meeting slots where every participant is inside working hours with a fairness score, computes daily overlap windows, DST change dates and business days, stores contacts with their zones, and writes valid .ics calendar files.","html":"<h1>mcp-timezone</h1>\n<p>Work with clients in other countries without doing time zone arithmetic in your head. Ask &quot;what time is it for\nMaria in Lisbon&quot;, &quot;convert 3pm Warsaw to New York and Bangalore&quot;, or &quot;find an hour next week that works for me,\nmy client in New York and my designer in London&quot; and get an answer you can act on: ranked meeting slots where\neveryone is inside their own working hours, the exact daily overlap, when the clocks change, how many business\ndays a delivery date is, and a calendar file you can send. Saved contacts, working hours and nothing else live\nas plain JSON on your own machine.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n<p><img src=\"../../assets/demo-timezone.gif\" alt=\"timezone demo\"></p>\n<p><strong>Find a meeting time that works for everyone abroad, convert any time between cities, and write the invite -- zero setup, all local.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-timezone</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a\nclone+build is the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>timezone.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;timezone&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-timezone&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add timezone -- npx -y @theluckystrike/mcp-timezone\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;timezone&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-timezone&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above,\nor build from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/timezone\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/timezone/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>now</code></td>\n<td>The current time in any list of places, with the zone abbreviation and UTC offset. With no arguments: this machine&#39;s zone and UTC.</td>\n</tr>\n<tr>\n<td><code>convert_time</code></td>\n<td>Convert a time from one place to any number of others. Reads <code>&quot;2026-09-10 15:00&quot;</code>, an ISO timestamp, or <code>&quot;3pm tomorrow&quot;</code> as wall-clock time in <code>from_zone</code>; a trailing <code>Z</code> or an explicit offset wins over <code>from_zone</code>. Flags a next-day or previous-day result, and states which occurrence it used on a DST fold (<code>gap</code>, <code>fold</code>).</td>\n</tr>\n<tr>\n<td><code>find_meeting_slots</code></td>\n<td>Ranked times where every participant is inside their own working hours, on a 30-minute grid, weekends skipped. Ranked by fairness (see below).</td>\n</tr>\n<tr>\n<td><code>overlap</code></td>\n<td>The daily window when every listed place is at work, in UTC and in each local clock, computed on a real date so DST weeks are honest.</td>\n</tr>\n<tr>\n<td><code>dst_changes</code></td>\n<td>Every clock change in a place for a year: the exact UTC instant, the offset before and after, and the local time either side.</td>\n</tr>\n<tr>\n<td><code>business_days</code></td>\n<td>Business days between two dates in a place, excluding weekends and any holidays you pass. Dates are strict: <code>2026-02-30</code> is refused, never rolled forward.</td>\n</tr>\n<tr>\n<td><code>ics_create</code></td>\n<td>Write a <code>.ics</code> calendar file for one meeting and return the path. Attendees with an email are invited; a name with no email is listed in the description. <code>organizer_email</code> writes the <code>ORGANIZER</code> line.</td>\n</tr>\n<tr>\n<td><code>contacts_set</code></td>\n<td>Remember a client or teammate&#39;s zone and working hours.</td>\n</tr>\n<tr>\n<td><code>contacts_list</code></td>\n<td>Everyone you have saved, their local time now, and whether they are inside working hours.</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Free or Pro, and where to upgrade.</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline).</td>\n</tr>\n</tbody></table>\n<p>Also exposed: the resource <code>tz://contacts</code> (saved contacts and their current local time) and the prompt\n<code>schedule_with</code> (proposes times with saved contacts, then offers to write the invite).</p>\n<h2>What you can say</h2>\n<p>No tool names required.</p>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;What time is it in Warsaw, New York and Bangalore right now?&quot;</td>\n<td><code>now</code></td>\n</tr>\n<tr>\n<td>&quot;Convert 3pm Warsaw on 10 September to New York and India.&quot;</td>\n<td><code>convert_time</code></td>\n</tr>\n<tr>\n<td>&quot;Find an hour next week for me in Warsaw, my client in New York and my designer in London.&quot;</td>\n<td><code>find_meeting_slots</code></td>\n</tr>\n<tr>\n<td>&quot;When are Warsaw and New York both at work?&quot;</td>\n<td><code>overlap</code></td>\n</tr>\n<tr>\n<td>&quot;Do the clocks change in Warsaw before our March call?&quot;</td>\n<td><code>dst_changes</code></td>\n</tr>\n<tr>\n<td>&quot;How many business days from 1 to 30 September, excluding the 15th?&quot;</td>\n<td><code>business_days</code></td>\n</tr>\n<tr>\n<td>&quot;Write me the .ics for Thursday 3pm Warsaw, 45 minutes, invite <a href=\"mailto:maria@acme.com\">maria@acme.com</a>.&quot;</td>\n<td><code>ics_create</code></td>\n</tr>\n<tr>\n<td>&quot;Remember that Maria is in Lisbon and works 10 to 6.&quot;</td>\n<td><code>contacts_set</code></td>\n</tr>\n<tr>\n<td>&quot;Who&#39;s awake right now?&quot;</td>\n<td><code>contacts_list</code></td>\n</tr>\n</tbody></table>\n<h2>Worked example</h2>\n<pre><code>You: Find an hour next week for me in Warsaw, my client in New York and my designer in London.\n\n  find_meeting_slots { participants: [{name:&quot;Me&quot;,zone:&quot;Warsaw&quot;},{name:&quot;Client&quot;,zone:&quot;New York&quot;},\n                                     {name:&quot;Designer&quot;,zone:&quot;London&quot;}], duration_minutes: 60, days: 5 }\n\nAssistant: 15 slots fit all 3 participants. Best first:\n  1. 2026-09-07T13:30:00.000Z  fairness 3.00h\n     Me 15:30-16:30 | Client 09:30-10:30 | Designer 14:30-15:30\n</code></pre>\n<p>Three hours of fairness is not a flaw in the ranking, it is the truth about Warsaw and New York: with 09:00-17:00\ndays on both sides the shared window is only 15:00-17:00 Warsaw, so somebody&#39;s meeting is always far from the\nmiddle of their day. The score says so instead of hiding it. Widen one person&#39;s hours and the score improves.</p>\n<pre><code>You: Write the invite for that one, 45 minutes, and send it to maria@acme.com.\n\n  ics_create { title:&quot;Kickoff with Acme&quot;, start:&quot;2026-09-10 15:00&quot;, zone:&quot;Warsaw&quot;,\n               duration_minutes:45, attendees:[{name:&quot;Maria Nowak&quot;, email:&quot;maria@acme.com&quot;}],\n               organizer_email:&quot;you@yourdomain.com&quot; }\n\nAssistant: Wrote /Users/you/.local/share/mcp-servers/timezone/meeting.ics\nKickoff with Acme: 2026-09-10 15:00 Thu (CEST, UTC+02:00) for 45 min\nDTSTART 2026-09-10T13:00:00.000Z (UTC)\n</code></pre>\n<h2>How the ranking works</h2>\n<p><code>find_meeting_slots</code> scores every candidate by <strong>the worst participant&#39;s</strong> distance from 13:00 local, in hours,\nand sorts ascending. The worst, not the average, on purpose: a slot that is pleasant for two people and 07:00\nfor the third must never outrank one that is 10:00 for everybody. A fairness of 0 would mean the meeting sits\nat midday for all of them; anything under about 2 is comfortable.</p>\n<p>A slot is only offered when the whole meeting -- start to end -- is inside every participant&#39;s working window,\non their own local calendar day. Weekends in the first participant&#39;s zone are skipped. No slot ever starts\nbefore <code>earliest_date</code> -- if you pass a time with it, slots earlier that day are not proposed.</p>\n<p>When nothing fits, the server says so, shows the windows, and then lists the closest times that are\n<strong>outside</strong> somebody&#39;s hours, ranked by the total minutes outside, with each person&#39;s local time and the\nworking hours that would make each one fit. On the free tier a search longer than 5 days is shortened to\n5 days and the answer says so; it is never refused outright.</p>\n<h2>How places are resolved</h2>\n<p>City and country names resolve through a built-in table of 490 entries (300+ cities, every commonly used\ncountry and US state shorthands). Every entry is verified against\n<code>Intl.supportedValuesOf(&quot;timeZone&quot;)</code> at startup; an entry this Node build cannot resolve is dropped with a\nline on stderr rather than silently answering with the wrong zone.</p>\n<ul>\n<li>A country that spans several zones maps to its main business zone: United States -&gt; America/New_York,\nAustralia -&gt; Australia/Sydney, Canada -&gt; America/Toronto, Brazil -&gt; America/Sao_Paulo, Russia -&gt; Europe/Moscow,\nMexico -&gt; America/Mexico_City, Indonesia -&gt; Asia/Jakarta. Name a city when you need a different one.</li>\n<li>IANA ids always work and always win: pass <code>America/Denver</code> and you get exactly that.</li>\n<li><code>UTC+2</code> style offsets resolve to the matching fixed zone (<code>Etc/GMT-2</code> -- the Etc signs are inverted by the\nIANA database, not by this server).</li>\n<li><strong>A fixed abbreviation is an offset, not a place.</strong> <code>EST</code> is <code>Etc/GMT+5</code> (UTC-05:00) all year, <code>PST</code> is\n<code>Etc/GMT+8</code>, <code>CET</code> is <code>Etc/GMT-1</code>, <code>JST</code> is <code>Etc/GMT-9</code>. Mapping them to a DST-observing zone made <code>EST</code>\nmean EDT (UTC-04:00) every summer, an hour wrong for half the year. Each answer carries a note naming the\nplace to pass instead (<code>&quot;New York&quot;</code>, <code>&quot;Los Angeles&quot;</code>, <code>&quot;Paris&quot;</code>). The region shorthands <code>ET</code>, <code>CT</code>, <code>MT</code>,\n<code>PT</code> still name places and keep their clock changes. <code>IST</code> is read as India (UTC+05:30, <code>Asia/Kolkata</code>,\nno daylight saving) and the note says so, because IST also names Irish and Israel time.</li>\n<li>An unknown name is <strong>never</strong> guessed. It comes back as an error with suggestions:\n<code>unknown time zone or place: &quot;Warsawa&quot;. Did you mean: warsaw (Europe/Warsaw)?</code></li>\n</ul>\n<p>DST is not stored here at all. Every offset comes from the ICU data inside your Node build via\n<code>Intl.DateTimeFormat</code>, so the rules stay current as Node updates instead of going stale in a bundled table.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>now</code>, <code>convert_time</code>, <code>overlap</code>, <code>dst_changes</code>, <code>business_days</code></td>\n<td>Unlimited</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>find_meeting_slots</code> participants</td>\n<td>Up to 3</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>find_meeting_slots</code> search window</td>\n<td>Up to 5 days (a longer request is shortened, not refused)</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td>Recurring-slot search (<code>recurring: true</code>)</td>\n<td>--</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td>Saved contacts</td>\n<td>5</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>.ics</code> files</td>\n<td>3 per month</td>\n<td>Unlimited</td>\n</tr>\n</tbody></table>\n<p>Pro is $19 one-time, or $39 for every server in the bundle: <strong><a href=\"https://mcp.zovo.one/buy/timezone\">Get Pro</a></strong>.\nActivation is offline: keys are Ed25519 signatures verified against a public key compiled into the package.</p>\n<h2>How it stores data</h2>\n<p>Contacts and the monthly <code>.ics</code> counter live in one JSON file:\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/timezone/data.json</code>.</p>\n<p>Every write happens under an advisory lock at <code>.../timezone/.lock</code>, held across the whole load-mutate-save\ncycle, so two clients sharing one data dir cannot discard each other&#39;s contacts. The save writes a temporary\nfile and renames it into place, so a crash mid-write leaves either the old file or the new one.</p>\n<p>If <code>data.json</code> is ever unreadable or not valid JSON, the server does <strong>not</strong> treat that as &quot;no contacts yet&quot;.\nIt moves the file aside byte-for-byte as <code>data.json.corrupt-&lt;timestamp&gt;</code>, writes a <code>data.json.corrupt</code> marker,\nand every later call fails with <code>data file is corrupt; moved to ...; nothing was written</code> until you restore a\ngood copy and delete the marker.</p>\n<h2>Dates, times and honest caveats</h2>\n<ul>\n<li><strong>A time with no offset is wall-clock time in <code>from_zone</code></strong>, not UTC. <code>2026-09-10 15:00</code> with\n<code>from_zone: &quot;Warsaw&quot;</code> is 15:00 in Warsaw. A trailing <code>Z</code> or an explicit <code>+05:30</code> is honoured exactly and\n<code>from_zone</code> is then only used for display.</li>\n<li><strong>A calendar date is validated, not normalised.</strong> <code>2026-02-30</code> is refused with the reason (February 2026\nhas 28 days) everywhere a date is read: <code>convert_time</code>, <code>overlap</code>, <code>business_days</code>, <code>ics_create</code> and the\nholiday list. Nothing is ever rolled forward into March.</li>\n<li><strong>A wall time inside a spring-forward gap does not exist, and is refused.</strong> <code>02:30</code> on 2026-03-29 in\nWarsaw comes back as an error naming both valid neighbours (<code>2026-03-29 01:30</code> and <code>2026-03-29 03:30</code>).\nPass <code>gap:&quot;forward&quot;</code> to take the time after the jump or <code>gap:&quot;backward&quot;</code> for the one before it; the answer\nthen says which it used. Guessing silently is how a meeting moves an hour once a year.</li>\n<li><strong>Ambiguous times in the autumn fold return the FIRST occurrence</strong> and say so: <code>02:30</code> on 2026-10-25 in\nWarsaw is <code>00:30Z</code>, the CEST reading. Pass <code>fold:&quot;second&quot;</code> for <code>01:30Z</code>, the CET one. Both answers name\nthe abbreviation and offset they used.</li>\n<li><strong><code>.ics</code> attendees are calendar addresses.</strong> An attendee with an email becomes\n<code>ATTENDEE;CN=&quot;Name&quot;;RSVP=TRUE:mailto:addr</code>, with <code>CN</code> written as a quoted RFC 5545 parameter value. A name\nwith no email is listed in <code>DESCRIPTION</code> instead of being written as an unroutable address. Any CR, LF or\ncontrol character in any field is refused, not escaped away: it is what a content-line injection looks\nlike. <code>organizer_email</code> writes the <code>ORGANIZER</code> line; without it no <code>ORGANIZER</code> is written and the answer\nsays so, because replies then have nowhere to go.</li>\n<li><strong><code>.ics</code> files carry UTC times</strong> (<code>DTSTART:20260910T130000Z</code>) and no <code>VTIMEZONE</code> block. That is deliberate:\na hand-written <code>VTIMEZONE</code> with stale DST rules is the classic way an invite lands an hour off.</li>\n<li><strong>Working hours are the only calendar this server has.</strong> It does not know about your existing meetings,\npublic holidays (pass them to <code>business_days</code> yourself) or anyone&#39;s lunch. <code>business_days</code> says so in\nits own answer when no holiday list is passed, so a &quot;22 business days&quot; figure is never mistaken for a\npublic-holiday-adjusted one.</li>\n<li><strong>Argument sizes are capped</strong>, so a runaway caller cannot turn one call into a hang or a huge reply:\nplace and date strings 100 characters, event titles 200, descriptions 5000, up to 50 zones,\n100 participants, 100 attendees, 400 holidays, <code>days</code> &lt;= 366, <code>duration_minutes</code> &lt;= 1440, and a\n<code>business_days</code> range of at most 3700 days (longer ranges are refused, never silently truncated).</li>\n<li><strong><code>out_path</code> is written where you name it.</strong> <code>ics_create</code> resolves a relative path against the\nprocess working directory and does not confine it to the data directory; an unwritable path returns\na clean <code>EACCES</code> and consumes none of the free monthly quota.</li>\n<li><strong>Weekend skipping uses the first participant&#39;s zone</strong> and the Saturday/Sunday convention. A Friday-Saturday\nweekend is not modelled; set <code>work_start</code>/<code>work_end</code> or read the dates.</li>\n</ul>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong><code>npx</code> hangs or fails to find the package</strong>: npm publish is pending. Use the <code>.mcpb</code> bundle or the\nclone-and-build path above.</li>\n<li><strong>A city is not recognised</strong>: the error lists suggestions. Any IANA id always works, so\n<code>America/Argentina/Cordoba</code> is available even when the city name is not in the table.</li>\n<li><strong>Node version</strong>: requires Node &gt;= 18. Check with <code>node -v</code>. Older builds ship older DST rules.</li>\n<li><strong>Nothing shows up / silent failures</strong>: this server writes to stderr only, never stdout. In Claude Desktop\ncheck Settings -&gt; Developer -&gt; the server&#39;s log; in Claude Code run with <code>--mcp-debug</code>.</li>\n<li><strong>A Pro key isn&#39;t recognized</strong>: run <code>license_status</code>, and confirm <code>MCP_LICENSE_KEY</code> is set in the process the\nclient launches, not just your shell.</li>\n</ul>\n<h2>Privacy</h2>\n<p>All data stays local: contacts live in <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/timezone/data.json</code>.\nThe server makes no network requests, has no telemetry and needs no account. Time zone data comes from the\nICU database already inside Node.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../time-tracker/README.md\">mcp-time-tracker</a> -- track the hours you spend on those clients and report them.</li>\n<li><a href=\"../invoice/README.md\">mcp-invoice</a> -- turn the tracked hours into a numbered PDF invoice for the client abroad.</li>\n<li><a href=\"../office-suite/README.md\">office-suite</a> -- several servers behind one install, one config entry.</li>\n</ul>\n<h2>FAQ</h2>\n<p><strong>Does it handle daylight saving?</strong>\nYes, and it does not store any DST rules of its own. Every offset is read from the ICU data in your Node\nbuild, so Warsaw and New York being 5 hours apart (not 6) between 8 and 29 March 2026 falls out correctly.</p>\n<p><strong>Why is my best slot still awkward?</strong>\nBecause a real overlap can be two hours wide. The fairness score reports the worst person&#39;s distance from\ntheir midday so you can see the cost and decide who absorbs it.</p>\n<p><strong>Can I use half-hour zones?</strong>\nYes. India (+05:30), Nepal (+05:45), Adelaide (+09:30) and Chatham are handled like any other zone; the slot\ngrid is 30 minutes, so a half-hour zone produces :00 and :30 local starts.</p>\n<p><strong>Does it read my calendar?</strong>\nNo. It knows only the working hours you give it. It writes <code>.ics</code> files; it never reads or connects to a\ncalendar service.</p>\n<p><strong>Does it need an internet connection?</strong>\nNo. There are no network calls anywhere, including license activation.</p>\n<h2>License</h2>\n<p>MIT</p>\n<h2>One business profile for the whole suite</h2>\n<p>Your identity is stored once, at <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/profile/business.json</code>,\nand every server in the suite reads it: the invoice issuer, the docx letterhead, the recurring\nissuer, expense-tracker&#39;s default VAT rate, time-tracker&#39;s and timezone&#39;s home zone, and the\nresume and contract letterheads. Set it once with <code>business_set</code> (invoice or docx) - you never\nrepeat it anywhere else. An email address is only ever taken from that profile or from an explicit\nargument; when none is stored, documents show <code>[add: email]</code> and the tool says so rather than\nletting anyone improvise an address.</p>\n"},"docx":{"title":"MCP Docx","tagline":"Real Word documents from chat: proposals, contracts, quotes.","description":"Creates .docx files from structured sections or markdown (headings, lists, tables, bold and italic), reads text back from existing Word files, fills {{placeholders}} in templates while keeping formatting, and generates structured proposals and freelance service agreements from the business profile, with an HTML export for printing.","html":"<h1>mcp-docx</h1>\n<p>Say &quot;write a proposal for Beta Corp, checkout rebuild, 4,500 EUR, three phases&quot; and get a real <code>.docx</code> you can send. This MCP server writes Word documents from chat -- proposals, quotes, service agreements, statements of work and letters -- with your letterhead, headings, bullet and numbered lists and tables. It also turns markdown into <code>.docx</code>, reads an existing <code>.docx</code> back as text and outline, and fills <code>{{placeholders}}</code> in a template you already use, keeping every style, table, header and image of the original. Everything runs locally: no upload, no account, no native dependency.</p>\n<p><img src=\"../../assets/demo-docx.gif\" alt=\"docx demo\"></p>\n<p><strong>Real Word documents from chat -- proposals, contracts and letters, without a template site or an office subscription.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-docx</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>docx.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;docx&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-docx&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add docx -- npx -y @theluckystrike/mcp-docx\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;docx&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-docx&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/docx\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/docx/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>business_set</code></td>\n<td>Store the sender profile printed on every document: name, address, email, VAT id, IBAN, bank, logo, letterhead colour, default currency, tax rate, payment terms. Same field set as <a href=\"../invoice\">mcp-invoice</a>, so one profile serves both</td>\n</tr>\n<tr>\n<td><code>doc_create</code></td>\n<td>Write a <code>.docx</code> from sections: headings, paragraphs, bullet lists, numbered lists and tables. Layouts: <code>plain</code>, <code>letter</code> (sender block, date, addressee) and <code>proposal</code> (letterhead band, cover title)</td>\n</tr>\n<tr>\n<td><code>doc_from_markdown</code></td>\n<td>Markdown to <code>.docx</code>: ATX headings, paragraphs, bullet and numbered lists (indentation kept, up to Word&#39;s nine levels), GFM pipe tables, fenced code blocks as monospace, and <code>**bold**</code> / <code>*italic*</code> / <code>`code`</code> inline</td>\n</tr>\n<tr>\n<td><code>doc_read</code></td>\n<td>Extract text, headings with levels, list items and tables from any existing <code>.docx</code>, in document order. <code>format: &quot;json&quot;</code> returns the block structure</td>\n</tr>\n<tr>\n<td><code>doc_to_html</code></td>\n<td>Convert a <code>.docx</code> to semantic HTML with a print stylesheet. This is the PDF route -- see the note below</td>\n</tr>\n<tr>\n<td><code>doc_fill_template</code></td>\n<td>Replace <code>{{placeholders}}</code> in a <code>.docx</code> and write a new file, keeping every style, table, header, footer and image. Called with no <code>values</code>, it lists the placeholders the template contains</td>\n</tr>\n<tr>\n<td><code>proposal_create</code></td>\n<td>A client-ready proposal: summary, scope, deliverables, timeline table, price, terms, validity and signature block, with a reference number that is never reused</td>\n</tr>\n<tr>\n<td><code>proposal_update</code></td>\n<td>Rewrite an existing proposal in place from its reference: only the fields you pass change, the rest comes from the structured data stored with the document, and the file and reference number stay the same</td>\n</tr>\n<tr>\n<td><code>contract_create</code></td>\n<td>A plain freelance service agreement skeleton: parties, services, term, fee, IP, confidentiality, contractor status, termination, liability, governing law. A template with labelled placeholders for a lawyer, not legal advice</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Show free or Pro mode</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline)</td>\n</tr>\n</tbody></table>\n<p>Resource: <code>docs://recent</code> returns the last 25 documents written, newest first, with kind, client, reference and path.\nPrompt: <code>write_proposal_from_hours</code> turns tracked hours (or an <code>invoice_summary</code> from <a href=\"../time-tracker\">mcp-time-tracker</a>) into a priced proposal.</p>\n<h2>There is no <code>doc_to_pdf</code>, and that is deliberate</h2>\n<p>Every pure-JavaScript path from Word to PDF needs either a native dependency (LibreOffice, a Chromium binary, a C++ rendering library) or a cloud API. This collection ships neither, so <code>npx</code> works on any machine with Node and nothing else. <code>doc_to_html</code> writes semantic HTML with a print stylesheet instead: open it, print to PDF, and the result is what you would have got. The tool description and the tool&#39;s own answer say the same thing, so the model does not promise a PDF it cannot produce.</p>\n<h2>What you can say</h2>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;Set up my letterhead: Acme Consulting, 1 Road Warsaw, VAT PL1234567890.&quot;</td>\n<td><code>business_set</code></td>\n</tr>\n<tr>\n<td>&quot;Write a proposal for Beta Corp: checkout rebuild, 4,500 EUR, 50/50, three phases.&quot;</td>\n<td><code>proposal_create</code></td>\n</tr>\n<tr>\n<td>&quot;Draft a service agreement with Beta Corp, 3,000 EUR monthly, starting October.&quot;</td>\n<td><code>contract_create</code></td>\n</tr>\n<tr>\n<td>&quot;Turn these meeting notes into a Word document.&quot;</td>\n<td><code>doc_from_markdown</code></td>\n</tr>\n<tr>\n<td>&quot;Write a letter to Beta Corp with an agenda for Tuesday.&quot;</td>\n<td><code>doc_create</code> with <code>style: &quot;letter&quot;</code></td>\n</tr>\n<tr>\n<td>&quot;What does this proposal.docx actually say?&quot;</td>\n<td><code>doc_read</code></td>\n</tr>\n<tr>\n<td>&quot;Fill my NDA template with this client&#39;s details.&quot;</td>\n<td><code>doc_fill_template</code></td>\n</tr>\n<tr>\n<td>&quot;Give me a printable version of that document.&quot;</td>\n<td><code>doc_to_html</code></td>\n</tr>\n</tbody></table>\n<h2>Worked example</h2>\n<pre><code>You: Write a proposal for Beta Corp. Checkout rebuild, 4,500 EUR, 50% on\nsignature 50% on delivery, three phases: discovery 1 week, build 3 weeks,\nlaunch 1 week. Valid until the end of the year.\n\n  proposal_create {\n    client: &quot;Beta Corp&quot;, project_title: &quot;Checkout rebuild&quot;,\n    summary: &quot;Beta Corp loses orders at checkout. This project rebuilds it.&quot;,\n    scope: [&quot;Audit the current funnel&quot;, &quot;Rebuild the checkout&quot;, &quot;Ship and measure&quot;],\n    deliverables: [&quot;New checkout in production&quot;, &quot;A one-page handover&quot;],\n    timeline: [{phase: &quot;Discovery&quot;, duration: &quot;1 week&quot;},\n               {phase: &quot;Build&quot;, duration: &quot;3 weeks&quot;},\n               {phase: &quot;Launch&quot;, duration: &quot;1 week&quot;}],\n    price: {amount: 4500, currency: &quot;EUR&quot;, terms: &quot;50% on signature, 50% on delivery&quot;},\n    valid_until: &quot;2026-12-31&quot;\n  }\n  -&gt; PROP-2026-0001, EUR 4,500.00\n  -&gt; ~/.local/share/mcp-servers/docx/documents/checkout-rebuild.docx\n</code></pre>\n<p>The file opens in Word, Pages, LibreOffice and Google Docs: letterhead, cover title, &quot;Prepared for Beta Corp&quot;,\nSummary, Scope of work, Deliverables, a Timeline table, an Investment table with <code>EUR 4,500.00</code>, the payment\nterms, and a signature block for both parties. Every amount carries its currency code -- no bare numbers.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>doc_create</code>, <code>doc_from_markdown</code>, <code>doc_read</code>, <code>doc_to_html</code></td>\n<td>Unlimited</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>proposal_create</code>, <code>contract_create</code></td>\n<td>3 per calendar month, combined</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>doc_fill_template</code></td>\n<td>Templates with up to 10 placeholders</td>\n<td>Any template</td>\n</tr>\n<tr>\n<td>Footer</td>\n<td>Carries &quot;Generated with mcp-docx by theluckystrike&quot;</td>\n<td>No branding</td>\n</tr>\n<tr>\n<td>Letterhead logo and colour</td>\n<td>Default colour, no logo</td>\n<td>Your <code>logo_path</code> and <code>brand_color</code></td>\n</tr>\n<tr>\n<td>Tables, lists, letter and proposal layouts, reference numbering</td>\n<td>Yes</td>\n<td>Yes</td>\n</tr>\n</tbody></table>\n<p>Pro is a one-time $19, or $39 for every server in the collection, lifetime.</p>\n<p><strong>Get Pro: <a href=\"https://mcp.zovo.one/buy/docx\">https://mcp.zovo.one/buy/docx</a></strong></p>\n<h2>How the .docx is produced and read</h2>\n<p>Documents are written with <a href=\"https://www.npmjs.com/package/docx\"><code>docx</code></a>, a pure-JavaScript OOXML writer -- no native module, no headless browser, no office install.</p>\n<p>Reading and template filling use no dependency at all. A <code>.docx</code> is a ZIP, so <code>node:zlib</code> opens it and a small WordprocessingML walk pulls out paragraphs, heading levels (from <code>w:pStyle</code>), list items and tables in document order. Numbered lists are told apart from bullets by resolving each paragraph&#39;s <code>w:numId</code> against <code>word/numbering.xml</code>, which is the only place that distinction is recorded -- without it every numbered list reads back as bullets.</p>\n<p>Template filling substitutes on the <strong>joined text of each paragraph</strong>, not per run. Word routinely breaks a placeholder you typed as <code>{{client}}</code> into three runs (<code>{{cli</code>, <code>ent}}</code>, ...) after an edit or a spell-check pass, and per-run replacement silently misses those -- the document comes back with the placeholder still in it. The replaced text goes into the first run, keeping its formatting, and the remaining runs of that paragraph are blanked. Every other part of the package is copied byte-for-byte, so styles, images, headers, footers and section setup survive. A placeholder with no value is left in place and reported, never blanked.</p>\n<h2>Existing files are never overwritten</h2>\n<p>Every tool that writes a file (<code>doc_create</code>, <code>doc_from_markdown</code>, <code>doc_to_html</code>, <code>doc_fill_template</code>,\n<code>proposal_create</code>, <code>contract_create</code>) refuses an <code>out_path</code> that already exists and tells you so:</p>\n<pre><code>Error: /path/acme-proposal.docx already exists and nothing was written.\nPass overwrite: true to replace it, or give a different out_path.\n</code></pre>\n<p>The path is reserved with an exclusive create, not an existence check, so two processes writing the same\n<code>out_path</code> at the same time cannot clobber each other: one wins, the other is refused and writes nothing.</p>\n<p>When you do not pass an <code>out_path</code>, the file name is derived from the title, and a derived path never\nlands on an earlier document: a second proposal with the same title is written as <code>...-2.docx</code>. To change\na proposal you already sent, use <code>proposal_update {reference}</code> instead: it rewrites the same file from the\nstored structured data and keeps the reference number.</p>\n<p>The check runs before anything is built or written, so a refused call leaves the disk untouched and burns\nno reference number. Pass <code>overwrite: true</code> when replacing the file is what you want.</p>\n<h2>Characters Word cannot carry</h2>\n<p>XML 1.0 allows TAB, LF and CR but no other control code. Every string that reaches <code>word/document.xml</code>\nis cleaned first: control codes and unpaired surrogates are removed, literal <code>\\n</code> escapes become real\nparagraph breaks, and stray whitespace collapses. When something was removed the tool says so in its\nanswer instead of handing you a file Word would offer to repair.</p>\n<h2>How it stores data</h2>\n<p>The business profile, the document register and the reference counter live under\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/docx/</code> as plain JSON, plus a <code>documents/</code> subfolder holding the\ngenerated files when you do not pass <code>out_path</code>. Every mutating call runs inside an advisory lock on\n<code>.../docx/.lock</code>, so two clients on one data directory cannot allocate the same reference number or lose a\nrecord. Saves go to a temporary file and are renamed into place.</p>\n<p>If one of those JSON files is unreadable or not valid JSON, it is never treated as &quot;empty&quot;. The file is moved\naside byte-for-byte as <code>&lt;name&gt;.json.corrupt-&lt;timestamp&gt;</code>, a <code>&lt;name&gt;.json.corrupt</code> marker is written, and every\ntool returns <code>data file is corrupt; moved to ...; nothing was written</code> until you restore a good copy and delete\nthe marker.</p>\n<p>Reference numbers are <code>PROP-YYYY-NNNN</code> for proposals and <code>AGR-YYYY-NNNN</code> for agreements. The counter is written\nbefore the record is stored, so a crash burns a number rather than reusing one, and existing numbers are scanned\nso a restored register can never hand back a reference that is already on a sent document.</p>\n<h2>Limits and honest caveats</h2>\n<ul>\n<li>No PDF output. Use <code>doc_to_html</code> and print. See the section above for why.</li>\n<li><code>doc_read</code> reads <code>.docx</code> only. Legacy <code>.doc</code>, <code>.rtf</code> and Pages files are refused with a message that says so.</li>\n<li><code>doc_read</code> extracts text structure: headings, paragraphs, lists and tables. It does not report fonts, colours,\ncomments, tracked changes, footnotes or embedded images.</li>\n<li><code>doc_fill_template</code> keeps the first run&#39;s formatting for the whole paragraph it rewrites. A paragraph that mixes\nbold and regular text around a placeholder comes back in the first run&#39;s formatting.</li>\n<li><code>contract_create</code> writes a drafting skeleton with <code>[BRACKETED PLACEHOLDERS]</code> and says on the document itself that\nit is a template and not legal advice. Nothing here has been reviewed by a lawyer in any jurisdiction.</li>\n<li>The free-tier limit of 3 counts proposals and contracts together, per calendar month, and resets on the 1st.\nEverything else stays unlimited when it closes.</li>\n</ul>\n<h2>Privacy</h2>\n<p>All data stays local. The server reads and writes files on your machine, stores its register under your data\ndirectory, and makes no network request of any kind -- not for licensing (keys are verified offline), not for\nfonts, not for telemetry.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../invoice/README.md\">mcp-invoice</a> -- same <code>business_set</code> profile shape; the proposal you accepted becomes the invoice you send.</li>\n<li><a href=\"../time-tracker/README.md\">mcp-time-tracker</a> -- the <code>write_proposal_from_hours</code> prompt turns <code>invoice_summary</code> hours into a priced proposal.</li>\n<li><a href=\"../expense-tracker/README.md\">mcp-expense-tracker</a> -- quote a project with its pass-through costs already counted.</li>\n<li><a href=\"../office-suite/README.md\">office-suite</a> -- several servers behind one install, one config entry.</li>\n</ul>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong><code>npx</code> hangs or fails to find the package</strong>: npm publish for this package is pending. Use the <code>.mcpb</code> bundle or\nthe clone-and-build path above until it lands.</li>\n<li><strong>&quot;Wrote ... .docx&quot; but Word will not open it</strong>: check the path is not inside a synced folder that was still\nuploading. The file is complete when the tool returns; nothing is written incrementally.</li>\n<li><strong>A placeholder did not get replaced</strong>: call <code>doc_fill_template</code> with no <code>values</code> to list what the template\nactually contains. Names are matched exactly, whitespace inside <code>{{ }}</code> is ignored, and the response names every\nkey you passed that the template does not have.</li>\n<li><strong>The letterhead shows &quot;Your business&quot;</strong>: run <code>business_set</code> once. Documents are never blocked by a missing\nprofile; the response says the sender block is a placeholder.</li>\n<li><strong>No logo on the document</strong>: the logo is a Pro feature, and <code>logo_path</code> must exist and be a PNG, JPG or GIF.</li>\n<li><strong>Node version</strong>: requires Node &gt;= 18. Check with <code>node -v</code>.</li>\n</ul>\n<p>MIT licensed. Support: <a href=\"mailto:support@zovo.one\">support@zovo.one</a></p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n<h2>One business profile for the whole suite</h2>\n<p>Your identity is stored once, at <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/profile/business.json</code>,\nand every server in the suite reads it: the invoice issuer, the docx letterhead, the recurring\nissuer, expense-tracker&#39;s default VAT rate, time-tracker&#39;s and timezone&#39;s home zone, and the\nresume and contract letterheads. Set it once with <code>business_set</code> (invoice or docx) - you never\nrepeat it anywhere else. An email address is only ever taken from that profile or from an explicit\nargument; when none is stored, documents show <code>[add: email]</code> and the tool says so rather than\nletting anyone improvise an address.</p>\n"},"resume":{"title":"MCP Resume and Cover Letter","tagline":"Resumes and cover letters as Word files from one profile, never invented.","description":"Stores a structured profile (experience, education, skills, links), writes resumes in three styles trimmed to a page budget with keyword highlighting, drafts one-page cover letters whose every claim traces to the profile with bracketed prompts for anything unknown, runs a gap analysis against a job description with suggested bullet rewrites from existing facts, reads an existing resume .docx back into the profile shape, and exports markdown and printable HTML. Built on the docx engine.","html":"<h1>mcp-resume</h1>\n<p>Store your CV facts once. Then say &quot;tailor my resume to this posting and write the cover letter&quot; and get two real\n<code>.docx</code> files: a resume whose bullets are reordered by relevance to the posting, trimmed to fit the page count you\nasked for, with the posting&#39;s keywords in bold where you actually have them -- and a one-page cover letter that\nstates nothing you did not put in your profile. Where a fact is missing, the letter leaves a bracketed prompt like\n<code>[add: metric]</code> instead of inventing a number. It also reads an existing resume <code>.docx</code> back into the profile shape,\nexports markdown for an ATS box, and writes printable HTML. Everything runs locally: no upload, no account, no\nnative dependency.</p>\n<p><img src=\"../../assets/demo-resume.gif\" alt=\"resume demo\"></p>\n<p><strong>A resume that fits the page and a cover letter that cannot lie about you.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-resume</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>resume.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;resume&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-resume&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add resume -- npx -y @theluckystrike/mcp-resume\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;resume&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-resume&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/docx -w servers/resume\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/resume/dist/index.js</code>.\n<code>servers/docx</code> is in that build list because this server shares the document engine with\n<a href=\"../docx\">mcp-docx</a> rather than carrying a second copy of it.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>profile_set</code></td>\n<td>Store the facts every output is built from: name, email, phone, location, links, summary, skills, roles with bullets, education, certifications, languages. One profile per data directory; Pro adds named variants</td>\n</tr>\n<tr>\n<td><code>profile_get</code></td>\n<td>Return the stored profile as JSON, exactly as the other tools see it</td>\n</tr>\n<tr>\n<td><code>resume_create</code></td>\n<td>Write the resume as <code>.docx</code>. Bullets are ranked by keyword hits and recency, then trimmed to <code>max_pages</code> against a measured word budget; matched keywords are bolded, missing ones are reported and never added. Styles: <code>modern</code>, <code>classic</code>, <code>compact</code></td>\n</tr>\n<tr>\n<td><code>cover_letter_create</code></td>\n<td>A one-page letter in four parts -- opening, fit, proof, close -- drawn only from the profile. Tones: <code>formal</code>, <code>direct</code>, <code>warm</code>. Unknowns come back as <code>[add: ...]</code> prompts</td>\n</tr>\n<tr>\n<td><code>tailor_to_job</code></td>\n<td>Gap analysis against a posting: the keywords it actually asks for, which of them your profile already covers, which it does not, a coverage figure, and rewrites that only reorder facts you already stated</td>\n</tr>\n<tr>\n<td><code>resume_read</code></td>\n<td>Read an existing resume <code>.docx</code> back into the profile shape, section by heading. Best effort; nothing is saved unless <code>save: true</code></td>\n</tr>\n<tr>\n<td><code>resume_to_markdown</code></td>\n<td>The resume as markdown, for an application form or an ATS box</td>\n</tr>\n<tr>\n<td><code>resume_to_html</code></td>\n<td>Printable HTML with a print stylesheet. Open it and print to PDF</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Show free or Pro mode</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline)</td>\n</tr>\n</tbody></table>\n<p>Resource: <code>resume://profile</code> returns the stored profile as JSON.\nPrompt: <code>apply_to_job</code> chains <code>tailor_to_job</code>, <code>resume_create</code> and <code>cover_letter_create</code> against one posting, and\nmakes the model report the gaps back to you instead of filling them in.</p>\n<h2>The cover letter does not invent facts</h2>\n<p>This is the part that matters, so it is enforced rather than requested:</p>\n<ul>\n<li>Every proof line in the letter is a <strong>verbatim bullet from your profile</strong>. The letter never paraphrases a\nbullet into a stronger claim.</li>\n<li>A <code>highlights</code> entry you pass is checked against the profile first. If the profile does not support it, it is\nprinted as <code>[add: &quot;...&quot; is not in your profile - add it there or drop it]</code>, not as a claim.</li>\n<li>Before the file is written, every digit run in the letter is checked against your profile and the arguments you\npassed. A number that traces to neither is a refusal, not a warning -- the tool returns an error and writes nothing.\nThe job description is deliberately <strong>not</strong> an allowed source: the employer&#39;s revenue, headcount and throughput\nfigures are theirs, and the letter will never restate one as yours. Comparison is on whole numbers, so a profile\nholding <code>2012</code> does not license a letter claiming <code>12</code>.</li>\n<li>Each proof bullet is printed under the role it actually belongs to. A letter that quotes work from two employers\nprints two headings; work from an earlier job is never listed under your current one.</li>\n<li>A bullet that carries no figure gets <code> [add: metric]</code> appended, so the letter asks you for the result instead of\nguessing one.</li>\n<li>Missing keywords from <code>tailor_to_job</code> never become skills. The response says so in plain words.</li>\n</ul>\n<h2>What you can say</h2>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;Here is my CV: Ada Rowe, backend engineer, Acme Pay since 2021, ...&quot;</td>\n<td><code>profile_set</code></td>\n</tr>\n<tr>\n<td>&quot;Read my old resume at ~/Documents/cv.docx and set up my profile.&quot;</td>\n<td><code>resume_read</code></td>\n</tr>\n<tr>\n<td>&quot;How well do I match this posting?&quot;</td>\n<td><code>tailor_to_job</code></td>\n</tr>\n<tr>\n<td>&quot;Make me a one-page resume for this Staff Engineer role.&quot;</td>\n<td><code>resume_create</code></td>\n</tr>\n<tr>\n<td>&quot;Write the cover letter for Zeta Systems, direct tone.&quot;</td>\n<td><code>cover_letter_create</code></td>\n</tr>\n<tr>\n<td>&quot;Give me the plain-text version for the application form.&quot;</td>\n<td><code>resume_to_markdown</code></td>\n</tr>\n<tr>\n<td>&quot;I need a PDF to attach.&quot;</td>\n<td><code>resume_to_html</code>, then print to PDF</td>\n</tr>\n<tr>\n<td>&quot;Apply me to this job.&quot;</td>\n<td>the <code>apply_to_job</code> prompt</td>\n</tr>\n</tbody></table>\n<h2>Worked example</h2>\n<pre><code>You: Tailor my resume to this posting and write the letter. One page.\n\n  tailor_to_job { job_description: &quot;...payments platform, PostgreSQL, Kubernetes...&quot; }\n  -&gt; matched: payments, postgresql, kubernetes   missing: rust\n  -&gt; coverage 75%\n\n  resume_create { style: &quot;modern&quot;, target_role: &quot;Staff Engineer&quot;,\n                  keywords: [&quot;postgresql&quot;, &quot;kubernetes&quot;, &quot;rust&quot;], max_pages: 1 }\n  -&gt; estimated_pages 1, words_used 402 of a 384-word budget for bullets\n  -&gt; bullets_dropped: [&quot;Wrote the deployment pipeline&quot;]\n  -&gt; keywords_missing: [&quot;rust&quot;]  (not added anywhere)\n  -&gt; ~/.local/share/mcp-servers/resume/documents/ada-rowe-resume.docx\n\n  cover_letter_create { company: &quot;Zeta Systems&quot;, role: &quot;Staff Engineer&quot;, tone: &quot;formal&quot; }\n  -&gt; fills_required: [&quot;[add: metric]&quot;]\n  -&gt; ~/.local/share/mcp-servers/resume/documents/zeta-systems-staff-engineer-cover-letter.docx\n</code></pre>\n<h2>How page trimming works</h2>\n<p>There is no page-layout engine in pure JavaScript, so this server does not pretend to measure one. It uses a word\nbudget instead: a full A4 page at 11pt Calibri with 2cm margins holds about 520 words of body text, and a resume\nspends roughly a seventh of that page on headings, blank lines and the contact block. The net figure is <strong>450 words\nper page</strong> (540 for the <code>compact</code> style). The contact block, summary, skills, role headers, education,\ncertifications and languages are counted first; whatever budget is left goes to experience bullets, highest score\nfirst. Every role keeps its first bullet before any role gets a second one, so trimming never leaves a job on the\npage with nothing under it. The response reports the budget, the words used, the estimated page count and every\nbullet it dropped, so you can see the decision rather than discover it in Word.</p>\n<p>Recency is part of that score, and it is read off array order, not off <code>start</code>/<code>end</code> text. <code>profile_set</code> enforces\nthe ordering that makes that safe: however you list roles when you call it, the stored profile always ends up\nnewest-first -- an open role with no <code>end</code> first, then by <code>end</code> descending, then by <code>start</code> descending. Enter roles\nin any order; the stored order (and the cover-letter bullet ranking in <code>cover_letter_create</code>) is always correct.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro ($19 one-time)</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Profile, <code>resume_create</code>, <code>resume_to_markdown</code>, <code>resume_to_html</code></td>\n<td>Yes, unlimited</td>\n<td>Yes, unlimited</td>\n</tr>\n<tr>\n<td>Resume styles</td>\n<td><code>modern</code></td>\n<td><code>modern</code>, <code>classic</code>, <code>compact</code></td>\n</tr>\n<tr>\n<td>Cover letters</td>\n<td>3 per calendar month</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>tailor_to_job</code></td>\n<td>Postings up to 2,000 characters</td>\n<td>Any length</td>\n</tr>\n<tr>\n<td>Profile variants</td>\n<td>One profile</td>\n<td>Named variants (<code>backend</code>, <code>management</code>, ...)</td>\n</tr>\n<tr>\n<td>Letterhead colour</td>\n<td>Default</td>\n<td>Your own <code>accent_color</code></td>\n</tr>\n<tr>\n<td>Footer credit</td>\n<td>&quot;Generated with mcp-docx by theluckystrike&quot;</td>\n<td>Removed</td>\n</tr>\n</tbody></table>\n<p><strong>Get Pro:</strong> <a href=\"https://mcp.zovo.one/buy/resume\">https://mcp.zovo.one/buy/resume</a> ($19 one-time, or $39 for the whole bundle).</p>\n<h2>Privacy</h2>\n<p>All data stays local. Your CV never leaves the machine: the server reads and writes files on your computer, stores\nthe profile under your data directory, and makes no network request of any kind -- not for licensing (keys are\nverified offline), not for fonts, not for telemetry.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../docx/README.md\">mcp-docx</a> -- this server imports its document engine (<code>@theluckystrike/mcp-docx/lib</code>); install docx too when you also want proposals, contracts and markdown-to-Word.</li>\n<li><a href=\"../office-suite/README.md\">office-suite</a> -- several servers behind one install, one config entry.</li>\n</ul>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong><code>npx</code> hangs or fails to find the package</strong>: npm publish for this package is pending. Use the <code>.mcpb</code> bundle or\nthe clone-and-build path above until it lands.</li>\n<li><strong>&quot;no profile stored&quot;</strong>: run <code>profile_set</code> once, or <code>resume_read {path, save: true}</code> from an existing resume.</li>\n<li><strong>The resume dropped a bullet I wanted</strong>: raise <code>max_pages</code>, or pass the posting&#39;s words in <code>keywords</code> so the\nbullet outranks the others. <code>bullets_dropped</code> in the response names every one that did not fit.</li>\n<li><strong>Short skill names</strong>: <code>tailor_to_job</code> keeps two- and one-character skills -- <code>Go</code>, <code>C</code>, <code>R</code>, <code>C#</code>, <code>C++</code>, <code>F#</code>,\n<code>Qt</code>, <code>UI</code>, <code>UX</code>, <code>QA</code>, <code>ML</code>, <code>AI</code>, <code>AR</code>, <code>VR</code>, <code>JS</code>, <code>TS</code>, <code>K8s</code>, <code>AWS</code>, <code>GCP</code>, <code>SQL</code>, <code>iOS</code> -- plus any short\nword that appears in your own <code>skills</code> list, and ranks a known skill above a longer word of the same frequency.</li>\n<li><strong>A keyword I have is reported missing</strong>: matching is on word boundaries, so <code>go</code> does not match <code>Google</code> and\n<code>k8s</code> does not match <code>Kubernetes</code>. Add the exact word to your skills if it is true.</li>\n<li><strong><code>resume_read</code> put a role in <code>unparsed</code></strong>: resumes have no schema. Nothing is dropped silently -- fix the fields\nand pass them to <code>profile_set</code>.</li>\n<li><strong>There is no <code>resume_to_pdf</code></strong>: every pure-JavaScript route from Word to PDF needs a native dependency or a\ncloud API. <code>resume_to_html</code> writes semantic HTML with a print stylesheet; print that to PDF.</li>\n<li><strong>Node version</strong>: requires Node &gt;= 18. Check with <code>node -v</code>.</li>\n</ul>\n<p>MIT licensed. Support: <a href=\"mailto:support@zovo.one\">support@zovo.one</a></p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n<h2>One business profile for the whole suite</h2>\n<p>Your identity is stored once, at <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/profile/business.json</code>,\nand every server in the suite reads it: the invoice issuer, the docx letterhead, the recurring\nissuer, expense-tracker&#39;s default VAT rate, time-tracker&#39;s and timezone&#39;s home zone, and the\nresume and contract letterheads. Set it once with <code>business_set</code> (invoice or docx) - you never\nrepeat it anywhere else. An email address is only ever taken from that profile or from an explicit\nargument; when none is stored, documents show <code>[add: email]</code> and the tool says so rather than\nletting anyone improvise an address.</p>\n"},"recurring":{"title":"MCP Recurring Invoices","tagline":"Scheduled invoices, generated into your invoice book with PDFs.","description":"Keeps billing schedules per client (weekly, monthly, quarterly, yearly or every N days) with month-end and leap-day rules, shows what falls due, generates exactly one invoice per schedule per period into the invoice server store with PDF rendering, never duplicates on a rerun, keeps a history, and forecasts revenue per month per currency.","html":"<h1>mcp-recurring</h1>\n<p>Say &quot;bill Acme 12 hours at 90 EUR on the 1st of every month&quot; once, and stop remembering it. This MCP server stores recurring invoice schedules -- client, line items, cadence, start and end dates -- and then, when you ask, creates the invoices that have actually fallen due as real records in the <a href=\"../invoice\">invoice server</a>, with its number series, its clients and its A4 PDF. Generation is idempotent: one invoice per schedule per period, keyed by the occurrence date, so running the billing run twice on the same day creates nothing the second time. It also answers &quot;what falls due in the next 30 days&quot; and &quot;what will I invoice per month for the next year&quot;. Everything is stored in plain JSON files on your own machine; nothing is uploaded anywhere.</p>\n<p><img src=\"../../assets/demo-recurring.gif\" alt=\"recurring demo\"></p>\n<p><strong>Define a repeating invoice once, generate the due PDFs from chat -- no billing SaaS required.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-recurring</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>recurring.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;recurring&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-recurring&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add recurring -- npx -y @theluckystrike/mcp-recurring\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;recurring&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-recurring&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/invoice -w servers/recurring\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/recurring/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><strong><a href=\"../invoice\">mcp-invoice</a></strong> -- required in practice, not by code. This server writes into the invoice\nserver&#39;s data directory and shares its number counter, its client list and its business profile, so every\ngenerated invoice appears in <code>invoice_list</code>, counts in <code>overdue_report</code> and can be re-rendered with\n<code>invoice_pdf</code>. Set your issuer details once with <code>business_set</code> <strong>there</strong>; this server has no <code>business_set</code>\nof its own on purpose, so there is only one profile to keep right.</li>\n<li><strong><a href=\"../time-tracker\">mcp-time-tracker</a></strong> -- for the hours that are not on a retainer. Track them, invoice\nthem ad hoc, and leave the fixed monthly part to a schedule here.</li>\n<li><strong><a href=\"../expense-tracker\">mcp-expense-tracker</a></strong> -- rebillable costs that change every month belong on an ad\nhoc invoice; a schedule is for the amount that does not change.</li>\n</ul>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>schedule_create</code></td>\n<td>Define a repeating invoice: client, items, cadence, start date, optional end date, due days, notes</td>\n</tr>\n<tr>\n<td><code>schedule_list</code></td>\n<td>Every schedule with cadence, amount per period, next due date and status</td>\n</tr>\n<tr>\n<td><code>schedule_get</code></td>\n<td>Full record for one schedule, plus how many invoices it has generated</td>\n</tr>\n<tr>\n<td><code>schedule_update</code></td>\n<td>Change client, items, currency, cadence, dates, due days or notes. Periods already invoiced are never re-issued</td>\n</tr>\n<tr>\n<td><code>schedule_pause</code></td>\n<td>Stop generating without deleting; history is kept</td>\n</tr>\n<tr>\n<td><code>schedule_resume</code></td>\n<td>Make it active again. Periods that fell due while paused are still due</td>\n</tr>\n<tr>\n<td><code>schedule_delete</code></td>\n<td>Remove the schedule. Invoices it made stay in the invoice server and its history is kept as an audit trail. A re-created schedule gets a new id, so <code>invoice_generate_due</code> warns when it re-covers a period the old one already billed</td>\n</tr>\n<tr>\n<td><code>schedule_skip</code></td>\n<td>Skip ONE occurrence for good, without pausing the schedule -- the answer to &quot;do not bill this client for October&quot;. <code>undo: true</code> puts the period back</td>\n</tr>\n<tr>\n<td><code>schedule_upcoming</code></td>\n<td>What falls due in the next N days, with amounts and totals per currency, plus any period that already fell due and was never invoiced. Free covers 30 days</td>\n</tr>\n<tr>\n<td><code>invoice_generate_due</code></td>\n<td>Create the invoices that are due as of a date and render their PDFs. Idempotent, keyed by period; reports created and skipped. At most 60 invoices per call, oldest period first, and it says how many are still due. <code>dry_run</code> shows the run first. Free and unlimited</td>\n</tr>\n<tr>\n<td><code>schedule_history</code></td>\n<td>Pro: the audit log for one schedule -- every period, invoice number, dates, amount, paid status and PDF path</td>\n</tr>\n<tr>\n<td><code>forecast</code></td>\n<td>Expected revenue per calendar month per currency, with paused schedules listed separately rather than dropped. Free covers 3 months</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Show free or Pro mode</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline)</td>\n</tr>\n</tbody></table>\n<p>Resource: <code>recurring://upcoming</code> returns the next 30 days of occurrences as JSON.\nPrompt: <code>monthly_billing_run</code> -- dry run, generate, list what is coming, then report who needs a payment reminder.</p>\n<h2>What you can say</h2>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;Bill Acme 12 hours at 90 EUR every month from the 1st.&quot;</td>\n<td><code>schedule_create</code></td>\n</tr>\n<tr>\n<td>&quot;What recurring invoices do I have?&quot;</td>\n<td><code>schedule_list</code></td>\n</tr>\n<tr>\n<td>&quot;What is due in the next 30 days?&quot;</td>\n<td><code>schedule_upcoming</code></td>\n</tr>\n<tr>\n<td>&quot;Run this month&#39;s billing.&quot;</td>\n<td><code>monthly_billing_run</code> / <code>invoice_generate_due</code></td>\n</tr>\n<tr>\n<td>&quot;Show me what would be created before you create it.&quot;</td>\n<td><code>invoice_generate_due {dry_run: true}</code></td>\n</tr>\n<tr>\n<td>&quot;Pause the Beta Corp retainer, they are on hold.&quot;</td>\n<td><code>schedule_pause</code></td>\n</tr>\n<tr>\n<td>&quot;Do not bill Acme for October.&quot;</td>\n<td><code>schedule_skip</code></td>\n</tr>\n<tr>\n<td>&quot;Put the Acme retainer up to 100 EUR an hour from now on.&quot;</td>\n<td><code>schedule_update</code></td>\n</tr>\n<tr>\n<td>&quot;How much will I invoice per month next year?&quot;</td>\n<td><code>forecast</code></td>\n</tr>\n<tr>\n<td>&quot;Show me every invoice this retainer has produced.&quot;</td>\n<td><code>schedule_history</code></td>\n</tr>\n</tbody></table>\n<h2>Worked example</h2>\n<pre><code>You: Bill Acme 12 hours at 90 EUR a month, starting 1 June, 14 day terms.\n\n  schedule_create {\n    client: &quot;Acme Retainer&quot;, currency: &quot;EUR&quot;, every: &quot;monthly&quot;,\n    start_date: &quot;2026-06-01&quot;, due_days: 14,\n    items: [{ description: &quot;Retainer hours&quot;, quantity: 12, unit_price: 90 }]\n  }\n  -&gt; schedule 9f2c1a04, next dates 2026-06-01, 2026-07-01, 2026-08-01, 2026-09-01\n\nYou (on 3 September): Run the billing.\n\n  invoice_generate_due {}\n  -&gt; as_of 2026-09-03: created 4 invoices, skipped 0 already invoiced.\n     INV-2026-0001  Acme Retainer  period 2026-06-01  EUR 1080.00  due 2026-06-15  .../pdf/INV-2026-0001.pdf\n     INV-2026-0002  Acme Retainer  period 2026-07-01  EUR 1080.00  due 2026-07-15  .../pdf/INV-2026-0002.pdf\n     INV-2026-0003  Acme Retainer  period 2026-08-01  EUR 1080.00  due 2026-08-15  .../pdf/INV-2026-0003.pdf\n     INV-2026-0004  Acme Retainer  period 2026-09-01  EUR 1080.00  due 2026-09-15  .../pdf/INV-2026-0004.pdf\n     Total: EUR 4320.00\n\nYou (five minutes later, having forgotten): Run the billing.\n\n  invoice_generate_due {}\n  -&gt; as_of 2026-09-03: created 0 invoices, skipped 4 already invoiced.\n</code></pre>\n<p>The second run is the point: the period, not the calendar day, is the key, so a repeated billing run is a\nno-op rather than a duplicate invoice sitting in a client&#39;s inbox.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Active schedules</td>\n<td>3</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>invoice_generate_due</code></td>\n<td>Yes, unlimited</td>\n<td>Yes, unlimited</td>\n</tr>\n<tr>\n<td><code>schedule_upcoming</code> horizon</td>\n<td>30 days</td>\n<td>Up to 10 years</td>\n</tr>\n<tr>\n<td><code>forecast</code></td>\n<td>3 months</td>\n<td>Up to 120 months</td>\n</tr>\n<tr>\n<td><code>schedule_history</code> audit log</td>\n<td>No</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td>End-of-month and anchor-day rules (<code>anchor_day</code>, <code>end_of_month</code>)</td>\n<td>No, bills on the start date&#39;s day of month</td>\n<td>Yes</td>\n</tr>\n<tr>\n<td>Pause, resume, update, delete, dry run, multi-currency</td>\n<td>Yes</td>\n<td>Yes</td>\n</tr>\n</tbody></table>\n<p>Pro is a one-time $19, or $39 for every server in the collection, lifetime.</p>\n<p><strong>Get Pro: <a href=\"https://mcp.zovo.one/buy/recurring\">https://mcp.zovo.one/buy/recurring</a></strong></p>\n<h2>Dates: what happens at a month end</h2>\n<p>Every date is a local ISO calendar date, <code>YYYY-MM-DD</code>. An occurrence is the k-th step from <code>start_date</code>, and\noccurrence 0 is <code>start_date</code> itself, so a schedule starting today is due today.</p>\n<ul>\n<li><code>weekly</code> = +7 days per step. <code>{days: n}</code> = +n days per step.</li>\n<li><code>monthly</code> = +1 month, <code>quarterly</code> = +3 months, <code>yearly</code> = +12 months.</li>\n<li><strong>Month ends.</strong> The month step keeps the day of month of <code>start_date</code> and clamps it to the length of the\ntarget month; it never carries the clamp forward. From <code>2026-01-31</code> the series is 01-31, <strong>02-28</strong>, 03-31,\n04-30, 05-31 -- February does not silently turn a month-end retainer into a 28th-of-the-month retainer.</li>\n<li><strong>Feb 29.</strong> The same rule makes a yearly schedule starting <code>2028-02-29</code> fall on 02-28 in common years and\nback on <strong>02-29</strong> in the next leap year.</li>\n<li><strong><code>anchor_day</code> / <code>end_of_month</code> (Pro).</strong> <code>anchor_day</code> replaces the day of month before clamping, so\n<code>anchor_day: 31</code> means the last day of every month; <code>end_of_month: true</code> does the same explicitly. Both are\nignored for <code>weekly</code> and <code>{days: n}</code>, which have no month to anchor to. An anchored first occurrence that\nwould land before <code>start_date</code> is dropped, never billed early.</li>\n<li><strong><code>end_date</code> is inclusive.</strong> An occurrence landing exactly on <code>end_date</code> is generated; the next one is not.</li>\n<li><strong>Long-lived schedules.</strong> Looking up what is due does not replay the schedule from <code>start_date</code>: it jumps to\nan estimate near the date you asked about and scans forward from there, so a daily schedule created in 2010\nstill reports what is due in 2026 instead of exhausting its per-run occurrence cap walking there one day at\na time.</li>\n</ul>\n<h2>Money</h2>\n<p>Amounts are held as integer minor units by the invoice engine -- the same ISO 4217 table, the same\nround-per-line-then-sum contract, so a schedule&#39;s amount and the invoice it produces can never disagree. Each\nline&#39;s gross is rounded first, tax is computed and rounded per line and grouped into one line per rate, and\nthe totals are integer sums of those already-rounded values. A schedule bills in its own <code>currency</code>, or your\nbusiness default currency if it has none; nothing here converts between currencies.</p>\n<h2>How it stores data</h2>\n<p>Schedules and the generation log live in\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/recurring/</code> as <code>schedules.json</code> and <code>history.json</code>. The\n<strong>invoices</strong> go into the invoice server&#39;s directory, <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/invoice/</code>,\nwith their PDFs under its <code>pdf/</code> subfolder -- the same files <code>invoice_list</code>, <code>overdue_report</code> and\n<code>invoice_pdf</code> read there.</p>\n<p>Every mutation runs under an advisory lock file. Anything that writes an invoice takes <strong>two</strong> locks, always\nin the same order -- <code>recurring/.lock</code> first, then <code>invoice/.lock</code> -- so two billing runs (or a billing run\nand a hand-written invoice in the other server) cannot interleave, cannot allocate the same invoice number\nand cannot deadlock. Invoice numbers are allocated inside the lock; the PDFs are rendered after it is\nreleased, so a slow render never holds up the counter. Saves go to a temporary file and are renamed into\nplace.</p>\n<p>If <code>schedules.json</code> or <code>history.json</code> is unreadable or not valid JSON it is never treated as &quot;empty&quot;: the\nfile is moved aside byte-for-byte as <code>&lt;name&gt;.json.corrupt-&lt;timestamp&gt;</code>, a <code>&lt;name&gt;.json.corrupt</code> marker is\nwritten, and every tool fails loudly until you restore a good copy and delete the marker. This matters more\nhere than anywhere else in the collection: a <code>history.json</code> silently read as empty would re-bill every period\nthe schedule has ever covered.</p>\n<h2>Limits and honest caveats</h2>\n<ul>\n<li><strong>Nothing runs in the background.</strong> This is a stdio MCP server: it exists while your client runs it. No\ndaemon, no cron, no email. Invoices are created when you (or the <code>monthly_billing_run</code> prompt) call\n<code>invoice_generate_due</code>. <code>auto_generate</code> is a marker for that prompt, not a scheduler.</li>\n<li><strong>Nothing is sent to the client.</strong> The server produces the invoice record and the PDF; delivering it and\nchasing payment is still yours to do. <code>overdue_report</code> in the invoice server tells you who to chase.</li>\n<li>Free tier allows 3 active schedules. Pausing one frees a slot; the paused schedule&#39;s history is kept.</li>\n<li>Deleting a schedule keeps its history rows, deliberately: a re-created schedule with the same id cannot\ndouble-bill a period. Invoices already generated are never touched by anything here.</li>\n<li><code>schedule_update</code> changes future periods only. A period already invoiced keeps the amount that was billed;\ncorrect it in the invoice server instead.</li>\n<li>No proration and no mid-period cancellation credit: a period is billed in full or not at all.</li>\n<li>No currency conversion; a schedule bills in one currency.</li>\n</ul>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong><code>npx</code> hangs or fails to find the package</strong>: npm publish for this package is pending. Use the <code>.mcpb</code>\nbundle or the clone-and-build path above until it lands.</li>\n<li><strong>Using the clone path</strong>: build <code>servers/invoice</code> before <code>servers/recurring</code> -- the engine is imported from\nit. <code>npm run build -w packages/mcp-license -w servers/invoice -w servers/recurring</code> does that in order.</li>\n<li><strong>&quot;No business profile yet&quot;</strong>: run <code>business_set</code> in the invoice server (mcp-invoice), not here.\nGeneration is never blocked by it; the PDF just carries the placeholder issuer &quot;Your business&quot;.</li>\n<li><strong>The invoices are not in my invoice server</strong>: both servers must see the same <code>XDG_DATA_HOME</code>. They write\nto <code>.../mcp-servers/invoice/</code> under it; if one client sets that variable and the other does not, you have\ntwo stores.</li>\n<li><strong>A period was skipped</strong>: <code>invoice_generate_due</code> only skips a period already present in <code>history.json</code>.\n<code>schedule_history</code> (Pro) or the file itself shows exactly which invoice covered it.</li>\n<li><strong>Node version</strong>: requires Node &gt;= 18. Check with <code>node -v</code>.</li>\n</ul>\n<h2>Privacy</h2>\n<p>All data stays local: schedules, the generation log, invoices and PDFs are plain files under your own home\ndirectory. The server makes no network calls at all, and license keys are verified offline.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>. MIT. Support: <a href=\"mailto:support@zovo.one\">support@zovo.one</a></p>\n<h2>One business profile for the whole suite</h2>\n<p>Your identity is stored once, at <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/profile/business.json</code>,\nand every server in the suite reads it: the invoice issuer, the docx letterhead, the recurring\nissuer, expense-tracker&#39;s default VAT rate, time-tracker&#39;s and timezone&#39;s home zone, and the\nresume and contract letterheads. Set it once with <code>business_set</code> (invoice or docx) - you never\nrepeat it anywhere else. An email address is only ever taken from that profile or from an explicit\nargument; when none is stored, documents show <code>[add: email]</code> and the tool says so rather than\nletting anyone improvise an address.</p>\n"},"clauses":{"title":"MCP Clause Library","tagline":"Reusable contract clauses, searched and assembled into Word.","description":"Ships 25 generic freelance clauses (scope, payment terms, IP, confidentiality, termination, liability, governing law and more), each marked as a template with variables, lets you add and search your own, extracts variables, fills them, and assembles a contract or proposal into .docx or markdown through the docx engine with a not-legal-advice line.","html":"<h1>mcp-clauses</h1>\n<p>Say &quot;draft a service agreement for Beta Corp, 4,500 EUR, 14-day terms&quot; and get a real <code>.docx</code> built from your own clause library. This MCP server keeps the contract and proposal paragraphs you reuse -- scope, payment terms, late fees, IP assignment, confidentiality, termination, liability cap and twenty more -- as searchable clauses with <code>{{variables}}</code> in them. It ships with 25 generic freelance starters, finds the right ones by ranked search, fills the variables you supply, leaves every fact you did not supply as a visible bracketed prompt like <code>[late fee percent]</code>, and writes the assembled document to Word or markdown. Every document opens with the line that it is a generic template and not legal advice. Everything runs locally: no upload, no account, no native dependency.</p>\n<p><img src=\"../../assets/demo-clauses.gif\" alt=\"clauses demo\"></p>\n<p><strong>Stop pasting last year&#39;s contract into a new file and hoping you changed every name.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-clauses</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>clauses.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;clauses&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-clauses&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add clauses -- npx -y @theluckystrike/mcp-clauses\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;clauses&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-clauses&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/docx -w servers/clauses\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/clauses/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>This is not legal advice</h2>\n<p>The 25 starter clauses are generic freelance templates written in plain language. They are not drafted for your\ncountry, your trade or your deal, and no clause here has been reviewed by a lawyer. Every starter carries the note\n<code>generic template, not legal advice</code>, and every document <code>contract_assemble</code> writes opens with the same line. Treat\nthe output as a first draft to take to a qualified lawyer, not as a contract to sign.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>clause_add</code></td>\n<td>Save a reusable clause: title, body with <code>{{variables}}</code>, category, tags, optional jurisdiction and language</td>\n</tr>\n<tr>\n<td><code>clause_get</code></td>\n<td>Return one clause in full, by id or by title (a partial title matches)</td>\n</tr>\n<tr>\n<td><code>clause_update</code></td>\n<td>Change the text, category, tags, variables or jurisdiction. In Pro the previous text is kept as a version</td>\n</tr>\n<tr>\n<td><code>clause_delete</code></td>\n<td>Remove a clause. A deleted starter is not re-seeded on the next call</td>\n</tr>\n<tr>\n<td><code>clause_list</code></td>\n<td>List every clause, in assembly order, optionally narrowed to one category</td>\n</tr>\n<tr>\n<td><code>clause_search</code></td>\n<td>Ranked search over titles, tags, categories and bodies. A title match outranks a body mention; a term matches on a real word boundary (&quot;fee&quot; never matches inside &quot;coffee&quot;), with plain substring containment kept only as a lower-ranked fallback. Pro adds jurisdiction and tag filters</td>\n</tr>\n<tr>\n<td><code>clause_import</code></td>\n<td>Bulk-load from a markdown file (<code>## Title</code>, <code>category:</code> / <code>tags:</code> lines, blank line, body) or from JSON (Pro). Only short, field-shaped lines right after the title are read as metadata, so body prose that happens to open with a word like &quot;note:&quot; is kept as body, not swallowed</td>\n</tr>\n<tr>\n<td><code>clause_export</code></td>\n<td>Write the whole library to markdown (free) or JSON (Pro). An existing destination is never replaced unless you pass <code>overwrite: true</code></td>\n</tr>\n<tr>\n<td><code>contract_assemble</code></td>\n<td>Build a document from clause ids or whole categories: orders the clauses, numbers them, fills the variables, brackets what is missing, prepends the not-legal-advice line, writes <code>.docx</code> or <code>.md</code>. An existing <code>out_path</code> is never replaced unless you pass <code>overwrite: true</code></td>\n</tr>\n<tr>\n<td><code>variables_list</code></td>\n<td>Every <code>{{variable}}</code> a selection of clauses needs, and which clause needs it, before you assemble</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Show free or Pro mode</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline)</td>\n</tr>\n</tbody></table>\n<p>Resource: <code>clauses://categories</code> returns every category in the library with its clause count, in assembly order.\nPrompt: <code>draft_contract</code> picks clauses that fit an engagement, assembles them, and reports the facts still missing.</p>\n<h2>The 25 starter clauses</h2>\n<table>\n<thead>\n<tr>\n<th>Category</th>\n<th>Clauses</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>scope</td>\n<td>Scope of Work, Revisions, Acceptance of Deliverables, Change Requests</td>\n</tr>\n<tr>\n<td>payment</td>\n<td>Payment Terms, Late Payment, Kill Fee, Rush Fee</td>\n</tr>\n<tr>\n<td>expenses</td>\n<td>Expenses</td>\n</tr>\n<tr>\n<td>ip</td>\n<td>Intellectual Property Assignment, Portfolio and Credit</td>\n</tr>\n<tr>\n<td>confidentiality</td>\n<td>Confidentiality</td>\n</tr>\n<tr>\n<td>data</td>\n<td>Data Protection</td>\n</tr>\n<tr>\n<td>term</td>\n<td>Termination</td>\n</tr>\n<tr>\n<td>liability</td>\n<td>Limitation of Liability</td>\n</tr>\n<tr>\n<td>warranty</td>\n<td>Warranty Disclaimer</td>\n</tr>\n<tr>\n<td>disputes</td>\n<td>Dispute Resolution</td>\n</tr>\n<tr>\n<td>general</td>\n<td>Governing Law, Force Majeure, Independent Contractor, Non-Solicitation, Notices, Entire Agreement, Severability, Assignment</td>\n</tr>\n</tbody></table>\n<p>They use variables such as <code>{{client}}</code>, <code>{{contractor}}</code>, <code>{{project}}</code>, <code>{{fee}}</code>, <code>{{currency}}</code>,\n<code>{{payment_days}}</code>, <code>{{deposit_percent}}</code>, <code>{{late_fee_percent}}</code>, <code>{{liability_cap}}</code>, <code>{{notice_days}}</code>,\n<code>{{revision_rounds}}</code>, <code>{{acceptance_days}}</code>, <code>{{kill_fee_percent}}</code>, <code>{{rush_fee_percent}}</code> and <code>{{jurisdiction}}</code>.\nCall <code>variables_list</code> with the clauses you picked to see exactly which ones your document needs.</p>\n<h2>What you can say</h2>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;What clauses do I have about late payment?&quot;</td>\n<td><code>clause_search</code></td>\n</tr>\n<tr>\n<td>&quot;Save this as a clause called Retainer, category payment.&quot;</td>\n<td><code>clause_add</code></td>\n</tr>\n<tr>\n<td>&quot;Draft a service agreement for Beta Corp: scope, payment, late fees, IP, termination. Fee 4,500 EUR, 14-day terms.&quot;</td>\n<td><code>contract_assemble</code></td>\n</tr>\n<tr>\n<td>&quot;What do I still need to fill in for those five clauses?&quot;</td>\n<td><code>variables_list</code></td>\n</tr>\n<tr>\n<td>&quot;Import my old clauses from clauses.md.&quot;</td>\n<td><code>clause_import</code></td>\n</tr>\n<tr>\n<td>&quot;Export the whole library so I can keep a copy.&quot;</td>\n<td><code>clause_export</code></td>\n</tr>\n</tbody></table>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>25 starter clauses</td>\n<td>yes</td>\n<td>yes</td>\n</tr>\n<tr>\n<td>Your own clauses</td>\n<td>10</td>\n<td>unlimited</td>\n</tr>\n<tr>\n<td>Ranked search, category filter</td>\n<td>yes</td>\n<td>yes</td>\n</tr>\n<tr>\n<td>Filter by jurisdiction or tag</td>\n<td>no</td>\n<td>yes</td>\n</tr>\n<tr>\n<td>Clauses per assembled document</td>\n<td>8</td>\n<td>unlimited</td>\n</tr>\n<tr>\n<td>Markdown import and export</td>\n<td>yes</td>\n<td>yes</td>\n</tr>\n<tr>\n<td>JSON import and export</td>\n<td>no</td>\n<td>yes</td>\n</tr>\n<tr>\n<td>Clause version history on <code>clause_update</code></td>\n<td>no</td>\n<td>yes</td>\n</tr>\n</tbody></table>\n<p><strong>Get Pro:</strong> <a href=\"https://mcp.zovo.one/buy/clauses\">https://mcp.zovo.one/buy/clauses</a> -- $19 one-time, or $39 for the whole collection.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../docx\">mcp-docx</a> -- the document engine this server assembles through. Install both and the same conversation\nthat picks clauses can also write the covering letter and read the client&#39;s own <code>.docx</code> back.</li>\n<li><a href=\"../invoice\">mcp-invoice</a> -- the payment terms you agreed in the contract are the terms you bill on.</li>\n<li><a href=\"../recurring\">mcp-recurring</a> -- once a retainer clause is signed, the schedule that raises its invoices.</li>\n</ul>\n<h2>Privacy</h2>\n<p>Every clause and every assembled document stays on your machine, in\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/clauses/</code>. The server makes no network calls at all. Your licence key\nis verified offline.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>. Support: <a href=\"mailto:support@zovo.one\">support@zovo.one</a></p>\n<h2>One business profile for the whole suite</h2>\n<p>Your identity is stored once, at <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/profile/business.json</code>,\nand every server in the suite reads it: the invoice issuer, the docx letterhead, the recurring\nissuer, expense-tracker&#39;s default VAT rate, time-tracker&#39;s and timezone&#39;s home zone, and the\nresume and contract letterheads. Set it once with <code>business_set</code> (invoice or docx) - you never\nrepeat it anywhere else. An email address is only ever taken from that profile or from an explicit\nargument; when none is stored, documents show <code>[add: email]</code> and the tool says so rather than\nletting anyone improvise an address.</p>\n"},"pdf":{"title":"MCP PDF Tools","tagline":"Merge, split, stamp and read PDFs, pure JavaScript.","description":"Merges and splits PDFs, extracts and reorders pages, rotates, stamps PAID or DRAFT or any text at a chosen position, watermarks the business name and VAT id from the shared profile, extracts text best effort, and reports page counts and metadata. Never edits inputs in place and refuses encrypted files with a clear message.","html":"<h1>mcp-pdf</h1>\n<p>Say &quot;stamp PAID on that invoice and save a copy&quot; or &quot;pull pages 2 to 6 out of this scan and merge them with the contract&quot; and it happens, on your machine, in a second. This MCP server does the small PDF jobs that otherwise send you to a web uploader: merge, split by page range, extract or reorder pages, rotate a sideways scan, stamp <code>PAID</code> or <code>DRAFT</code> or any text you like, put your business name and VAT id in the footer, count pages across a folder of files, and read a PDF&#39;s text back as text. No upload, no account, no native dependency, no office install.</p>\n<p><img src=\"../../assets/demo-pdf.gif\" alt=\"pdf demo\"></p>\n<p><strong>The PDF chores of a freelance business, done from chat instead of from a browser tab you do not trust with an invoice.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-pdf</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>pdf.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;pdf&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-pdf&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add pdf -- npx -y @theluckystrike/mcp-pdf\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;pdf&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-pdf&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/pdf\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/pdf/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>pdf_info</code></td>\n<td>Page count, every page&#39;s size in points and millimetres, the paper name (A4, Letter, custom), the rotation already on each page, the metadata (title, author, producer, dates) and whether the file is encrypted</td>\n</tr>\n<tr>\n<td><code>pdf_count</code></td>\n<td>Page count per file plus the total, for any number of PDFs. A file it cannot read is reported per file; the rest still count</td>\n</tr>\n<tr>\n<td><code>pdf_merge</code></td>\n<td>Join PDFs into one, in the order given. Page sizes are kept as they are, and a merged file with mixed sizes says so</td>\n</tr>\n<tr>\n<td><code>pdf_split</code></td>\n<td>One new file per range. <code>&quot;1-3,5,7-&quot;</code> gives pages 1-3, page 5, and page 7 to the end. <code>out_path_pattern</code> takes <code>{n}</code>, <code>{range}</code> and <code>{name}</code></td>\n</tr>\n<tr>\n<td><code>pdf_pages</code></td>\n<td>Extract selected pages into one new PDF, in the order you write them. <code>&quot;5,1,1&quot;</code> puts page 5 first and page 1 in twice</td>\n</tr>\n<tr>\n<td><code>pdf_rotate</code></td>\n<td>Turn pages by a multiple of 90 degrees, added to the rotation the page already had, which is what a sideways scan needs</td>\n</tr>\n<tr>\n<td><code>pdf_stamp</code></td>\n<td>Draw text on the pages: <code>PAID</code> and <code>DRAFT</code> presets, or any text, colour, position, opacity and size. Centre stamps go on the 45-degree diagonal</td>\n</tr>\n<tr>\n<td><code>pdf_watermark_business</code></td>\n<td>Your business name and VAT id in the footer of every page, from the shared profile <a href=\"../invoice\">mcp-invoice</a> and <a href=\"../docx\">mcp-docx</a> write</td>\n</tr>\n<tr>\n<td><code>pdf_text</code></td>\n<td>Best-effort text extraction from the page content streams. See the honest caveats below</td>\n</tr>\n<tr>\n<td><code>pdf_reorder</code></td>\n<td>Write a new PDF with the pages in a new order. The order must name every page exactly once, so nothing is dropped by accident</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Show free or Pro mode</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline)</td>\n</tr>\n</tbody></table>\n<p>Resource: <code>pdf://recent</code> returns the last 25 operations, newest first, with the inputs, the files written and when.\nPrompt: <code>mark_invoice_paid</code> chains <a href=\"../invoice\">mcp-invoice</a>&#39;s <code>invoice_get</code> into <code>pdf_stamp PAID</code> and saves the stamped copy beside the original.</p>\n<h2>What you can say</h2>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;Mark invoice INV-2026-0007 as paid.&quot;</td>\n<td><code>mark_invoice_paid</code> prompt, then <code>pdf_stamp</code></td>\n</tr>\n<tr>\n<td>&quot;Stamp DRAFT across this contract before I send it.&quot;</td>\n<td><code>pdf_stamp</code></td>\n</tr>\n<tr>\n<td>&quot;Join these three PDFs into one file.&quot;</td>\n<td><code>pdf_merge</code></td>\n</tr>\n<tr>\n<td>&quot;Split the scan: pages 1-3 are the invoice, 4 onwards is the receipt.&quot;</td>\n<td><code>pdf_split</code></td>\n</tr>\n<tr>\n<td>&quot;Pull pages 2 to 6 out of this and save them separately.&quot;</td>\n<td><code>pdf_pages</code></td>\n</tr>\n<tr>\n<td>&quot;This scan is sideways, turn it.&quot;</td>\n<td><code>pdf_rotate</code></td>\n</tr>\n<tr>\n<td>&quot;How many pages are in all of these?&quot;</td>\n<td><code>pdf_count</code></td>\n</tr>\n<tr>\n<td>&quot;What does page 1 of this PDF say?&quot;</td>\n<td><code>pdf_text</code></td>\n</tr>\n<tr>\n<td>&quot;Put my company name and VAT number in the footer.&quot;</td>\n<td><code>pdf_watermark_business</code></td>\n</tr>\n<tr>\n<td>&quot;Move the cover page to the front.&quot;</td>\n<td><code>pdf_reorder</code></td>\n</tr>\n</tbody></table>\n<h2>Worked example</h2>\n<pre><code>You: Stamp PAID on ~/invoices/INV-2026-0007.pdf and save a copy.\n\n  pdf_stamp {\n    path: &quot;~/invoices/INV-2026-0007.pdf&quot;,\n    text: &quot;PAID&quot;,\n    position: &quot;center&quot;,\n    out_path: &quot;~/invoices/INV-2026-0007-paid.pdf&quot;\n  }\n  -&gt; Stamped &quot;PAID&quot; on 1 page\n  -&gt; ~/invoices/INV-2026-0007-paid.pdf, 0.9 MB\n</code></pre>\n<p>The stamp is drawn text, not a flattened image: it can be selected and searched, it is drawn on the 45-degree\ndiagonal in the preset green, and the original file is byte-for-byte unchanged.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>pdf_info</code>, <code>pdf_count</code>, <code>pdf_text</code></td>\n<td>Unlimited</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>pdf_merge</code></td>\n<td>Up to 5 files per call</td>\n<td>Any number of files</td>\n</tr>\n<tr>\n<td><code>pdf_split</code>, <code>pdf_pages</code>, <code>pdf_rotate</code></td>\n<td>Files up to 30 pages</td>\n<td>Any length</td>\n</tr>\n<tr>\n<td><code>pdf_stamp</code></td>\n<td>The <code>PAID</code> and <code>DRAFT</code> presets in their preset colours</td>\n<td>Any text, any colour, any size</td>\n</tr>\n<tr>\n<td><code>pdf_watermark_business</code></td>\n<td>-</td>\n<td>Your business name and VAT id in the footer</td>\n</tr>\n<tr>\n<td><code>pdf_reorder</code></td>\n<td>-</td>\n<td>Yes</td>\n</tr>\n</tbody></table>\n<p>A tier limit is an answer, not an error, and nothing is written when one refuses a call.</p>\n<p>Pro is a one-time $19, or $39 for every server in the collection, lifetime.</p>\n<p><strong>Get Pro: <a href=\"https://mcp.zovo.one/buy/pdf\">https://mcp.zovo.one/buy/pdf</a></strong></p>\n<h2><code>pdf_text</code> is best effort, and here is exactly what that means</h2>\n<p>A PDF does not store text. It stores drawing operators, and the text you see is bytes handed to a font. This server\ndecompresses each page&#39;s FlateDecode content stream with <code>node:zlib</code> and reads the four text-showing operators --\n<code>Tj</code>, <code>TJ</code>, <code>&#39;</code> and <code>&quot;</code> -- plus the positioning operators that end a line. That is a parser written here, in about\ntwo hundred lines, with no <code>pdfjs</code> and no native module, and it has three real limits the tool states in its own\nanswer every time:</p>\n<ul>\n<li><strong>A scan has no text.</strong> An image-only page carries no text operators, so nothing comes back. There is no OCR here\nand there will not be one; the answer says the page is probably a scan rather than returning an empty string with\nno explanation.</li>\n<li><strong>A custom or CID encoding returns glyph indices, not letters.</strong> Subset-embedded and CJK fonts map bytes to glyph\nnumbers through a table this parser does not read. When the output has no readable characters, the answer says the\nfont is the reason.</li>\n<li><strong>Reading order is drawing order.</strong> There is no column detection and no layout reconstruction. Word spaces are\nrecovered from the large negative kerns a <code>TJ</code> array uses for them, so a PDF that positions every word separately\ncan come back with words run together.</li>\n</ul>\n<p>PDFs written by word processors, invoicing tools and report generators -- everything with a standard or fully\nembedded font -- read back cleanly. Anything else tells you why it did not.</p>\n<h2>Encrypted PDFs are refused, not guessed</h2>\n<p><code>pdf-lib</code> cannot decrypt, and loading an encrypted file while ignoring the encryption produces pages of garbage that\nwould be written straight into the output. So a file whose trailer names an <code>/Encrypt</code> dictionary is refused by every\ntool that writes, with the reason and the fix in the message: open it in a reader with the password, export or print\nto a new PDF, and use that file. <code>pdf_info</code> still answers, with <code>encrypted: true</code>, because reporting is its whole job,\nand <code>pdf_count</code> reports it as one unreadable file while still counting the rest.</p>\n<h2>Existing files are never overwritten, and inputs are never modified</h2>\n<p>Every tool writes a new file and leaves its inputs byte-for-byte alone -- there is no in-place mode, on purpose.\nAn <code>out_path</code> that already exists is refused:</p>\n<pre><code>Error: /path/merged.pdf already exists and nothing was written.\nPass overwrite: true to replace it, or give a different out_path.\n</code></pre>\n<p>The path is reserved with an exclusive create, not an existence check, so two processes writing the same <code>out_path</code>\nat the same time cannot clobber each other: one wins, the other is refused and writes nothing. <code>pdf_split</code> reserves\nevery one of its output paths before it writes any of them, so a collision on part 3 does not leave parts 1 and 2\nbehind as a half-done split -- and the reservations are released, so nothing empty is left on disk either.</p>\n<p>Pass <code>overwrite: true</code> when replacing the file is what you want.</p>\n<p><code>overwrite: true</code> still does not let an output be an input. Writing the result of an operation back over one of its\nown sources destroys that source -- the pages are already in memory and get written over the file they came from,\nwhich takes a three-page file to the one page you extracted -- and every later read of that path is then quietly\nwrong. So an <code>out_path</code> that resolves to (or shares an inode with) any input of the same call is refused before any\nwork happens:</p>\n<pre><code>Error: out_path /path/scan.pdf is also an input of this operation, so writing it would destroy the source.\nNothing was written. Write beside it instead - /path/scan-out.pdf - and, if the result really is meant to\ntake the original&#39;s place, rename it yourself once you have checked it.\n</code></pre>\n<h2>Limits</h2>\n<ul>\n<li>Inputs over 100 MB are refused: rewriting a PDF needs several times its size in memory.</li>\n<li>A file that does not start with <code>%PDF-</code> is refused before anything is read.</li>\n<li>Rotation is recorded as page metadata, in multiples of 90 degrees, which is all the format has. Nothing is redrawn.</li>\n<li>Stamp text goes through a built-in PDF font, which carries WinAnsi and its 256 code points. A character outside it\nis transliterated where it has an obvious Latin body -- Polish <code>OPŁACONE</code> stamps as <code>OPLACONE</code>, and the answer says\nso and prints what was actually drawn -- and removed and counted where it does not, so there is no CJK stamp. A\nnewline is a word separator, never a deletion.</li>\n<li><code>font_size</code> must be between 1 and 1600 points. A stamp too long to fit even at the smallest size this server will\nuse is still drawn, and the answer says how far past the edge it runs, so you can shorten it.</li>\n<li><code>pdf_text</code> caps one answer at 200,000 characters and names the <code>pages</code> argument that continues from where it\nstopped. Nothing is missing from the file; the cut is in the answer, not the read.</li>\n<li>No page numbering, no signatures, no redaction, no compression. This server does the page-level jobs; it is not a\nPDF editor. It does not <em>write</em> PDF/A: <code>pdf_info</code> reports a file&#39;s own <code>pdfa_claim</code>, and every tool that writes\nsays plainly that its output no longer holds that claim.</li>\n<li>Forms are read, not filled: <code>pdf_text</code> prints the AcroForm field values, which live in the fields rather than in\nthe page content stream and are therefore not part of the page text.</li>\n<li>No OCR. See the <code>pdf_text</code> section.</li>\n</ul>\n<h2>How it stores data</h2>\n<p>Only a register of what it did: <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/pdf/operations.json</code>, the last 500\noperations, as plain JSON. Your PDFs stay where you put them. Every write to the register runs inside an advisory\nlock on <code>.../pdf/.lock</code>, so two clients on one data directory cannot lose a record, and saves go to a temporary file\nand are renamed into place.</p>\n<p>If the register is unreadable or not valid JSON it is never treated as &quot;empty&quot;: it is moved aside byte-for-byte as\n<code>operations.json.corrupt-&lt;timestamp&gt;</code> with a marker beside it. The PDF you asked for is still written -- the file is\non disk before the register is touched -- and the answer tells you the history could not be updated.</p>\n<p>The business footer reads the one shared profile the whole suite uses,\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/profile/business.json</code>, written by <code>business_set</code> in mcp-invoice or\nmcp-docx. Nothing is invented: with no name stored, <code>pdf_watermark_business</code> refuses and says which tool to run.</p>\n<h2>Privacy</h2>\n<p>All data stays local. The server reads and writes files on your machine and makes no network request of any kind --\nnot for licensing (keys are verified offline), not for fonts, not for telemetry. A PDF you stamp is never uploaded\nanywhere, which is the entire reason this exists.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../invoice/README.md\">mcp-invoice</a> -- the invoice you sent, stamped PAID: the <code>mark_invoice_paid</code> prompt chains <code>invoice_get</code> into <code>pdf_stamp</code>.</li>\n<li><a href=\"../docx/README.md\">mcp-docx</a> -- write the proposal as <code>.docx</code>, print it to PDF, then stamp, merge or split it here.</li>\n<li><a href=\"../resume/README.md\">mcp-resume</a> -- join a CV, a cover letter and a portfolio into the one file an application form accepts.</li>\n<li><a href=\"../office-suite/README.md\">office-suite</a> -- several servers behind one install, one config entry.</li>\n</ul>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong><code>npx</code> hangs or fails to find the package</strong>: npm publish for this package is pending. Use the <code>.mcpb</code> bundle or the\nclone-and-build path above until it lands.</li>\n<li><strong>&quot;is encrypted and was refused&quot;</strong>: the file is password or permissions protected. Open it in a PDF reader, export\nor print to a new PDF, and run the tool on that copy. This server never guesses or strips a password.</li>\n<li><strong><code>pdf_text</code> returned nothing</strong>: the page is a scan, or its font uses a custom encoding. The answer says which.\nThere is no OCR here.</li>\n<li><strong>The stamp is too small or too big</strong>: by default it is sized to fit the page width. Pass <code>font_size</code> for an exact\npoint size, and <code>opacity</code> between 0 and 1.</li>\n<li><strong>&quot;already exists and nothing was written&quot;</strong>: pass <code>overwrite: true</code>, or a different <code>out_path</code>.</li>\n<li><strong>Node version</strong>: requires Node &gt;= 18. Check with <code>node -v</code>.</li>\n</ul>\n<p>MIT licensed. Support: <a href=\"mailto:support@zovo.one\">support@zovo.one</a></p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n"},"calendar":{"title":"MCP Calendar","tagline":"Read .ics calendars: events, free and busy, conflicts, exports.","description":"Parses .ics files and feeds from Google, Apple and Outlook (recurrence rules, exceptions, time zones, all-day events), lists events in a window in your home zone, computes busy blocks and free windows, flags overlapping events, exports selections as valid .ics, and turns a meeting into the arguments for a billable time entry.","html":"<h1>mcp-calendar</h1>\n<p>Your calendar app can show you next Tuesday. It cannot tell you where your week actually went, which two things you\nsaid yes to at the same time, or how many billable hours last month&#39;s meetings were worth. This server reads the\n<code>.ics</code> file your calendar already exports and answers those questions: events in any window with recurring series\nexpanded properly, merged busy blocks and the real free gaps inside your working hours, every double booking across\nwork and personal calendars at once, a clean <code>.ics</code> of just the events you picked, and the exact time entry for a\nmeeting you should have billed. No account is connected, nothing is synced, and the calendar file stays on your\nmachine as plain text.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n<p><img src=\"../../assets/demo-calendar.gif\" alt=\"calendar demo\"></p>\n<p><strong>Import the .ics your calendar exports, then ask what is on, when you are free, and what clashes -- zero setup, all local.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-calendar</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>calendar.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;calendar&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-calendar&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add calendar -- npx -y @theluckystrike/mcp-calendar\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;calendar&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-calendar&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above,\nor build from source with exactly these four commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/timezone -w servers/calendar\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/calendar/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Getting the .ics out of your calendar</h2>\n<table>\n<thead>\n<tr>\n<th>App</th>\n<th>Where</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Google Calendar</td>\n<td>Settings -&gt; Import &amp; export -&gt; Export. You get a zip; import the <code>.ics</code> inside it.</td>\n</tr>\n<tr>\n<td>Apple Calendar</td>\n<td>File -&gt; Export -&gt; Export...</td>\n</tr>\n<tr>\n<td>Outlook (desktop)</td>\n<td>File -&gt; Save Calendar, format iCalendar (.ics)</td>\n</tr>\n<tr>\n<td>Outlook / Microsoft 365 (web)</td>\n<td>Settings -&gt; Calendar -&gt; Shared calendars -&gt; Publish, then use the ICS link (Pro)</td>\n</tr>\n<tr>\n<td>Anything else</td>\n<td>Any published feed URL ending in <code>.ics</code>, or <code>webcal://</code> (Pro)</td>\n</tr>\n</tbody></table>\n<p>Then: <code>ics_import {path: &quot;~/Downloads/mike@example.com.ics&quot;, name: &quot;work&quot;}</code></p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>ics_import</code></td>\n<td>Read a <code>.ics</code> file (<code>path</code>), pasted contents (<code>text</code>) or a public feed (<code>url</code>, Pro) and keep it locally under a name. Importing the same name again replaces it.</td>\n</tr>\n<tr>\n<td><code>calendars_list</code></td>\n<td>Every imported calendar: name, source, event count, size, when it was imported.</td>\n</tr>\n<tr>\n<td><code>events_list</code></td>\n<td>Every event between two dates with recurring series expanded to real occurrences, sorted, in your own time zone. Each row carries an id.</td>\n</tr>\n<tr>\n<td><code>events_search</code></td>\n<td>Events whose title, description, location, organizer or attendees contain a phrase.</td>\n</tr>\n<tr>\n<td><code>free_busy</code></td>\n<td>Merged busy blocks and the free gaps inside your working hours, day by day. Events marked free/transparent do not count as busy.</td>\n</tr>\n<tr>\n<td><code>conflicts</code></td>\n<td>Every pair of events that overlap, with the overlap in minutes -- across all calendars, so a client call clashing with a school run is caught.</td>\n</tr>\n<tr>\n<td><code>next_event</code></td>\n<td>The next thing that has not started, and how long until it does.</td>\n</tr>\n<tr>\n<td><code>event_export</code></td>\n<td>Write chosen events (by id, or a whole window) to a new <code>.ics</code> you can send. Times in UTC, so it lands correctly in any client.</td>\n</tr>\n<tr>\n<td><code>event_to_time_entry</code></td>\n<td>Turn one meeting into the exact arguments for the time-tracker&#39;s <code>entry_add</code>, so a call becomes billable time without retyping it.</td>\n</tr>\n<tr>\n<td><code>ics_forget</code></td>\n<td>Remove one calendar and its local copy.</td>\n</tr>\n<tr>\n<td><code>license_status</code> / <code>license_activate</code></td>\n<td>Show the tier; activate a Pro key offline.</td>\n</tr>\n</tbody></table>\n<p>Resource <code>calendar://today</code> gives today&#39;s schedule in one read. Prompt <code>plan_my_day</code> walks the whole loop: what is on,\nwhat clashes, where the free stretches are, and which finished meetings to bill.</p>\n<h3>What the parser handles</h3>\n<p>Written against real exports, not just the spec: line folding (CRLF, bare LF and bare CR, and folds that Exchange\nplaces inside a multi-byte character, which are rejoined on the bytes before decoding), <code>\\n \\, \\; \\\\</code> escaping,\nwhole-day events with an exclusive <code>DTEND</code>, <code>DURATION</code> instead of <code>DTEND</code>, <code>TZID</code> and UTC times, floating times,\n<code>RRULE</code> for <code>HOURLY</code>, <code>DAILY</code>, <code>WEEKLY</code>, <code>MONTHLY</code> and <code>YEARLY</code> with <code>COUNT</code>, <code>UNTIL</code> (inclusive), <code>INTERVAL</code>,\n<code>BYDAY</code> including ordinals (<code>2MO</code>, <code>-1FR</code>), <code>BYMONTHDAY</code>, <code>BYMONTH</code> and <code>BYSETPOS</code>, <code>EXDATE</code>, <code>RDATE</code>,\n<code>RECURRENCE-ID</code> overrides (the moved instance replaces the original instead of appearing twice),\n<code>STATUS:CANCELLED</code>, <code>TRANSP:TRANSPARENT</code>, and <code>VALARM</code> blocks inside an event. An event it cannot read is skipped\nand counted rather than costing you the rest of the file.</p>\n<p><code>FREQ=MINUTELY</code> and <code>FREQ=SECONDLY</code> are deliberately not expanded: one such rule fills any window with thousands of\noccurrences and buries the rest of the week. Those events are listed at their first occurrence and the reason is\nsaid out loud. A rule with no <code>COUNT</code> and no <code>UNTIL</code> is expanded only as far as the window you asked for.</p>\n<p>Exports written by this server keep whole-day events as <code>DATE</code> values, so a holiday leaves as the same day it\narrived rather than a timed block on the day before in your machine&#39;s zone.</p>\n<p><code>VTIMEZONE</code> is deliberately ignored. A file&#39;s inline DST rules are only as fresh as the app that wrote it; the <code>TZID</code>\nis kept and every offset is computed from the ICU data inside your Node build instead, which is what keeps a weekly\nWarsaw 10:00 meeting at 10:00 local across the March clock change.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro ($19 one-time)</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Calendars kept</td>\n<td>2</td>\n<td>unlimited</td>\n</tr>\n<tr>\n<td>Window per question</td>\n<td>up to 31 days</td>\n<td>any window</td>\n</tr>\n<tr>\n<td>Events per export</td>\n<td>50</td>\n<td>unlimited</td>\n</tr>\n<tr>\n<td>Import from a URL or webcal feed</td>\n<td>--</td>\n<td>yes</td>\n</tr>\n<tr>\n<td>Everything else</td>\n<td>full</td>\n<td>full</td>\n</tr>\n</tbody></table>\n<p><a href=\"https://mcp.zovo.one/buy/calendar\">Get Pro</a> -- or $39 for every server in the suite, lifetime.</p>\n<h2>Privacy</h2>\n<p>Everything is local. The calendar file you import is copied to\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/calendar/</code> and read from there; deleting that directory resets the\nserver. There is exactly one network call in the whole server, and only when you pass a <code>url</code> yourself: it fetches\nthat address once, with a 12-second timeout, a 5 MB cap, and a refusal on loopback, private and cloud-metadata\naddresses. Nothing is uploaded, no account is linked, and licence keys verify offline.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../timezone/README.md\">mcp-timezone</a> -- this server reads its time zone and <code>.ics</code> engine from it: convert a meeting time, find slots across countries, write an invite.</li>\n<li><a href=\"../time-tracker/README.md\">mcp-time-tracker</a> -- <code>event_to_time_entry</code> hands it a finished meeting; it tracks and reports the hours.</li>\n<li><a href=\"../invoice/README.md\">mcp-invoice</a> -- turn those tracked hours into a numbered invoice.</li>\n<li><a href=\"../office-suite/README.md\">office-suite</a> -- several servers behind one install, one config entry.</li>\n</ul>\n<h2>FAQ</h2>\n<p><strong>Does it connect to my Google or Outlook account?</strong>\nNo. There is no OAuth, no token and no sync. You export a file (or paste a public feed URL on Pro) and it is read\nlocally. That also means an import is a snapshot: re-import when the calendar has moved on.</p>\n<p><strong>Do recurring events work properly?</strong>\nYes, and that is most of the code. A weekly series is expanded on the wall clock, so a Warsaw 10:00 meeting stays\n10:00 through the DST change rather than drifting to 11:00. Monthly on the 31st skips February instead of rolling into\nMarch, <code>UNTIL</code> includes the occurrence that falls exactly on it, and a single moved instance replaces the original.</p>\n<p><strong>Why is my whole-day event one day shorter than I expected?</strong>\nIt is not: RFC 5545 makes <code>DTEND</code> exclusive for a whole-day event, so 1--3 June means the 1st and the 2nd. This\nserver follows the spec, which is what your calendar app does too.</p>\n<p><strong>Can it write to my calendar?</strong>\nIt writes <code>.ics</code> files with <code>event_export</code>, which you can import anywhere. It never modifies the calendar it read.</p>\n<p><strong>How big a file can it read?</strong>\n5 MB, which is a few thousand events. Export a narrower date range if a full history is bigger than that.</p>\n"},"kanban":{"title":"MCP Kanban","tagline":"A task board per project that hands off to the time tracker.","description":"Keeps tasks per project in columns (backlog, todo, doing, review, done) with due dates, estimates, priorities and tags; shows the board, overdue items and a weekly review of planned versus done and estimates versus actuals; starts a time-tracker timer from a task and records the link. Local dates in your home time zone.","html":"<h1>mcp-kanban</h1>\n<p>A task board for each of your projects, driven from your AI chat. Say &quot;add a task to the nova site board: write the\nlaunch email, due Friday, 90 minutes&quot;, then ask &quot;what&#39;s on the nova board?&quot;, &quot;what&#39;s overdue?&quot; or &quot;plan my week&quot;.\nTasks carry a title, notes, tags, a due date, an estimate, a priority and a column (backlog, todo, doing, review,\ndone by default), each one gets a short stable id like <code>NS-12</code>, and a task can hand its exact arguments to the\ntime-tracker server so the time you spend on it lands against the same project name. Everything is stored as plain\nJSON on your own machine.</p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n<p><img src=\"../../assets/kanban-logo.png\" alt=\"mcp-kanban\"></p>\n<p><strong>A local kanban board per project -- columns, due dates, estimates, weekly review -- zero setup, all local.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-kanban</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>kanban.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;kanban&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-kanban&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add kanban -- npx -y @theluckystrike/mcp-kanban\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;kanban&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-kanban&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/kanban\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/kanban/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>task_add</code></td>\n<td>Add a task: title plus optional project, column, due date, estimate, priority, tags and notes. A new project name creates its board; a partial name that matches exactly one existing project is used as that project.</td>\n</tr>\n<tr>\n<td><code>task_list</code></td>\n<td>Table of tasks, filtered by project, column, tag, <code>due_before</code> or <code>overdue</code>. Finished tasks are hidden unless you ask for them.</td>\n</tr>\n<tr>\n<td><code>task_move</code></td>\n<td>Move a task to another column on its board.</td>\n</tr>\n<tr>\n<td><code>task_update</code></td>\n<td>Change any field: title, notes, due (<code>none</code> clears it), estimate, priority, tags, column or project.</td>\n</tr>\n<tr>\n<td><code>task_done</code></td>\n<td>Mark a task done: it moves to the done column and is stamped with the time.</td>\n</tr>\n<tr>\n<td><code>task_delete</code></td>\n<td>Delete a task. The id is never reused.</td>\n</tr>\n<tr>\n<td><code>task_search</code></td>\n<td>Find tasks by text in the title, notes, tags or id.</td>\n</tr>\n<tr>\n<td><code>board</code></td>\n<td>Column-by-column summary of one board: tasks, estimate total, actual total, overdue count.</td>\n</tr>\n<tr>\n<td><code>task_start_timer</code></td>\n<td>Return the exact arguments to pass to the time-tracker server&#39;s <code>timer_start</code> for this task, and record the link on the task.</td>\n</tr>\n<tr>\n<td><code>task_log_time</code></td>\n<td>Add real minutes worked to a task, so estimate and actual can be compared.</td>\n</tr>\n<tr>\n<td><code>project_list</code></td>\n<td>Every board with open and done counts, remaining estimate and overdue count.</td>\n</tr>\n<tr>\n<td><code>overdue</code></td>\n<td>Everything past its due date, across all boards; <code>as_of</code> measures against another day.</td>\n</tr>\n<tr>\n<td><code>weekly_review</code></td>\n<td>Done versus planned for a week, with estimate against actual per project. This week is free; past weeks are Pro.</td>\n</tr>\n<tr>\n<td><code>columns_set</code></td>\n<td>Replace one board&#39;s columns (Pro). Tasks in a removed column move to the first column.</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Free or Pro, and where to upgrade.</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline).</td>\n</tr>\n</tbody></table>\n<p>Also exposed: the resource <code>kanban://today</code> (due today plus everything overdue) and the prompt <code>plan_week</code>\n(turns the open board into a day-by-day plan that fits the hours you actually have).</p>\n<h2>What you can say</h2>\n<ul>\n<li>&quot;Add a task to the nova site board: write the launch email, due Friday, 90 minutes, high priority.&quot;</li>\n<li>&quot;Move NS-3 to doing.&quot;</li>\n<li>&quot;What does the nova board look like?&quot;</li>\n<li>&quot;What&#39;s overdue?&quot;</li>\n<li>&quot;Start a timer on NS-3.&quot; -- then pass the arguments it returns to the time-tracker server.</li>\n<li>&quot;I spent 40 minutes on NS-3.&quot;</li>\n<li>&quot;Weekly review.&quot;</li>\n</ul>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Project boards</td>\n<td>3</td>\n<td>unlimited</td>\n</tr>\n<tr>\n<td>Open tasks</td>\n<td>200</td>\n<td>unlimited</td>\n</tr>\n<tr>\n<td>Columns</td>\n<td>the default five (backlog, todo, doing, review, done)</td>\n<td>custom columns per board (<code>columns_set</code>)</td>\n</tr>\n<tr>\n<td>Weekly review</td>\n<td>the current week</td>\n<td>any week, full history</td>\n</tr>\n<tr>\n<td>Estimate vs actual reports</td>\n<td>current week only</td>\n<td>every week</td>\n</tr>\n<tr>\n<td>Everything else</td>\n<td>included</td>\n<td>included</td>\n</tr>\n</tbody></table>\n<p>Pro is a one-time $19 for this server, or $39 for every server, lifetime.</p>\n<p><strong>Get Pro:</strong> <a href=\"https://mcp.zovo.one/buy/kanban\">https://mcp.zovo.one/buy/kanban</a></p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../time-tracker\">mcp-time-tracker</a> -- <code>task_start_timer</code> hands it the project and task name, so hours land against the same project.</li>\n<li><a href=\"../calendar\">mcp-calendar</a> -- due dates on the board next to what is actually in your week.</li>\n<li><a href=\"../invoice\">mcp-invoice</a> -- turn the tracked hours behind those tasks into invoice line items.</li>\n</ul>\n<h2>Privacy</h2>\n<p>All data stays local, in <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/kanban/</code>. Nothing is uploaded, no account is\nneeded, and licence keys are verified offline.</p>\n<h2>License</h2>\n<p>MIT. Support: <a href=\"mailto:support@zovo.one\">support@zovo.one</a></p>\n"},"image":{"title":"MCP Image Tools","tagline":"Resize, convert, compress and watermark images, pure JavaScript.","description":"Reads PNG, JPEG, BMP, GIF and TIFF, resizes with inside, cover or exact fit, converts formats, compresses with a byte report, crops, makes thumbnails in batches, strips metadata by re-encoding, watermarks with the shared business name or custom text, and reports dominant colours. Never edits inputs in place; refuses decompression bombs.","html":"<h1>mcp-image</h1>\n<p>Say &quot;make these five photos 1200 pixels wide&quot; or &quot;shrink this screenshot and strip the GPS out of it&quot; and it happens, on your machine, in a second. This MCP server does the small image jobs that otherwise send you to a web uploader: resize, convert between PNG, JPEG, BMP, GIF and TIFF, compress with a real before-and-after byte count, crop, thumbnail a folder, watermark with your business name, and drop the EXIF block a phone camera writes into every photo. No upload, no account, no native dependency, no image editor.</p>\n<p><strong>The image chores of a freelance business, done from chat instead of from a browser tab you would rather not hand a client photo to.</strong></p>\n<h2>60-second install</h2>\n<p>npm publish for <code>@theluckystrike/mcp-image</code> is pending. Until then, the <code>.mcpb</code> one-click bundle or a clone+build\nis the working path -- both are verified below.</p>\n<p><strong>One-click (.mcpb):</strong> download <code>image.mcpb</code> from the latest release and double-click it in Claude Desktop:\n<a href=\"https://github.com/theluckystrike/mcp-servers/releases/latest\">https://github.com/theluckystrike/mcp-servers/releases/latest</a></p>\n<p><strong>Claude Desktop</strong> (<code>claude_desktop_config.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;image&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-image&quot;]\n    }\n  }\n}\n</code></pre>\n<p><strong>Claude Code:</strong></p>\n<pre><code class=\"language-sh\">claude mcp add image -- npx -y @theluckystrike/mcp-image\n</code></pre>\n<p><strong>Cursor</strong> (<code>.cursor/mcp.json</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;mcpServers&quot;: {\n    &quot;image&quot;: {\n      &quot;command&quot;: &quot;npx&quot;,\n      &quot;args&quot;: [&quot;-y&quot;, &quot;@theluckystrike/mcp-image&quot;]\n    }\n  }\n}\n</code></pre>\n<p>The <code>npx</code> form above starts working the moment the package is published. Until then, use the .mcpb bundle above, or\nbuild from source with exactly these three commands:</p>\n<pre><code class=\"language-sh\">git clone https://github.com/theluckystrike/mcp-servers.git &amp;&amp; cd mcp-servers\nnpm install\nnpm run build -w packages/mcp-license -w servers/image\n</code></pre>\n<p>Then point your client&#39;s <code>command</code> at <code>node</code> with one arg: the absolute path to <code>servers/image/dist/index.js</code>.</p>\n<p>To run in Pro mode set <code>MCP_LICENSE_KEY</code> in the same config block, or call <code>license_activate</code> once with your key.</p>\n<h2>Tools</h2>\n<table>\n<thead>\n<tr>\n<th>Tool</th>\n<th>What it does</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>image_info</code></td>\n<td>Format, pixel dimensions, megapixels, aspect ratio, file size in bytes, whether the image carries an alpha channel, and the bytes it spends per pixel</td>\n</tr>\n<tr>\n<td><code>image_resize</code></td>\n<td>A resized copy. <code>inside</code> fits within the box and keeps the ratio, <code>cover</code> fills the box and crops the overflow, <code>exact</code> stretches. Give one of width or height and the other follows</td>\n</tr>\n<tr>\n<td><code>image_convert</code></td>\n<td>Re-encode as PNG, JPEG, BMP, GIF or TIFF. Transparency going to JPEG is flattened onto white, and the answer says so</td>\n</tr>\n<tr>\n<td><code>image_compress</code></td>\n<td>Smaller bytes, with the count before and after and the percentage saved. <code>quality</code> (default 80) and <code>max_width</code></td>\n</tr>\n<tr>\n<td><code>image_crop</code></td>\n<td>Cut a rectangle: <code>x</code>, <code>y</code> from the top-left, <code>width</code>, <code>height</code>. A rectangle past an edge is refused with the real size, never clamped</td>\n</tr>\n<tr>\n<td><code>image_thumbnails</code></td>\n<td>One thumbnail per file into <code>out_dir</code>, each fitting inside <code>size</code> by <code>size</code> and keeping its aspect ratio</td>\n</tr>\n<tr>\n<td><code>image_watermark</code></td>\n<td>Text over the image at a corner and an opacity, white on a translucent plate. With no text, your business name from the shared profile <a href=\"../invoice\">mcp-invoice</a> and <a href=\"../docx\">mcp-docx</a> write</td>\n</tr>\n<tr>\n<td><code>image_strip_metadata</code></td>\n<td>A copy with pixels and nothing else: EXIF, GPS, camera, capture time, XMP and colour profiles are not carried across</td>\n</tr>\n<tr>\n<td><code>image_batch_resize</code></td>\n<td>Resize a whole list into <code>out_dir</code>, each keeping its own aspect ratio, named <code>&lt;name&gt;-&lt;W&gt;x&lt;H&gt;.&lt;ext&gt;</code></td>\n</tr>\n<tr>\n<td><code>image_dominant_colors</code></td>\n<td>The colours that cover most of an image, as hex codes with the share of pixels each covers</td>\n</tr>\n<tr>\n<td><code>license_status</code></td>\n<td>Show free or Pro mode</td>\n</tr>\n<tr>\n<td><code>license_activate</code></td>\n<td>Activate a Pro key (verified offline)</td>\n</tr>\n</tbody></table>\n<p>Resource: <code>image://recent</code> returns the last 25 operations, newest first, with the inputs, the files written and when.\nPrompt: <code>prepare_for_web</code> takes a camera-sized image down to a page-sized JPEG, drops the metadata that came with it, and reports the bytes saved.</p>\n<h2>What you can say</h2>\n<table>\n<thead>\n<tr>\n<th>You say</th>\n<th>Tool</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>&quot;How big is this image, and does it have transparency?&quot;</td>\n<td><code>image_info</code></td>\n</tr>\n<tr>\n<td>&quot;Make this 1200 pixels wide.&quot;</td>\n<td><code>image_resize</code></td>\n</tr>\n<tr>\n<td>&quot;Turn this PNG into a JPEG.&quot;</td>\n<td><code>image_convert</code></td>\n</tr>\n<tr>\n<td>&quot;This photo is 8 MB, get it under a megabyte.&quot;</td>\n<td><code>image_compress</code></td>\n</tr>\n<tr>\n<td>&quot;Crop the top banner out of this screenshot.&quot;</td>\n<td><code>image_crop</code></td>\n</tr>\n<tr>\n<td>&quot;Make thumbnails of everything in this folder.&quot;</td>\n<td><code>image_thumbnails</code></td>\n</tr>\n<tr>\n<td>&quot;Put my company name in the corner of this photo.&quot;</td>\n<td><code>image_watermark</code></td>\n</tr>\n<tr>\n<td>&quot;Strip the GPS coordinates before I send this.&quot;</td>\n<td><code>image_strip_metadata</code></td>\n</tr>\n<tr>\n<td>&quot;Resize all of these to 1600 wide.&quot;</td>\n<td><code>image_batch_resize</code></td>\n</tr>\n<tr>\n<td>&quot;What colours is this logo built from?&quot;</td>\n<td><code>image_dominant_colors</code></td>\n</tr>\n<tr>\n<td>&quot;Get this ready for the website.&quot;</td>\n<td><code>prepare_for_web</code> prompt</td>\n</tr>\n</tbody></table>\n<h2>Worked example</h2>\n<pre><code>You: This photo is too big for the site. Get it to 1600 wide and drop the metadata.\n\n  image_info { path: &quot;~/photos/DSC_0491.jpg&quot; }\n  -&gt; 6000x4000, 24 MP, JPEG, 8.4 MB, no alpha\n\n  image_compress {\n    path: &quot;~/photos/DSC_0491.jpg&quot;,\n    max_width: 1600,\n    quality: 80,\n    out_path: &quot;~/photos/DSC_0491-web.jpg&quot;\n  }\n  -&gt; 8.4 MB -&gt; 412.6 KB (8809124 -&gt; 422500 bytes, -95.2%)\n  -&gt; Method: JPEG quality 80 and a resize to 1600 px wide\n  -&gt; ~/photos/DSC_0491-web.jpg, 1600x1067, JPEG\n</code></pre>\n<p>The JPEG re-encode also drops the EXIF block the camera wrote, including the GPS coordinates. The original file is\nbyte-for-byte unchanged.</p>\n<h2>Free vs Pro</h2>\n<table>\n<thead>\n<tr>\n<th></th>\n<th>Free</th>\n<th>Pro</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>image_info</code></td>\n<td>Unlimited, at any size</td>\n<td>Unlimited</td>\n</tr>\n<tr>\n<td><code>image_resize</code>, <code>image_convert</code>, <code>image_compress</code>, <code>image_crop</code>, <code>image_strip_metadata</code></td>\n<td>Sources up to 4 MP (2000x2000 and a bit)</td>\n<td>Any size, up to the 50 MB / 10,000 px input caps</td>\n</tr>\n<tr>\n<td><code>image_thumbnails</code>, <code>image_batch_resize</code></td>\n<td>Up to 5 files per call, sources up to 4 MP</td>\n<td>Any number of files, any size</td>\n</tr>\n<tr>\n<td><code>image_watermark</code></td>\n<td>Your business name from the shared profile</td>\n<td>Any text you pass</td>\n</tr>\n<tr>\n<td><code>image_dominant_colors</code></td>\n<td>-</td>\n<td>Yes</td>\n</tr>\n</tbody></table>\n<p>A tier limit is an answer, not an error, and nothing is written when one refuses a call.</p>\n<p>Pro is a one-time $19, or $39 for every server in the collection, lifetime.</p>\n<p><strong>Get Pro: <a href=\"https://mcp.zovo.one/buy/image\">https://mcp.zovo.one/buy/image</a></strong></p>\n<h2><code>quality</code> is a JPEG parameter, and only a JPEG parameter</h2>\n<p>This is the one thing about image compression that surprises people, so the server says it in its own answer rather\nthan accepting a number and quietly doing nothing with it. A JPEG throws away detail to get smaller, and <code>quality</code>\nis the dial for how much. A PNG is lossless: there is no dial. Passing <code>quality: 40</code> to a PNG output does not make\na PNG 60% smaller, it makes exactly the same PNG.</p>\n<p>So the output format follows the extension of your <code>out_path</code>, and that is the lever:</p>\n<ul>\n<li><code>out_path: &quot;shot-small.jpg&quot;</code> -- <code>quality</code> applies, and a photo typically drops 80-95%.</li>\n<li><code>out_path: &quot;shot-small.png&quot;</code> -- <code>quality</code> is reported as not applicable, and only <code>max_width</code> removes bytes.</li>\n</ul>\n<p>Palette quantisation was measured as an alternative for PNG and rejected: re-encoding a 300x220 noisy PNG through a\n16-colour quantiser produced a <strong>larger</strong> file (115,451 bytes against 39,262), because the encoder here writes RGBA\neither way and quantising only destroys the row-to-row similarity that the deflate step was exploiting. A tool that\nclaims to compress and returns a bigger file is worse than a tool that says which knob exists.</p>\n<p><code>image_compress</code> therefore always reports the byte count before and after, the percentage, and the method that did\nthe work -- and, if the output came out larger, it says so and tells you to keep the original.</p>\n<h2>Decompression bombs are refused from the header, before anything decodes</h2>\n<p>A 4 KB PNG can declare that it is 20,000 by 20,000 pixels. Decoding it allocates 1.6 GB of RGBA and takes the\nprocess down before any size check written after the decode could ever run. So the declared dimensions are read out\nof the container header -- PNG IHDR, the JPEG SOF segment, the GIF logical screen descriptor, the BMP info header,\nthe first TIFF IFD -- and a file over 10,000 px on a side is refused there, with the memory it would have taken\nnamed in the message:</p>\n<pre><code>Error: /path/bomb.png declares 20000x20000 pixels in its PNG header and was refused before decoding:\nthis server caps a side at 10000 px. A small file that declares an enormous canvas is a decompression\nbomb - decoding it would allocate 1526 MB of RGBA. Nothing was decoded.\n</code></pre>\n<p>The decoded size is checked again afterwards, because a header probe can come back empty on an unusual TIFF, and a\nguard that only sometimes runs is not a guard.</p>\n<h2>Existing files are never overwritten, and inputs are never modified</h2>\n<p>Every tool writes a new file and leaves its inputs byte-for-byte alone -- there is no in-place mode, on purpose.\nAn <code>out_path</code> that already exists is refused:</p>\n<pre><code>Error: /path/shot-small.png already exists and nothing was written.\nPass overwrite: true to replace it, or give a different out_path.\n</code></pre>\n<p>The path is reserved with an exclusive create, not an existence check, so two processes writing the same <code>out_path</code>\nat the same time cannot clobber each other: one wins, the other is refused and writes nothing. <code>image_thumbnails</code>\nand <code>image_batch_resize</code> reserve every one of their output paths before writing any of them, so a collision on file\n3 does not leave files 1 and 2 behind as a half-done batch -- and the reservations are released, so nothing empty is\nleft on disk either.</p>\n<p>Pass <code>overwrite: true</code> when replacing the file is what you want.</p>\n<p><code>overwrite: true</code> still does not let an output be an input. Writing a result back over one of its own sources\ndestroys that source -- the pixels are already decoded in memory and get written over the file they came from, so a\n4000 px original becomes the 512 px thumbnail and every later read of that path is quietly wrong. So an <code>out_path</code>\nthat resolves to (or shares an inode with) any input of the same call is refused before any work happens.</p>\n<h2>What &quot;strip metadata&quot; actually does</h2>\n<p><code>image_strip_metadata</code> decodes the image and re-encodes it from the raw pixels. EXIF, GPS coordinates, the camera\nand lens, the capture time, XMP packets and embedded colour profiles are not removed one by one -- they are simply\nnever handed to the encoder, so they cannot come out the other side. Two consequences worth knowing:</p>\n<ul>\n<li>The pixels are the same, the bytes are not. A JPEG goes through the encoder a second time, so the copy is not\nbit-identical to the original even though it looks the same. Keep the original if that matters.</li>\n<li>Every other writing tool here has the same effect as a side effect. Resizing a photo also drops its EXIF.</li>\n</ul>\n<h2>Limits</h2>\n<ul>\n<li>Inputs over 50 MB are refused: an image decodes to width x height x 4 bytes of raw RGBA regardless of how well it\nis compressed on disk.</li>\n<li>10,000 px per side, checked from the header first and from the decode second.</li>\n<li>Read and written: PNG, JPEG, BMP, GIF and TIFF, detected by magic bytes rather than by file extension. No WebP,\nno AVIF, no HEIC, no SVG -- there is no pure-JavaScript decoder for those that is worth shipping, and this server\ntakes no native dependency.</li>\n<li>An animated GIF is read as its first frame. This server does not do animation.</li>\n<li>Watermark text is drawn with the bundled Open Sans bitmap faces (8, 16, 32, 64 and 128 px), and the largest one\nthat fits the image is chosen. A character those faces do not carry is not drawn, so there is no CJK watermark.</li>\n<li>No rotation, no filters, no colour correction, no OCR, no background removal. This server does the size-and-format\njobs; it is not an image editor.</li>\n</ul>\n<h2>How it stores data</h2>\n<p>Only a register of what it did: <code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/image/operations.json</code>, the last 500\noperations, as plain JSON. Your images stay where you put them. Every write to the register runs inside an advisory\nlock on <code>.../image/.lock</code>, so two clients on one data directory cannot lose a record, and saves go to a temporary\nfile and are renamed into place.</p>\n<p>If the register is unreadable or not valid JSON it is never treated as &quot;empty&quot;: it is moved aside byte-for-byte as\n<code>operations.json.corrupt-&lt;timestamp&gt;</code> with a marker beside it. The image you asked for is still written -- the file\nis on disk before the register is touched -- and the answer tells you the history could not be updated.</p>\n<p>The watermark reads the one shared profile the whole suite uses,\n<code>${XDG_DATA_HOME:-~/.local/share}/mcp-servers/profile/business.json</code>, written by <code>business_set</code> in mcp-invoice or\nmcp-docx. Nothing is invented: with no name stored and no text passed, <code>image_watermark</code> refuses and says which tool\nto run.</p>\n<h2>Privacy</h2>\n<p>All data stays local. The server reads and writes files on your machine and makes no network request of any kind --\nnot for licensing (keys are verified offline), not for fonts, not for telemetry. A client photo you resize is never\nuploaded anywhere, which is the entire reason this exists.</p>\n<h2>Pairs with</h2>\n<ul>\n<li><a href=\"../docx/README.md\">mcp-docx</a> -- size a logo here, then put it in the letterhead: <code>image_resize</code> to the header\nheight, then the <code>.docx</code> writer picks the file up from disk.</li>\n<li><a href=\"../pdf/README.md\">mcp-pdf</a> -- shrink the scans before you merge them, so the joined PDF is not 40 MB.</li>\n<li><a href=\"../resume/README.md\">mcp-resume</a> -- one correctly sized, metadata-free headshot for the CV and the application form.</li>\n<li><a href=\"../office-suite/README.md\">office-suite</a> -- several servers behind one install, one config entry.</li>\n</ul>\n<h2>Troubleshooting</h2>\n<ul>\n<li><strong><code>npx</code> hangs or fails to find the package</strong>: npm publish for this package is pending. Use the <code>.mcpb</code> bundle or\nthe clone-and-build path above until it lands.</li>\n<li><strong>&quot;quality does not apply to a PNG output&quot;</strong>: write to a <code>.jpg</code> <code>out_path</code>, or pass <code>max_width</code>. See the section\nabove.</li>\n<li><strong>&quot;declares NxN pixels ... and was refused before decoding&quot;</strong>: the file claims a canvas over 10,000 px per side.\nIf it is genuinely that large, resize it with a tool that streams tiles; this server decodes whole images.</li>\n<li><strong>&quot;does not start with the magic bytes&quot;</strong>: the file is not a PNG, JPEG, BMP, GIF or TIFF. A <code>.png</code> name on a WebP\nfile is the usual cause; convert it first.</li>\n<li><strong>&quot;already exists and nothing was written&quot;</strong>: pass <code>overwrite: true</code>, or a different <code>out_path</code>.</li>\n<li><strong>The watermark is tiny</strong>: the largest bundled face is 128 px, and it is chosen to fit within 80% of the image\nwidth. On a very large photo, resize first and watermark second.</li>\n<li><strong>Node version</strong>: requires Node &gt;= 18. Check with <code>node -v</code>.</li>\n</ul>\n<p>MIT licensed. Support: <a href=\"mailto:support@zovo.one\">support@zovo.one</a></p>\n<p>Built by <a href=\"https://github.com/theluckystrike\">theluckystrike</a>.</p>\n"}};
