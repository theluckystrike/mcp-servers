# Release v0.17.0 (2026-09-06)

status: done
evidence: petty-cash joins as the twenty-seventh server (imprest float; replenishment is imprest minus balance, never the voucher sum; a count is a fact that moves the book balance; audit 16/18 with a date-guard fix; stdio round 33 14/18); recoverable free slots across nine servers: a byte-identical record is refused by name before the cap and ten free delete tools recover a slot; every gate text now states that any total or journal composed outside the free tools is an estimate (the earlier imperative wording was read by a client as a prompt injection); amortization hosted round 15/18; intel round 12 found five more empty slots; 29 package.json and 70 ranges at 0.17.0; 102 manifests bumped, 74 reference a bundle; release-check green on 27 servers; npm test, 28 bundles, boot-check, GitHub release, sha256, publish, mirrors and by-name verify recorded at loop close.
artifacts: https://github.com/theluckystrike/mcp-servers/releases/tag/v0.17.0
cost: by hand in bounded steps.
failures: none open at cut time. GSC unmeasurable: key file iCloud-dataless.
insight: text inside a tool result that reads as a command to the model is treated as hostile; the same protection stated as a fact about the data ("figures outside these tools are estimates") is something a client can act on without being told what to do.
