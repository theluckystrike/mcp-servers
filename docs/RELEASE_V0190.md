# Release v0.19.0 (2026-09-06)

status: done
evidence: catalogue joins as the twenty-ninth server (SKUs with per-currency, per-tier, valid-from price rows; a labour rate card; lines_resolve builds both the invoice and the quote payload itself because the two engines take prices in different scales, a 100x gap asserted exactly by re-deriving the net from each payload's own items; audit 18/18; stdio round 37 14/18, the miss being an undated question answered against the wrong valid-from row; hosted on mcp.zovo.one/mcp/catalogue as the twenty-ninth endpoint with the ladder, the scale and both downloads verified live on one Pro key); checkout for catalogue and work-order both stay named gaps because the keychain Stripe key lacks product write, /buy answers 503 with the bundle link; release-check gained a per-server dist-version check and build-mcpb a stale-dist guard, both after v0.18.0 shipped a kanban bundle from a stale build; work-order hosted round 16/18; intel round 13 found five more empty slots led by change-order; 30 package.json and 80 ranges at 0.19.0; 110 manifests bumped, 80 reference a bundle; release-check green with two named gaps on 29 servers; npm test 1455 pass, 0 fail, 11 skipped of 1466 (data/tests.json); 30 bundles, no stale dist; boot-check catalogue 12 tools, invoice 13, office-suite 273, all serverInfo 0.19.0; GitHub release with 30 assets; sha256 written into 80 manifests, catalogue digest equal to the release asset digest; registry publish, mirrors and by-name verify recorded at loop close.
artifacts: https://github.com/theluckystrike/mcp-servers/releases/tag/v0.19.0
cost: by hand in bounded steps; one chain interrupted at the bundle step and resumed from the bundle step only.
failures: two checkouts human-gated on the Stripe key (docs/HUMAN_GATED_PACK.md). GSC unmeasurable: key file iCloud-dataless.
insight: two sibling engines that accept the same field name in different units cannot detect a swap; the one place that knows both shapes must emit both payloads and print the scale next to each.
