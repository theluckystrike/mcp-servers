# Release v0.13.0 (2026-09-05)

status: done
evidence: asset-register joins as the twenty-third server (Polish KST, UK capital allowance and US MACRS tables bundled with provenance; monthly journals; disposals; audit 18/18; stdio round 24 recorded); every cap message on every server ends with the twenty-three-server bundle link at $39 carrying src=<product>.<tool>.bundle, and the instrument recorded the first bundle-sourced click; 25 package.json and 49 ranges at 0.13.0; 86 manifests bumped, 62 reference a bundle; release-check green on 23 servers; npm test 1069 tests, 1056 pass, 0 fail after two fixes (invoice PROFILE_READERS gained asset-register; the per-diem contract now accepts a real sha after its first release); 24 bundles (invoice and office-suite rebuilt after the reader fix), three boot-checked at 0.13.0; GitHub release with 24 assets; sha256 into 62 manifests; registry 61 of 62 on the first pass and the 62nd a confirmed duplicate; mirrors and by-name verify running in the background at loop close.
artifacts: https://github.com/theluckystrike/mcp-servers/releases/tag/v0.13.0
cost: by hand in bounded steps, about 50 minutes wall at load 12 to 22; the wiring agent stalled on its final doc, verified and finished by the orchestrator.
failures: none open. GSC unmeasurable: the key file is iCloud-dataless again.
insight: two tests broke for the same reason in two consecutive releases: a constant that must grow with the server count (the invoice reader list) and a contract that was true only before the first release (fileSha256 is TBD). Both are now caught by tests that state the rule rather than the moment.
