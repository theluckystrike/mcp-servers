# Release v0.20.0 (2026-09-06)

status: done
evidence: change-order joins as the thirtieth server (added, removed and changed lines against a quote or work-order reference with reason and date; running contract value counting approved deltas only, pending shown separately; approval flow draft, sent, approved, rejected, void; a changed line emitted as a reversal plus a revised item, never one net item; the invoice-ready delta payload in both scales with the 100x quotient re-derived from each payload's own items, 117,012 against 11,701,200; 47 tests at build); SERVER_COUNT 30 in both licence files, bundle saving $531; hosted catalogue round 38 scored 18/18 with the undated question answered from the in_force block both times; D-R97 and D-R98 fixed and deployed (hosted price list tool says HTML, PDF names the booked later row and no longer repeats the issuer); every SPEC.md regenerated without an em dash; checkout for change-order joins work-order and catalogue as a named PENDING_HUMAN gap; wiring round 23 closed 20/20 (guide, GIF, logo, Docker repin to 8f37031, Cline issue 2461, /buy 503 live); hosted on mcp.zovo.one/mcp/change-order as the thirtieth endpoint, re-vendored after the audit and the below-zero refusal verified live; audit 16/18 shipped, 18/18 after D-R99 (a removal cannot take the contract below zero, checked at the line and again at approval) and D-R100 (money in item descriptions formatted with its code, not a bare minor figure); stdio round 39 11/18 with four client-side classes (invented gross value, false nothing-written, hand-built Pro payload, netted changed line) and the server clean on every figure; 32 package.json and 84 ranges at 0.20.0; 114 manifests bumped, 83 reference a bundle; release-check green with three named gaps on 30 servers; npm test 1507 pass, 0 fail, 11 skipped of 1518; 31 bundles, no stale dist; boot-check change-order 11 tools, invoice 13, office-suite 282, all serverInfo 0.20.0; GitHub release with 31 assets; sha256 into 83 manifests, change-order digest equal to the release asset digest; registry publish, mirrors and by-name verify recorded at loop close.
artifacts: https://github.com/theluckystrike/mcp-servers/releases/tag/v0.20.0
cost: by hand in bounded steps.
failures: three checkouts human-gated on the Stripe key (docs/HUMAN_GATED_PACK.md). GSC unmeasurable: key file iCloud-dataless.
insight: a change order that nets a changed line into one figure destroys the audit trail the document exists to keep; reversal plus revised line is the only shape both engines and an auditor can read.
