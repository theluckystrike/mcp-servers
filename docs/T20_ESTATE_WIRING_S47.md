# T20 Estate Wiring — servers 'leave' and 'onboarding'

STATUS: in progress

## Goal
Wire servers `leave` and `onboarding` into the full estate so `node scripts/release-check.mjs` is green for both.

## Evidence Log
