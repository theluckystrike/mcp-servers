# Release v0.23.0 (2026-09-19)

status: done
evidence: sprint 46 shipped three crawled-visible changes and no release doc existed for them, which is why sitemap lastmod stayed at 2026-09-17 while the pages themselves changed: (1) T13 social meta, twitter:card summary + twitter:title + twitter:description emitted from the shared page() helper so all 193 sitemap URLs carry them, plus Organization and WebSite JSON-LD on the homepage, SearchAction correctly skipped because no real search route exists, live-verified with curl on production (twitter:card present on / and /s/invoice, both JSON-LD blocks present); (2) T15 fix 3, sitewide footer hub links, every page() render now links Home, Guides, Setup, Compare, Changelog so crawlers hitting a deep guide page are one hop from the section hubs; (3) the honesty gate number on the homepage was reconciled with the tests actually on disk (148 unit tests green, verified live). tests: billing test runner reported 158 pass 0 fail before and after; remote 141/141; validation 1192/1192 unchanged. deploys: 71ae9dbd (footer links), 66af2e1c (honesty gate), both live on mcp.zovo.one.
artifacts: no new .mcpb assets; this release is site-surface only, no server binaries changed, manifests and bundles untouched at v0.22.0.
cost: one session, mostly absorbed inside the sprint 46 delegation fan-out; the release doc and pages rebuild are the only new steps this loop, about 10 minutes wall.
failures: none. the T14 leaf hit its iteration cap with two TODO sections and the ledger update unwritten; the orchestrator finished those from the leaf's own curl evidence, no re-run needed.
insight: sitemap lastmod is downstream of the release doc, not of the deploy. three shipped, production-verified changes moved nothing a crawler schedules by because the freshness signal comes from docs/RELEASE_V*.md; a release doc is part of shipping, not bookkeeping after shipping.
