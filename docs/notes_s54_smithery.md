# Smithery publish check
Login blocked (GitHub OAuth password entry not automatable; user AFK).
Workaround options documented:
1. smithery.ai "Add via URL" (dashboard) — also behind same auth.
2. Once any user session logs into smithery.ai, all 46 servers with smithery.yaml (local stdio, typescript runtime) are publishable via `npx @smithery/cli publish` per directory — prepped and unblocked.
Conclusion: park smithery until user is present 2 min. Move to next lane.
