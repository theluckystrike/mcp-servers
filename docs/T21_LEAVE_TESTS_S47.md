# T21_LEAVE_TESTS_S47 — Port servers/leave test suite to leave_* tools

STATUS: in progress

## Goal
Port test/ (6 .mjs files, 60 tests) from checklist_* tool names to leave_* tools until `npm test` reports # fail 0. GATE: `# tests / # pass / # fail` lines.

## Evidence

(TBD)

## Result
STATUS: in progress