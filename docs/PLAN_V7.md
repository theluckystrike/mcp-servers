# Plan v7 (2026-09-04, loop 10): guardrails free, one more server, findability

## Signals at the start
| Signal | Value | Read |
|---|---|---|
| Bundle downloads | 843 (703 at loop 9 start) | +140 in one loop, catalog-driven |
| Sales | 0 | Free tier delivers value; nothing converts yet |
| GSC | sitemap 179 fetched 01:41Z, 0 indexed; home and /s/quotes "unknown to Google" | Crawl has not started; zovo.one link live since 09:20 |
| Registry findable share | 41% of tracked tokens, target 60 | Empty tokens remain where the fleet holds no name |
| Round 11 | 49/54 hosted; D-R55: a Pro gate on recurring_detect made the model hand-compute the wrong annualisation | A gated guardrail is worse than no tool |
| Next slots | epub 6 results (fit 0.4), barcode 9 (fit 0.5, pure TS), zip 14, payroll 8 (tax tables) | Barcode pairs with invoice through SEPA EPC payment QR codes |

## Top 5, ranked by impact x autonomy
1. Guardrail tools free across all 17 servers: every free-tier gate that hides an analysis or a check (recurring_detect, dominant colours, reconcile, overdue, conflicts, win rate) becomes free with a row or count cap, Pro keeps volume and exports. Pass: no gated analysis tool remains; tests; validate green.
2. Build `barcode`: QR codes (SEPA EPC payment QR for an invoice, URL, vCard, WiFi, plain text) and Code128 / EAN-13 barcodes as SVG and PNG, pure TypeScript, invoice integration (invoice_payment_qr reads the shared business profile IBAN). Audit, wire, host, content, catalogs.
3. Registry findable share toward 60: honest variants on empty tokens for servers that still have one name; re-probe.
4. Round 12 through the hosted path on quotes, calendar and pdf. Pass: scored scenarios, seam fixes with tests.
5. Storefront home leading with connect-by-URL and the seventeen-server bundle; hype grep zero; IndexNow.
Release v0.7.1 or v0.8.0 (barcode) only if sources change; validation, measure, KPIs, sprint log, dashboard, memory, sound at the end.
