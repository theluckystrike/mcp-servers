1. [servers/expense-tracker/src/money.ts:61](/Users/mike/mcp-servers/servers/expense-tracker/src/money.ts:61) — P1 — Rounding net instead of the VAT component loses VAT on half-cent cases. Fix: calculate `vat = roundHalfUp(gross * rate / (100 + rate))`, then `net = gross - vat`. Trigger: `expense_add {amount:0.03,currency:"EUR",vat_rate:20}` reports VAT `EUR 0.00` instead of `EUR 0.01`.

2. [servers/expense-tracker/src/money.ts:14](/Users/mike/mcp-servers/servers/expense-tracker/src/money.ts:14), [servers/invoice/src/money.ts:16](/Users/mike/mcp-servers/servers/invoice/src/money.ts:16) — P1 — Every non-zero-decimal currency is treated as two-decimal, corrupting currencies with three or four decimals. Fix: use an ISO 4217 minor-unit table covering 0, 2, 3, and 4 decimals. Trigger: `expense_add {amount:1.234,currency:"BHD"}` stores 123 minor units and displays `BHD 1.23` instead of 1234 and `BHD 1.234`.

3. [servers/expense-tracker/src/money.ts:24](/Users/mike/mcp-servers/servers/expense-tracker/src/money.ts:24), [servers/invoice/src/money.ts:26](/Users/mike/mcp-servers/servers/invoice/src/money.ts:26) — P3 — Adding `1e-9` changes values genuinely below a half boundary into round-ups. Fix: use decimal arithmetic or an error bound based on `Number.EPSILON`. Trigger: `toMinor(0.004999999995,"EUR")` returns 1 instead of 0.

4. [servers/expense-tracker/src/index.ts:425](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:425) — P1 — User-controlled group names index normal objects, so prototype keys vanish from totals and mutate shared prototypes. Fix: use `Map` or `Object.create(null)` for both grouping levels. Trigger: add an expense with `category:"__proto__"`, then call `expense_summary {group_by:"category"}`; the expense is omitted.

5. [servers/expense-tracker/src/money.ts:71](/Users/mike/mcp-servers/servers/expense-tracker/src/money.ts:71), [servers/expense-tracker/src/index.ts:499](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:499) — P1 — Rates have no effective dates: the US table charges $0.70/mile for every date, while the 2026 rate is $0.725 through June and $0.76 from July. Fix: select the rate by trip date and preserve the applied schedule version. Trigger: `mileage_add {miles:100,region:"US",date:"2026-09-02",purpose:"client"}` records `$70` instead of `$76`. [IRS rate table](https://www.irs.gov/tax-professionals/standard-mileage-rates?nav=2)

6. [servers/expense-tracker/src/money.ts:73](/Users/mike/mcp-servers/servers/expense-tracker/src/money.ts:73) — P1 — The UK rate is fixed at £0.45 and cannot apply the £0.55 first-10,000-mile rate or £0.25 rate above the threshold. Fix: store tax-year mileage and calculate each claim across the threshold. Trigger: `mileage_add {miles:1,region:"UK",date:"2026-09-02",purpose:"client"}` records £0.45 instead of £0.55. [HMRC rates](https://www.gov.uk/tax-relief-for-employees/vehicles-you-use-for-work)

7. [servers/expense-tracker/src/money.ts:72](/Users/mike/mcp-servers/servers/expense-tracker/src/money.ts:72) — P1 — Poland always receives PLN 1.15/km although the limit is PLN 0.89 for cars up to 900 cm³ and PLN 1.15 above 900 cm³. Fix: require vehicle type and engine class before selecting the table rate. Trigger: `mileage_add {km:100,region:"PL",purpose:"client"}` for an 800 cm³ car records PLN 115 instead of PLN 89. [Polish regulation](https://eli.gov.pl/api/acts/DU/2023/5/text.html)

8. [servers/expense-tracker/src/index.ts:501](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:501) — P1 — A caller can replace a table rate’s currency without converting the rate. Fix: reject `currency` unless `rate_per_km` is supplied, otherwise force `table.currency`. Trigger: `mileage_add {km:100,region:"PL",currency:"EUR",purpose:"client"}` records EUR 115 from a PLN 1.15/km rate.

9. [servers/expense-tracker/src/index.ts:653](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:653) — P1 — An explicit zero VAT rate is treated as unknown and replaced by the current default. Fix: define a known rate as any finite stored number, including zero. Trigger: set default VAT to 23, add `amount:123, vat_rate:0, billable:true`, then run `expense_to_invoice`; it emits `unit_price:100,tax_rate:23`.

10. [servers/expense-tracker/src/index.ts:650](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:650) — P1 — Changing the VAT default rewrites the tax meaning of expenses recorded before that default existed. Fix: apply defaults only when inserting an expense and never infer VAT during rebilling. Trigger: add a EUR 120 expense without VAT, later set `default_vat_rate:20`, then rebill; the tool invents EUR 20 VAT.

11. [servers/expense-tracker/src/index.ts:656](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:656), [servers/invoice/src/money.ts:102](/Users/mike/mcp-servers/servers/invoice/src/money.ts:102) — P1 — Net-plus-rate output cannot always reproduce the stored gross because the invoice rounds tax again. Fix: support VAT-inclusive invoice lines or carry an explicit tax/rounding adjustment. Trigger: rebill EUR 0.03 at 20%; the expense split emits net EUR 0.03 and the invoice computes EUR 0.01 tax, charging EUR 0.04.

12. [servers/expense-tracker/src/index.ts:674](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:674) — P1 — `mark_rebilled:true` still stamps expenses before an invoice exists. Fix: remove this option and permit marking only through `expense_mark_rebilled` with an invoice identifier. Trigger: call `expense_to_invoice {...,mark_rebilled:true}` and let `invoice_create` fail; the expense disappears from later rebill previews.

13. [servers/expense-tracker/src/index.ts:680](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:680), [servers/expense-tracker/src/index.ts:737](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:737) — P1 — The prescribed range-based marking is not scoped to a currency group or preview snapshot. Fix: return expense IDs per currency and require those IDs when marking each completed invoice. Trigger: preview one EUR and one PLN expense, invoice only the EUR group, then follow `next_step`; both expenses are marked rebilled.

14. [servers/expense-tracker/src/index.ts:290](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:290) — P2 — Clearing `rebilled_at` leaves `rebilled_invoice`, allowing the next rebill to retain the prior invoice number. Fix: delete both fields when `rebilled:false`. Trigger: mark an expense with `INV-1`, clear `rebilled`, then mark it without `invoice_number`; the store still links it to `INV-1`.

15. [servers/expense-tracker/src/index.ts:282](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:282) — P1 — Amount, currency, and VAT remain editable after rebilling without invalidating the invoice link. Fix: reject money-field edits on rebilled expenses or clear both rebill fields atomically. Trigger: mark a EUR 100 expense on `INV-1`, then call `expense_update {id,amount:200}`; it remains linked to a EUR 100 invoice.

16. [servers/expense-tracker/src/index.ts:731](/Users/mike/mcp-servers/servers/expense-tracker/src/index.ts:731) — P2 — ID-based marking can overwrite an existing invoice association. Fix: reject IDs with `rebilled_at` unless an explicit reassignment operation is used. Trigger: mark ID `x` with `INV-1`, then call `expense_mark_rebilled {ids:["x"],invoice_number:"INV-2"}`.

17. [servers/office-suite/src/index.ts:143](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:143) — P2 — Collision prefixing can overwrite a real tool already using the generated name. Fix: build the complete inventory first, reserve every name, and verify uniqueness before publishing. Trigger: child `a` exposes `foo`; child `b` exposes `a_foo` followed by `foo`; `a_foo` is overwritten and child `b`’s tool becomes unreachable.

18. [servers/office-suite/src/index.ts:105](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:105) — P1 — Assigning `transport.onclose` replaces the SDK client’s close callback, so pending requests are not rejected when a child dies. Fix: chain the prior callback before scheduling restart. Trigger: call a blocking child tool and kill that child; the proxy call remains pending until timeout and a retry can duplicate a completed mutation.

19. [servers/office-suite/src/index.ts:108](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:108) — P2 — A child receives one restart for the entire suite lifetime. Fix: use restart backoff with a circuit state that resets after recovery. Trigger: kill the invoice child, wait for restart, then kill it again; invoicing remains unavailable until the suite restarts.

20. [servers/office-suite/src/index.ts:83](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:83) — P2 — `optional` covers only a missing entry; failure during connect or capability loading aborts the whole suite. Fix: catch the complete optional-child initialization path and continue after cleanup. Trigger: leave the expense child installed but make it exit during initialization; time, spreadsheet, and invoice tools never start.

21. [servers/office-suite/src/index.ts:256](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:256) — P2 — Resource templates are discarded unconditionally. Fix: enumerate each child’s templates, namespace collisions, preserve pagination, and route reads from expanded URIs. Trigger: a child returns one template from `resources/templates/list`; the suite returns an empty array.

22. [servers/office-suite/src/index.ts:249](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:249), [servers/office-suite/src/index.ts:270](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:270) — P2 — Resource and prompt ownership exists only after the corresponding list call. Fix: populate ownership during capability loading or probe children on an owner miss. Trigger: call `resources/read` for `invoices://open`, or `prompts/get` for `monthly_close`, immediately after initialization; both return unknown-owner errors.

23. [servers/office-suite/src/index.ts:250](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:250), [servers/office-suite/src/index.ts:271](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:271) — P2 — Duplicate resource URIs or prompt names overwrite ownership while both definitions remain listed. Fix: namespace collisions and expose the rewritten identifiers consistently. Trigger: two children expose `report://month` or `monthly_close`; listing returns both, but every read/get is routed to the last child.

24. [servers/office-suite/src/index.ts:184](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:184) — P1 — License activation reports success when any child succeeds, even if another rejects the key or crashes. Fix: return `isError:true` unless every connected child succeeds and include per-child status. Trigger: child A activates and child B returns `isError:true`; the aggregate response has `isError:false`.

25. [servers/office-suite/src/index.ts:90](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:90), [servers/office-suite/src/index.ts:121](/Users/mike/mcp-servers/servers/office-suite/src/index.ts:121) — P1 — Child stderr is piped but never consumed, so enough diagnostics apply backpressure and block the child. Fix: use `stderr:"inherit"` or drain each stream into `process.stderr`. Trigger: a child writes more than the pipe buffer to stderr before answering a tool call; the call hangs.

Verdict  
No P0 found. P1 paths can change VAT, mileage, currency amounts, invoice totals, and rebill state; proxy failures can cause missing or repeated money operations.